# [TIL] 2026-09-07 SQL 기본 데이터 처리와 Pandas 데이터 선택·필터링

## 오늘 배운 내용

오늘은 데이터베이스와 데이터 분석 강의를 각각 학습했다.

데이터베이스에서는 SQL을 이용해 테이블을 만들고 데이터를 추가·조회·수정·삭제하는 기본 방법을 학습했고, 데이터 분석에서는 Pandas를 이용해 분석 질문에 필요한 데이터를 선택·필터링·정렬하고 파생 컬럼을 만드는 방법을 학습했다.

두 분야는 목적과 사용하는 도구가 다르기 때문에 하나의 흐름으로 섞지 않고 각각 정리했다.

---

# 데이터베이스

## 1. SQL과 CRUD

관계형 데이터베이스에서는 SQL을 이용해 데이터를 추가하고 조회하고 수정하고 삭제할 수 있다.

기본적인 데이터 처리 작업은 다음과 같다.

CREATE → 생성  
READ → 조회  
UPDATE → 수정  
DELETE → 삭제  

이를 줄여서 CRUD라고 한다.

SQL에서는 보통 다음 명령을 사용한다.

INSERT → 데이터 추가  
SELECT → 데이터 조회  
UPDATE → 데이터 수정  
DELETE → 데이터 삭제  

---

## 2. DDL과 DML

SQL은 목적에 따라 여러 종류로 나눌 수 있다.

### DDL

DDL(Data Definition Language)은 데이터 구조를 정의하는 SQL이다.

대표적으로 테이블을 생성할 때 사용한다.

예시:

CREATE TABLE students (
    id INTEGER,
    name VARCHAR(50)
);

즉, 테이블이라는 데이터 저장 공간 자체를 만드는 작업이다.

### DML

DML(Data Manipulation Language)은 테이블 안의 실제 데이터를 처리하는 SQL이다.

대표적인 명령은 다음과 같다.

INSERT  
SELECT  
UPDATE  
DELETE  

테이블 자체를 만드는 CREATE TABLE과 테이블 안의 데이터를 다루는 INSERT, SELECT 등은 서로 역할이 다르다.

---

## 3. Schema와 Table

PostgreSQL에서는 데이터베이스 안에 Schema가 존재하고, Schema 안에 Table이 존재한다.

Database  
└── Schema  
    └── Table  
        ├── Column  
        └── Row  

예를 들어 다음과 같이 사용할 수 있다.

CREATE TABLE public.students (...);

여기서 public은 스키마 이름이다.

현재 활성화된 스키마가 public이라면 다음처럼 생략할 수도 있다.

CREATE TABLE students (...);

---

## 4. Row와 Column

테이블은 행과 열로 구성된다.

Column  
→ 데이터가 가지고 있는 속성

Row / Record  
→ 하나의 데이터

예를 들어 학생 테이블에 다음 데이터가 있다고 가정한다.

id | name | email | major | grade
1 | 김민지 | minji@test.com | 컴퓨터공학 | 2

여기서 id, name, email, major, grade는 Column이다.

그리고 김민지 학생 한 명의 전체 데이터가 하나의 Row 또는 Record이다.

---

## 5. Primary Key

Primary Key(PK)는 테이블의 각 행을 고유하게 구분하기 위한 값이다.

특징:

- 중복될 수 없다.
- NULL이 될 수 없다.
- 각 행을 고유하게 식별한다.

즉 다음과 같이 이해할 수 있다.

PRIMARY KEY = UNIQUE + NOT NULL

학생 테이블에서는 보통 id를 Primary Key로 사용한다.

---

## 6. 자동 증가 ID

Primary Key로 사용하는 id는 자동 증가 값으로 만들 수 있다.

이 경우 사용자가 직접 ID를 입력하지 않아도 DBMS가 값을 생성한다.

예:

1  
2  
3  
4  

하지만 중간 값이 삭제되거나 생성 과정에서 번호가 건너뛸 수 있기 때문에 ID가 반드시 연속적이라고 생각하면 안 된다.

ID는 순번 자체보다 데이터를 고유하게 식별하기 위한 값으로 이해해야 한다.

---

## 7. 주요 제약 조건

### NOT NULL

반드시 값이 들어가야 한다.

예:

name VARCHAR(50) NOT NULL

값이 없으면 오류가 발생한다.

### UNIQUE

중복된 값을 허용하지 않는다.

예:

email VARCHAR(100) UNIQUE

동일한 이메일을 다시 입력하면 오류가 발생한다.

### DEFAULT

사용자가 값을 입력하지 않았을 때 DBMS가 기본값을 넣는다.

예:

created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

사용자가 시간을 직접 입력하지 않아도 현재 시간이 자동으로 저장된다.

---

## 8. NULL 허용 여부

테이블 생성 시 NOT NULL을 지정하지 않으면 기본적으로 NULL을 허용한다.

예:

major VARCHAR(100)

이 경우 전공 값을 입력하지 않아도 된다.

반대로 다음과 같이 작성하면 반드시 값이 필요하다.

email VARCHAR(100) NOT NULL

제약 조건은 데이터 입력을 불편하게 하기 위한 기능이 아니라 잘못된 데이터가 저장되는 것을 막아 데이터 품질을 유지하기 위한 기능이다.

---

## 9. 주요 데이터 타입

### INTEGER

정수를 저장한다.

예:

grade INTEGER

### BIGINT

INTEGER보다 더 큰 정수를 저장할 수 있다.

로그 데이터나 거래 데이터처럼 매우 많은 행이 생성되는 시스템에서는 BIGINT 사용을 고려할 수 있다.

### VARCHAR

길이가 제한된 문자열을 저장한다.

예:

name VARCHAR(50)

### TEXT

긴 문자열 데이터를 저장할 때 사용한다.

게시판을 예로 들면 제목은 VARCHAR, 본문은 TEXT로 사용할 수 있다.

### TIMESTAMP

날짜와 시간을 함께 저장한다.

---

## 10. INSERT

INSERT는 새로운 데이터를 테이블에 추가할 때 사용한다.

예:

INSERT INTO students (name, email, major, grade)
VALUES ('김민지', 'minji@test.com', '컴퓨터공학', 2);

중요한 점은 컬럼 순서와 VALUES에 입력한 값의 순서가 서로 정확하게 맞아야 한다는 것이다.

자동 증가 ID나 DEFAULT가 지정된 컬럼은 생략할 수 있다.

여러 행을 한 번에 입력할 수도 있다.

예:

INSERT INTO students (name, email, major, grade)
VALUES
    ('김민지', 'minji@test.com', '컴퓨터공학', 2),
    ('이준호', 'junho@test.com', '데이터사이언스', 3);

---

## 11. RETURNING

INSERT 후 DBMS가 자동으로 생성한 값을 확인할 수도 있다.

예:

INSERT INTO students (name, email, major, grade)
VALUES ('김민지', 'minji2@test.com', '컴퓨터공학', 2)
RETURNING id, created_at;

id와 created_at처럼 사용자가 직접 입력하지 않은 값을 확인할 때 유용하다.

---

## 12. SELECT

SELECT는 데이터를 조회할 때 사용한다.

전체 데이터 조회:

SELECT *
FROM students;

특정 컬럼만 조회:

SELECT name, major
FROM students;

SELECT 뒤에 어떤 컬럼을 작성하느냐에 따라 원하는 열만 볼 수 있다.

---

## 13. Alias

조회 결과에서 컬럼 이름을 다른 이름으로 표시할 수 있다.

예:

SELECT name AS student_name
FROM students;

AS는 별칭을 지정할 때 사용한다.

원본 컬럼 이름 자체가 변경되는 것은 아니며 조회 결과에서만 다른 이름으로 보인다.

---

## 14. WHERE

WHERE는 조건에 맞는 데이터만 조회할 때 사용한다.

예:

SELECT *
FROM students
WHERE major = '컴퓨터공학';

숫자 비교도 가능하다.

예:

SELECT *
FROM students
WHERE grade >= 3;

WHERE 뒤에는 조건식이 들어가며 조건 결과가 TRUE인 행만 조회된다.

---

## 15. AND와 OR

여러 조건을 함께 사용할 수 있다.

AND:

SELECT *
FROM students
WHERE major = '컴퓨터공학'
  AND grade >= 3;

두 조건을 모두 만족해야 한다.

OR:

SELECT *
FROM students
WHERE major = '컴퓨터공학'
   OR major = '데이터사이언스';

두 조건 중 하나만 만족해도 된다.

조건이 복잡해질 경우 괄호를 사용하여 우선순위를 명확하게 해야 한다.

---

## 16. IN

같은 컬럼에 여러 값을 비교해야 할 때 OR를 반복하는 대신 IN을 사용할 수 있다.

예:

SELECT *
FROM students
WHERE major IN ('컴퓨터공학', '데이터사이언스', '경영학');

다음과 같은 OR 조건을 간단하게 표현한 것이다.

major = '컴퓨터공학'
OR major = '데이터사이언스'
OR major = '경영학'

IN을 사용하면 코드가 더 간결하고 읽기 쉬워진다.

---

## 17. LIKE

문자열 패턴을 검색할 때 LIKE를 사용한다.

김으로 시작하는 이름 검색:

SELECT *
FROM students
WHERE name LIKE '김%';

AI가 포함된 전공 검색:

SELECT *
FROM students
WHERE major LIKE '%AI%';

%는 길이에 관계없이 여러 문자를 의미한다.

LIKE는 문자열 검색에서 주로 사용한다.

---

## 18. 대소문자 처리

PostgreSQL에서는 문자열 비교 시 대소문자가 구분될 수 있다.

예를 들어 데이터에 AI라고 저장되어 있는데 ai로 검색하면 원하는 결과가 나오지 않을 수 있다.

이럴 때 LOWER() 또는 UPPER()를 활용할 수 있다.

예:

SELECT *
FROM students
WHERE LOWER(major) LIKE LOWER('%ai%');

비교하는 양쪽 값을 같은 대소문자 형태로 변경하여 비교할 수 있다.

---

## 19. NULL 비교

NULL은 일반적인 값과 다르기 때문에 = 연산자로 비교하지 않는다.

잘못된 예:

WHERE major = NULL

올바른 예:

WHERE major IS NULL

NULL이 아닌 데이터는 다음처럼 확인한다.

WHERE major IS NOT NULL

NULL을 비교할 때는 반드시 IS NULL 또는 IS NOT NULL을 사용해야 한다.

---

## 20. DISTINCT

중복된 값을 제거하고 고유한 값만 조회할 때 사용한다.

예:

SELECT DISTINCT major
FROM students;

학생 수가 많더라도 현재 존재하는 전공 종류만 확인할 수 있다.

---

## 21. ORDER BY

데이터를 정렬할 때 사용한다.

오름차순:

SELECT *
FROM students
ORDER BY id ASC;

ASC는 기본값이므로 생략할 수 있다.

내림차순:

SELECT *
FROM students
ORDER BY id DESC;

여러 컬럼을 기준으로 정렬할 수도 있다.

예:

SELECT *
FROM students
ORDER BY major ASC, email DESC;

먼저 major를 기준으로 정렬하고 같은 major 안에서는 email을 기준으로 다시 정렬한다.

---

## 22. LIMIT

조회 결과 중 일부만 보고 싶을 때 LIMIT을 사용한다.

예:

SELECT *
FROM students
ORDER BY grade DESC
LIMIT 3;

학년을 높은 순서대로 정렬한 후 상위 3개 데이터만 조회한다.

기본적인 SELECT 문의 흐름은 다음과 같다.

SELECT
FROM
WHERE
ORDER BY
LIMIT

순서를 지켜 작성해야 한다.

---

## 23. UPDATE

기존 데이터를 수정할 때 사용한다.

예:

UPDATE students
SET grade = 4
WHERE id = 1;

여기서 가장 중요한 것은 WHERE 조건이다.

WHERE 조건을 빼면 모든 행이 수정될 수 있다.

예:

UPDATE students
SET grade = 4;

위 쿼리는 전체 학생의 grade를 4로 바꿀 수 있기 때문에 매우 위험하다.

---

## 24. DELETE

데이터를 삭제할 때 사용한다.

예:

DELETE FROM students
WHERE id = 1;

DELETE 역시 WHERE 조건을 빼면 전체 데이터가 삭제될 수 있다.

따라서 UPDATE와 DELETE는 특히 주의해야 한다.

---

## 25. 안전한 수정·삭제 순서

UPDATE 또는 DELETE를 실행할 때는 먼저 SELECT로 대상을 확인하는 습관이 중요하다.

권장 순서:

1. SELECT로 대상 확인
2. UPDATE 또는 DELETE 실행
3. SELECT로 결과 다시 확인

가능하면 Primary Key를 WHERE 조건으로 사용하는 것이 가장 안전하다.

---

## 데이터베이스에서 가장 중요하게 느낀 것

이번 강의에서는 SQL 문법 자체도 중요하지만 데이터를 안전하게 다루는 습관이 중요하다는 점을 배웠다.

특히 UPDATE와 DELETE는 WHERE 조건 없이 실행하면 전체 데이터에 영향을 줄 수 있기 때문에 반드시 먼저 SELECT로 대상 데이터를 확인해야 한다.

또한 Primary Key, NOT NULL, UNIQUE 같은 제약 조건은 단순한 제한이 아니라 잘못된 데이터가 저장되는 것을 막고 데이터 품질을 유지하기 위한 중요한 기능이라는 점을 이해했다.

---

# 데이터 분석

## 1. 데이터 분석은 질문에서 시작한다

데이터 분석에서는 Pandas 코드를 바로 작성하기보다 먼저 어떤 질문에 답할 것인지 정해야 한다.

예:

완료 주문 기준 카테고리별 매출은 얼마인가?

이 질문을 해결하려면 필요한 데이터와 컬럼을 먼저 찾아야 한다.

필요한 데이터 예:

order_items
- quantity
- unit_price

orders
- order_status

products
- category

분석 과정은 다음과 같이 생각할 수 있다.

수량 × 단가  
↓  
line_total 생성  
↓  
완료 주문만 필터링  
↓  
상품 데이터와 연결  
↓  
카테고리별 그룹  
↓  
매출 합계 계산  

즉, 데이터 분석에서는 코드를 작성하기 전에 질문을 세부 단계로 나누는 과정이 중요하다.

---

## 2. 필요한 DataFrame과 Column 찾기

분석 질문을 해결하려면 어떤 데이터프레임과 컬럼이 필요한지 판단해야 한다.

예를 들어 카테고리별 매출 분석이라면 다음 정보가 필요하다.

수량  
단가  
주문 상태  
상품 카테고리  

이 정보가 각각 어느 데이터프레임에 존재하는지를 찾아야 한다.

실제 프로젝트에서는 데이터프레임이 매우 많아질 수 있기 때문에 어떤 데이터가 어디에 있는지 판단하는 능력이 중요하다.

---

## 3. 결과의 한 행 의미 확인

데이터를 분석할 때 항상 결과의 한 행이 무엇을 의미하는지 확인해야 한다.

예를 들어 order_items의 한 행은 주문 전체가 아니라 주문 안에 포함된 상품 한 건을 의미할 수 있다.

즉 다음을 구분해야 한다.

Order  
→ 주문 한 건

Order Item  
→ 주문 안에 포함된 상품 한 줄

영수증을 생각하면 이해하기 쉽다.

영수증 전체가 하나의 Order이고, 영수증 안에 있는 각각의 상품 내역이 Order Item이다.

---

## 4. Series와 DataFrame

Pandas의 주요 데이터 구조는 Series와 DataFrame이다.

Series는 하나의 컬럼이다.

예:

customers["age"]

DataFrame은 여러 컬럼을 가진 표 형태의 데이터이다.

예:

customers[["customer_id", "age", "city"]]

중요한 점은 컬럼이 하나더라도 리스트 형태로 선택하면 DataFrame이 된다는 것이다.

customers["age"]
→ Series

customers[["age"]]
→ DataFrame

대괄호 사용 방식에 따라 반환되는 자료구조가 달라진다.

---

## 5. 속성과 메서드

DataFrame에는 속성과 메서드가 존재한다.

속성 예:

customers.columns

columns는 데이터프레임이 가지고 있는 컬럼 정보를 나타낸다.

메서드 예:

customers.info()
customers.head()
customers.copy()

일반적으로 뒤에 괄호가 붙으면 메서드라고 이해할 수 있다.

---

## 6. 컬럼 목록 가져오기

현재 데이터프레임에 어떤 컬럼이 있는지 확인할 수 있다.

예:

customers.columns

컬럼을 Python 리스트 형태로 가져오려면 다음처럼 사용할 수 있다.

customers.columns.tolist()

이렇게 가져온 컬럼 목록은 반복문 등에서 동적으로 활용할 수 있다.

---

## 7. copy()와 원본 데이터 보존

데이터 분석에서는 원본 데이터를 직접 수정하지 않고 복사본을 만들어 작업하는 것이 좋다.

예:

customers_work = customers.copy()

이렇게 하면 다음과 같이 구분할 수 있다.

customers
→ 원본 데이터

customers_work
→ 분석용 작업 데이터

분석 과정에서 실수하거나 다른 방식으로 다시 분석해야 할 때 원본을 그대로 사용할 수 있다는 장점이 있다.

---

## 8. 데이터 필터링

특정 조건에 해당하는 행만 선택할 수 있다.

예:

customers_30 = customers[
    customers["age"] >= 30
]

위 코드는 나이가 30세 이상인 고객만 선택한다.

조건식:

customers["age"] >= 30

은 각 행마다 TRUE 또는 FALSE 값을 만든다.

TRUE인 행만 남기기 때문에 이를 Boolean Mask 또는 Boolean Masking이라고 한다.

SQL의 WHERE와 비슷한 역할이다.

---

## 9. Boolean Mask

조건식을 실행하면 내부적으로 다음과 같은 형태가 만들어진다.

True
False
True
True
False

이 결과를 이용하여 True인 행만 선택한다.

예:

customers[
    customers["age"] >= 30
]

데이터 분석에서 매우 많이 사용하는 기본적인 필터링 방법이다.

---

## 10. 복합 조건

여러 조건을 동시에 적용할 수 있다.

30세 이상이면서 서울 거주:

customers[
    (customers["age"] >= 30)
    & (customers["city"] == "서울")
]

&는 AND 역할을 한다.

서울 또는 부산:

customers[
    (customers["city"] == "서울")
    | (customers["city"] == "부산")
]

|는 OR 역할을 한다.

여러 조건을 작성할 때는 각각의 조건을 괄호로 묶어주는 것이 중요하다.

---

## 11. isin()

특정 값들 중 하나에 포함되는 데이터를 찾을 때 isin()을 사용할 수 있다.

예:

customers[
    customers["city"].isin(["서울", "부산"])
]

서울 또는 부산에 거주하는 고객을 선택한다.

SQL의 IN과 비슷한 개념이다.

SQL:

WHERE city IN ('서울', '부산')

Pandas:

customers["city"].isin(["서울", "부산"])

---

## 12. value_counts()

value_counts()는 특정 컬럼에서 각각의 값이 몇 번 등장하는지 확인할 때 사용한다.

예:

customers["city"].value_counts()

결과 예:

성남 21  
광주 17  
부산 16  
서울 15  

이렇게 하면 도시별 고객 수를 빠르게 확인할 수 있다.

데이터 분석을 처음 시작할 때 값의 분포를 확인하는 데 매우 유용하다.

EDA 과정에서 자주 사용하는 기능이다.

---

## 13. sort_values()

sort_values()는 데이터를 정렬할 때 사용한다.

나이 오름차순:

customers.sort_values("age")

나이 내림차순:

customers.sort_values(
    "age",
    ascending=False
)

ascending=True
→ 오름차순

ascending=False
→ 내림차순

SQL의 ORDER BY와 비슷한 역할이다.

---

## 14. 여러 컬럼 기준 정렬

여러 컬럼을 기준으로 정렬할 수도 있다.

예:

products.sort_values(
    ["category", "price"],
    ascending=[True, False]
)

먼저 category를 오름차순으로 정렬한다.

같은 category 안에서는 price를 내림차순으로 정렬한다.

SQL에서 여러 컬럼으로 ORDER BY 하는 것과 비슷하다.

---

## 15. 파생 컬럼

기존 데이터에 없던 새로운 컬럼을 계산하여 만들 수 있다.

이를 파생 컬럼이라고 한다.

예:

order_items_work["line_total"] = (
    order_items_work["quantity"]
    * order_items_work["unit_price"]
)

기존 컬럼:

quantity  
unit_price  

새로운 파생 컬럼:

line_total  

즉 다음 계산을 수행한 것이다.

line_total = quantity × unit_price

---

## 16. 파생 컬럼 활용 예

파생 컬럼은 데이터 분석에서 매우 많이 사용된다.

예:

나이
→ 연령대

날짜
→ 월

가격
→ 가격대

시간
→ 시간대

수량 × 단가
→ 매출 금액

원본 데이터만으로는 분석하기 어려운 경우 새로운 기준을 만들어 분석에 활용할 수 있다.

---

## 17. 프로젝트 경로 확인

데이터 분석 실습에서는 현재 실행 중인 위치를 확인하기 위해 Path.cwd()를 사용할 수 있다.

예:

PROJECT_ROOT = Path.cwd()

Path.cwd()는 현재 작업 중인 폴더를 반환한다.

하지만 Notebook이 하위 폴더에 있을 경우 실제 프로젝트 루트와 다를 수 있다.

이때 .parent를 이용해 상위 폴더로 이동할 수 있다.

예:

PROJECT_ROOT = PROJECT_ROOT.parent

---

## 18. print()를 이용한 디버깅

코드가 원하는 대로 동작하지 않을 때 중간 변수 값을 출력하면 실행 흐름을 확인할 수 있다.

예:

print("PROJECT_ROOT 1:", PROJECT_ROOT)

PROJECT_ROOT = PROJECT_ROOT.parent

print("PROJECT_ROOT 2:", PROJECT_ROOT)

이렇게 하면 코드가 현재 어떤 경로를 가리키고 있는지 확인할 수 있다.

Python은 위에서 아래로 실행되기 때문에 위에서 오류가 발생하면 아래 코드는 실행되지 않는다.

따라서 확인하고 싶은 값은 오류가 발생하기 전에 출력해야 한다.

---

## 19. Path와 문자열 연결

Path 객체와 문자열을 + 연산자로 직접 연결하려고 하면 TypeError가 발생할 수 있다.

잘못된 방식:

DATA_DIR + "/customers.csv"

Path를 사용할 때는 / 연산자를 이용해 경로를 연결할 수 있다.

예:

DATA_DIR / "customers.csv"

이 방식은 운영체제에 맞는 경로를 안전하게 만들어준다.

---

## 20. CSV 읽기

Pandas에서는 read_csv()를 이용해 CSV 파일을 DataFrame으로 읽을 수 있다.

예:

customers = pd.read_csv(
    DATA_DIR / "customers.csv"
)

데이터를 읽은 후에는 먼저 구조를 확인하는 것이 좋다.

예:

customers.info()

info()를 이용하면 다음 내용을 확인할 수 있다.

- 전체 행 수
- 컬럼 수
- 컬럼 이름
- 데이터 타입
- NULL 여부

---

## 21. value_counts()와 sort_values() 차이

이름이 비슷해서 헷갈릴 수 있지만 역할이 다르다.

value_counts()
→ 값별 등장 횟수 확인

sort_values()
→ 데이터 정렬

예:

customers["city"].value_counts()

→ 도시별 고객 수 확인

customers.sort_values("age")

→ 나이 기준 정렬

둘의 역할을 명확하게 구분해야 한다.

---

## 22. 분석 질문을 실제 작업으로 바꾸기

예를 들어 다음 질문이 있다고 가정한다.

완료 주문 기준 카테고리별 매출은 얼마인가?

이 질문을 다음 단계로 나눌 수 있다.

1. order_items에서 quantity와 unit_price 확인
2. quantity × unit_price로 line_total 생성
3. orders와 연결하여 order_status 확인
4. completed 주문만 필터링
5. products와 연결하여 category 확인
6. category별로 그룹
7. line_total 합계 계산
8. 결과 검증

이처럼 분석 질문을 작은 작업 단위로 나누는 것이 중요하다.

---

## 23. Order와 Order Item 구분

Order는 주문 전체를 의미한다.

예:

- 주문 번호
- 주문 날짜
- 고객
- 결제 방법
- 주문 상태

Order Item은 주문 안에 포함된 각각의 상품 내역이다.

예:

- 상품
- 수량
- 단가
- line_total

영수증으로 생각하면 이해하기 쉽다.

영수증 전체
→ Order

영수증에 적힌 각각의 상품 줄
→ Order Item

이 차이를 이해해야 주문 건수와 주문 상세 행 수를 혼동하지 않을 수 있다.

---

## 24. 전체 제품과 주문된 제품의 차이

전체 제품 수는 products 데이터에서 확인할 수 있다.

하지만 실제로 한 번이라도 주문된 제품을 찾으려면 order_items 데이터를 확인해야 한다.

products
→ 판매 가능한 전체 제품

order_items
→ 실제 주문 이력이 있는 제품

즉 어떤 질문을 하느냐에 따라 사용해야 하는 데이터프레임이 달라진다.

---

## 데이터 분석에서 가장 중요하게 느낀 것

이번 강의에서는 Pandas 문법 자체보다 먼저 분석 질문을 구체적으로 이해하는 것이 중요하다는 점을 배웠다.

예를 들어 카테고리별 매출을 계산한다고 해서 바로 groupby를 사용하는 것이 아니라 먼저 어떤 데이터프레임과 어떤 컬럼이 필요한지 찾아야 한다.

또한 원본 데이터를 직접 변경하기보다 copy()를 이용해 작업용 데이터를 만들고, 기존 컬럼을 이용해 파생 컬럼을 생성하는 습관이 중요하다는 점을 알게 되었다.

데이터 분석에서는 코드를 외우는 것보다 질문을 작은 단계로 나누고 각 단계에서 필요한 데이터를 찾는 사고 과정이 중요하다.

---

# 실습·복습 체크리스트

## 데이터베이스

- [ ] CRUD의 의미를 설명할 수 있다.
- [ ] DDL과 DML의 차이를 설명할 수 있다.
- [ ] Schema와 Table의 관계를 설명할 수 있다.
- [ ] Row와 Column의 차이를 설명할 수 있다.
- [ ] Primary Key의 역할을 설명할 수 있다.
- [ ] NOT NULL, UNIQUE, DEFAULT의 차이를 설명할 수 있다.
- [ ] INSERT로 데이터를 추가할 수 있다.
- [ ] SELECT로 원하는 컬럼을 조회할 수 있다.
- [ ] WHERE로 조건을 적용할 수 있다.
- [ ] AND와 OR의 차이를 이해한다.
- [ ] IN을 사용할 수 있다.
- [ ] LIKE를 이용해 문자열을 검색할 수 있다.
- [ ] NULL을 IS NULL로 비교할 수 있다.
- [ ] DISTINCT로 중복 값을 제거할 수 있다.
- [ ] ORDER BY로 정렬할 수 있다.
- [ ] LIMIT으로 일부 데이터만 조회할 수 있다.
- [ ] UPDATE 전에 SELECT로 대상을 확인한다.
- [ ] DELETE 전에 SELECT로 대상을 확인한다.

## 데이터 분석

- [ ] 분석 질문에 필요한 DataFrame을 찾을 수 있다.
- [ ] 필요한 Column을 선택할 수 있다.
- [ ] Series와 DataFrame의 차이를 설명할 수 있다.
- [ ] 속성과 메서드를 구분할 수 있다.
- [ ] columns.tolist()를 사용할 수 있다.
- [ ] Boolean Mask를 이용해 데이터를 필터링할 수 있다.
- [ ] &, |를 이용해 복합 조건을 작성할 수 있다.
- [ ] isin()을 사용할 수 있다.
- [ ] value_counts()로 값의 빈도를 확인할 수 있다.
- [ ] sort_values()로 데이터를 정렬할 수 있다.
- [ ] copy()를 이용해 원본 데이터를 보존할 수 있다.
- [ ] 파생 컬럼을 만들 수 있다.
- [ ] Path.cwd()와 .parent를 이용해 경로를 확인할 수 있다.
- [ ] print()를 이용해 실행 흐름을 확인할 수 있다.
- [ ] Path 객체에서 / 연산자로 경로를 연결할 수 있다.
- [ ] 분석 질문을 작은 작업 단계로 나눌 수 있다.

---

# 오늘의 핵심 한 줄

데이터베이스에서는 SQL로 데이터를 안전하게 저장·조회·수정하고, 데이터 분석에서는 질문에 필요한 데이터를 선택·필터링·가공하여 의미 있는 결과를 만들어낸다.

---

# 다음에 복습할 것

## 데이터베이스

- SELECT / WHERE / ORDER BY / LIMIT 조합
- AND와 OR
- IN과 LIKE
- NULL 처리
- UPDATE와 DELETE 안전하게 사용하기
- Primary Key와 제약 조건

## 데이터 분석

- Boolean Filtering
- isin()
- value_counts()
- sort_values()
- copy()
- 파생 컬럼
- DataFrame과 Series
- 분석 질문에서 필요한 데이터 찾기
- Order와 Order Item 구분
- groupby와 merge로 이어지는 분석 흐름
