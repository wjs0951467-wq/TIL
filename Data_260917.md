# 2026-09-17 TIL JOIN과 GROUP BY, 데이터 시각화의 기본 흐름

## 오늘 배운 것

- 정규화된 테이블을 `JOIN`으로 연결하는 방법
- `INNER JOIN`과 `LEFT JOIN`의 차이
- `GROUP BY`와 집계 함수 사용 규칙
- `COUNT(*)`, `COUNT(column)`, `COUNT(DISTINCT column)`의 차이
- IP 주소, 포트, 도메인, DNS, hosts 파일의 관계
- 분석 질문에 맞는 그래프 선택 방법
- pandas로 시각화용 데이터를 집계하고 검증하는 과정

---

## 데이터베이스

### 1. 정규화와 JOIN

정규화는 데이터의 중복을 줄이고 각 데이터를 알맞은 테이블로 분리하는 과정이다.

예를 들어 강사 이름은 강의의 속성이 아니라 강사의 속성이므로 `instructors` 테이블에 저장한다. 분리된 데이터가 필요할 때는 기본키와 외래키를 기준으로 테이블을 연결한다.

```sql
SELECT
    c.id,
    c.title,
    i.name AS instructor_name
FROM courses AS c
INNER JOIN instructors AS i
    ON c.instructor_id = i.id;
```

- `ON`: 어떤 컬럼끼리 연결할지 지정한다.
- `별칭.컬럼명`: 컬럼의 출처를 명확하게 표시한다.
- 필요한 데이터가 다른 테이블에 있다면 관계를 따라 JOIN을 추가한다.

### 2. INNER JOIN과 LEFT JOIN

#### INNER JOIN

양쪽 테이블에 연결되는 데이터가 모두 있을 때만 결과에 포함한다.

```sql
SELECT
    s.name,
    e.status
FROM students AS s
INNER JOIN enrollments AS e
    ON s.id = e.student_id;
```

수강 신청 기록이 없는 학생은 결과에서 제외된다.

`JOIN`만 작성해도 기본적으로 `INNER JOIN`을 의미한다.

#### LEFT JOIN

왼쪽 테이블의 데이터는 모두 유지하고, 연결되는 기록이 없으면 오른쪽 테이블의 컬럼을 `NULL`로 표시한다.

```sql
SELECT
    s.name,
    e.status
FROM students AS s
LEFT JOIN enrollments AS e
    ON s.id = e.student_id;
```

회원가입만 하고 수강 신청을 하지 않은 학생까지 확인하려면 `LEFT JOIN`이 적합하다.

JOIN을 선택하기 전에는 다음 요구사항을 먼저 확인해야 한다.

- 기록이 없는 대상도 포함할 것인가?
- 연결된 기록이 있는 대상만 볼 것인가?

### 3. ON과 WHERE의 역할

- `ON`: 테이블을 연결하는 조건
- `WHERE`: 연결된 결과에서 필요한 행을 거르는 조건

특히 `LEFT JOIN`에서 오른쪽 테이블의 조건을 `WHERE`에 작성하면 `NULL`인 행이 제거된다. 이 경우 결과가 사실상 `INNER JOIN`처럼 달라질 수 있다.

```sql
SELECT
    s.name,
    e.status
FROM students AS s
LEFT JOIN enrollments AS e
    ON s.id = e.student_id
   AND e.status = '수강중';
```

조건의 위치는 단순한 문법 차이가 아니라 결과의 범위에 영향을 준다. 따라서 조건을 변경한 후에는 반드시 결과 건수를 비교해야 한다.

### 4. GROUP BY와 집계 함수

`GROUP BY`는 여러 행을 같은 기준끼리 묶어서 요약할 때 사용한다.

```sql
SELECT
    status,
    COUNT(*) AS enrollment_count
FROM enrollments
GROUP BY status;
```

주요 집계 함수는 다음과 같다.

- `COUNT`: 개수
- `SUM`: 합계
- `AVG`: 평균
- `MIN`: 최솟값
- `MAX`: 최댓값

기본 원칙은 `SELECT`에 집계되지 않은 일반 컬럼을 사용하려면 해당 컬럼을 `GROUP BY`에도 포함하는 것이다.

```sql
SELECT
    course_id,
    status,
    COUNT(*) AS enrollment_count
FROM enrollments
GROUP BY course_id, status;
```

강의 ID와 제목을 함께 표시할 때는 중복될 수 있는 제목만 기준으로 삼지 않고, 고유한 ID도 함께 사용한다.

```sql
SELECT
    c.id,
    c.title,
    COUNT(e.id) AS enrollment_count
FROM courses AS c
LEFT JOIN enrollments AS e
    ON c.id = e.course_id
GROUP BY c.id, c.title;
```

### 5. PostgreSQL의 GROUP BY 예외

PostgreSQL에서는 한 테이블의 기본키를 `GROUP BY`에 넣으면 그 기본키에 의해 값이 하나로 결정되는 같은 테이블의 다른 컬럼을 `SELECT`에 허용하는 경우가 있다.

```sql
SELECT
    c.id,
    c.title,
    COUNT(e.id)
FROM courses AS c
LEFT JOIN enrollments AS e
    ON c.id = e.course_id
GROUP BY c.id;
```

위 쿼리는 PostgreSQL에서 실행될 수 있다.

하지만 모든 상황과 DBMS에서 동일하게 동작한다고 볼 수 없다. 혼란을 줄이고 쿼리의 의도를 분명히 하기 위해 다음처럼 작성하는 편이 안전하다.

```sql
GROUP BY c.id, c.title
```

반면 기본키가 아닌 `student_id` 등으로 그룹화했다면 다른 일반 컬럼을 임의로 `SELECT`할 수 없다.

### 6. COUNT의 차이

```sql
COUNT(*)
COUNT(enrollment_id)
COUNT(DISTINCT student_id)
```

- `COUNT(*)`: `NULL` 여부와 관계없이 모든 행을 계산한다.
- `COUNT(column)`: 해당 컬럼이 `NULL`이 아닌 행만 계산한다.
- `COUNT(DISTINCT column)`: `NULL`과 중복을 제외한 고유값을 계산한다.

같은 데이터에서도 무엇을 세는지에 따라 결과가 달라진다. 따라서 집계 기준을 명확하게 정해야 한다.

### 7. NULL 처리와 업무 순서 정렬

집계 결과가 `NULL`일 때 `0`으로 표시하려면 `COALESCE`를 사용할 수 있다.

```sql
COALESCE(SUM(e.amount), 0) AS total_amount
```

문자열을 사전순이 아닌 업무 흐름에 맞게 정렬하려면 `CASE`를 사용한다.

```sql
ORDER BY CASE status
    WHEN '신청' THEN 1
    WHEN '수강중' THEN 2
    WHEN '완료' THEN 3
    WHEN '취소' THEN 4
    ELSE 99
END;
```

### 8. 서브쿼리

서브쿼리는 쿼리 안에 작성한 또 다른 쿼리이다.

안쪽 쿼리가 먼저 실행되고, 그 결과를 임시 테이블처럼 바깥 쿼리에서 사용한다.

```sql
SELECT SUM(enrollment_count)
FROM (
    SELECT
        status,
        COUNT(*) AS enrollment_count
    FROM enrollments
    GROUP BY status
) AS status_summary;
```

한 번의 쿼리로 처리하기 어려운 작업에 사용할 수 있지만, JOIN이나 단순 집계로 해결할 수 있는지도 먼저 확인해야 한다.

### 9. IP, 포트, 도메인과 DNS

- IP 주소: 네트워크에서 컴퓨터나 서버를 찾기 위한 주소
- 포트: 한 서버 안에서 어떤 서비스를 사용할지 구분하는 번호
- 도메인: 사람이 기억하기 쉬운 서버 이름
- DNS: 도메인을 IP 주소로 변환하는 시스템
- DHCP: 사용 가능한 IP 주소를 자동으로 할당하는 방식

브라우저에서 도메인을 입력하면 먼저 로컬의 `hosts` 파일을 확인하고, 등록된 정보가 없으면 DNS 서버에 IP 주소를 요청한다.

Windows의 hosts 파일 위치는 다음과 같다.

```text
C:\Windows\System32\drivers\etc\hosts
```

다음과 같이 IP 주소와 사용할 도메인을 연결할 수 있다.

```text
10.2.16.254 kant.test
```

이후 브라우저에서 다음 주소로 접속할 수 있다.

```text
http://kant.test:8000
```

서버의 IP 주소가 바뀌면 hosts 파일에 작성한 IP 주소도 수정해야 한다.

기본적인 네트워크 확인 명령어는 다음과 같다.

```powershell
ping kant.test
ipconfig /all
ipconfig /flushdns
```

---

## 데이터 분석

### 1. 시각화의 목적

시각화는 그래프를 그리는 것 자체가 목적이 아니다.

분석 질문에 답하고 데이터를 쉽게 이해할 수 있도록 표현하는 과정이다.

```text
분석 질문 설정
→ 데이터 범위 확인
→ 전처리 및 집계
→ 집계 결과 검증
→ 그래프 선택과 작성
→ 원본 데이터와 교차 검증
→ 결과 해석
```

### 2. 질문에 맞는 그래프 선택

| 확인하려는 내용 | 적합한 그래프 |
| --- | --- |
| 범주별 크기 비교 | 막대 그래프 |
| 시간에 따른 변화 | 선 그래프 |
| 연속형 값의 분포 | 히스토그램 |
| 두 숫자형 변수의 관계 | 산점도 |
| 전체에서 차지하는 비율 | 파이 차트 |

파이 차트는 비율을 보여줄 수 있지만 크기가 비슷한 항목은 비교하기 어렵다.

정확한 크기 비교가 목적이라면 막대 그래프가 더 효과적일 수 있다.

### 3. 시각화 전 데이터 준비

원본 데이터보다 전처리가 끝난 `processed` 데이터를 사용하는 것이 안전하다.

```python
orders["order_date"] = pd.to_datetime(
    orders["order_date"],
    errors="raise"
)
```

날짜를 문자열 상태로 두면 월별 집계가 어렵기 때문에 `datetime` 형식으로 변환한다.

변환 후에는 다음 항목을 다시 확인해야 한다.

- 데이터 타입
- 결측치
- 중복값
- 금액 컬럼
- 데이터 범위

여러 데이터가 필요하면 `merge()`로 통합한 뒤 분석에 필요한 컬럼이 모두 들어 있는지 검증한다.

### 4. pandas 집계

카테고리별 매출과 판매 수량은 다음과 같이 계산할 수 있다.

```python
category_sales = (
    df_visualization
    .groupby(
        "category",
        dropna=False,
        as_index=False
    )
    .agg(
        total_amount=("line_total", "sum"),
        total_quantity=("quantity", "sum"),
    )
    .sort_values(
        "total_amount",
        ascending=False
    )
)
```

- `dropna=False`: 결측 범주도 집계에서 제외하지 않는다.
- `as_index=False`: 그룹 기준을 일반 컬럼으로 유지한다.
- `sum`: 실제 수량과 금액의 합계를 계산한다.
- `sort_values`: 그래프에서 비교하기 쉽도록 정렬한다.

수량의 합계를 구할 때 `count`를 사용하면 행의 개수가 계산된다.

판매 수량 컬럼에 들어 있는 실제 값을 더하려면 `sum`을 사용해야 한다.

### 5. 막대 그래프

막대 그래프는 카테고리별 매출처럼 범주 간 크기를 비교할 때 사용한다.

```python
fig, ax = plt.subplots(figsize=(10, 5))

ax.bar(
    category_sales["category"],
    category_sales["total_amount"]
)

ax.set_title("카테고리별 매출 금액")
ax.set_xlabel("카테고리")
ax.set_ylabel("매출 금액")

plt.show()
```

그래프를 그리기 전에 집계 결과가 원하는 기준으로 정렬되었는지 확인해야 한다.

### 6. 선 그래프

선 그래프는 월별 매출처럼 시간 흐름에 따른 변화를 확인할 때 사용한다.

월별 데이터를 만들 때는 주문 날짜에서 연도와 월을 추출한 뒤 집계한다.

```python
monthly_sales = (
    df_visualization
    .groupby(
        "order_month",
        as_index=False
    )
    .agg(
        total_amount=("line_total", "sum"),
        order_count=("order_id", "nunique"),
    )
    .sort_values("order_month")
)
```

시간 데이터가 뒤섞이지 않도록 날짜 순서로 정렬해야 한다.

날짜 레이블이 겹치면 `rotation`을 사용해 글자를 기울일 수 있다.

### 7. 히스토그램과 bins

히스토그램은 고객 나이, 상품 가격처럼 연속형 값이 어느 구간에 많이 분포하는지 확인할 때 사용한다.

```python
fig, ax = plt.subplots(figsize=(10, 5))

ax.hist(
    customers["age"].dropna(),
    bins=10
)

ax.set_title("고객 나이 분포")
ax.set_xlabel("나이")
ax.set_ylabel("고객 수")

plt.show()
```

- `bins`가 적으면 넓은 구간으로 묶여 전체 흐름이 잘 보인다.
- `bins`가 많으면 세밀한 차이를 볼 수 있지만 그래프가 복잡해질 수 있다.
- 극단적으로 큰 값이 있으면 나머지 데이터가 한쪽에 몰려 보일 수 있다.

히스토그램을 그리기 전에 `describe()`로 최솟값, 최댓값, 평균과 분포를 확인하는 것이 좋다.

### 8. 산점도와 상관관계

산점도는 두 숫자형 변수의 관계와 대략적인 추세를 확인할 때 사용한다.

- 우상향: 한 변수가 증가할 때 다른 변수도 증가하는 경향
- 우하향: 한 변수가 증가할 때 다른 변수는 감소하는 경향
- 불규칙한 분포: 뚜렷한 관계가 보이지 않음

그래프에서 추세가 보여도 곧바로 인과관계나 확실한 상관관계라고 결론 내리면 안 된다.

정확하게 판단하려면 상관분석이나 가설검정 같은 통계 검증이 필요하다.

### 9. 상위 고객 분석

고객별 총 구매 금액과 주문 횟수를 집계한 뒤 정렬하면 상위 고객을 확인할 수 있다.

```python
customer_sales = (
    df_visualization
    .groupby(
        "customer_id",
        dropna=False,
        as_index=False
    )
    .agg(
        total_amount=("line_total", "sum"),
        order_count=("order_id", "nunique"),
    )
    .sort_values(
        "total_amount",
        ascending=False
    )
)

top_customers = customer_sales.head(10)
```

주문 하나에 여러 상품 행이 있을 수 있으므로 주문 횟수는 `count`보다 `nunique`로 계산하는 것이 정확하다.

단, VIP의 기준은 서비스 목적에 따라 먼저 정의해야 한다.

- 총 구매 금액
- 주문 횟수
- 최근 구매일
- 일정 기간의 구매 활동

### 10. 한글 폰트와 실행 환경

Matplotlib은 기본 설정만으로 한글이 깨질 수 있으므로 운영체제에 설치된 한글 폰트를 지정해야 한다.

Windows와 macOS는 폰트 이름과 경로가 다를 수 있으므로 다른 환경에서도 정상적으로 실행되는지 확인해야 한다.

---

## 가장 중요하게 느낀 것

SQL과 데이터 시각화는 모두 먼저 기준을 정하는 것이 중요했다.

- JOIN: 기록이 없는 대상까지 포함할 것인지 결정한다.
- GROUP BY: 어떤 단위로 묶고 무엇을 계산할지 결정한다.
- COUNT: 행, 결측치 제외 값, 고유값 중 무엇을 셀지 결정한다.
- 시각화: 무엇을 보여줄지 정한 뒤 그래프를 선택한다.
- 해석: 그래프만 믿지 않고 집계표와 원본 데이터로 교차 검증한다.

## 실습·복습 체크리스트

- [ ] 기본키와 외래키를 찾아 JOIN 조건을 설명할 수 있다.
- [ ] INNER JOIN과 LEFT JOIN의 차이를 설명할 수 있다.
- [ ] LEFT JOIN에서 ON과 WHERE의 조건 위치를 비교할 수 있다.
- [ ] GROUP BY에 들어갈 컬럼을 직접 판단할 수 있다.
- [ ] 세 가지 COUNT의 차이를 설명할 수 있다.
- [ ] NULL을 COALESCE로 처리할 수 있다.
- [ ] 질문에 맞는 그래프를 선택할 수 있다.
- [ ] 집계 결과와 원본 데이터의 건수 및 합계를 교차 검증할 수 있다.
- [ ] 산점도의 추세를 인과관계로 단정하지 않는다.

## 오늘의 핵심 한 줄

데이터를 정확히 연결하고 집계한 뒤, 질문에 맞는 그래프로 표현하고 원본과 교차 검증해야 올바른 해석이 가능하다.
