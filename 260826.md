# 2026-08-26 TIL 딕셔너리(Dictionary)로 데이터 관리하기

## 오늘 배운 것

오늘은 파이썬의 네 가지 주요 복합 자료 구조 중 하나인 **딕셔너리(Dictionary)**를 배웠다.

지금까지 리스트(List), 튜플(Tuple), 셋(Set)을 배웠고, 이번에는 데이터를 **키(Key)와 값(Value)의 형태로 저장하고 관리하는 딕셔너리**를 학습했다.

딕셔너리는 데이터를 단순히 순서대로 저장하는 것이 아니라,

```python
키(Key) : 값(Value)
```

형태로 저장한다.

예를 들어 학생의 정보를 저장한다면 다음과 같이 만들 수 있다.

```python
student = {
    "name": "홍길동",
    "age": 20,
    "major": "컴퓨터공학"
}
```

여기서 `"name"`, `"age"`, `"major"`가 **키(Key)**이고, `"홍길동"`, `20`, `"컴퓨터공학"`이 **값(Value)**이다.

즉, 딕셔너리는 **어떤 데이터가 무엇을 의미하는지 명확하게 표현하면서 저장할 수 있는 자료 구조**라고 이해하면 된다.

---

## 1. 딕셔너리의 기본 구조

딕셔너리는 중괄호 `{}`를 사용해서 만든다.

```python
변수명 = {
    키: 값,
    키: 값,
    키: 값
}
```

예를 들어 사람의 정보를 저장하면:

```python
person = {
    "name": "철수",
    "age": 25,
    "job": "개발자"
}
```

구조를 보면 다음과 같다.

```text
"name" → "철수"
"age"  → 25
"job"  → "개발자"
```

여기서 중요한 것은 **키와 값이 한 쌍으로 연결되어 있다는 것**이다.

이 한 쌍을 흔히 **Key-Value Pair(키-값 쌍)**라고 한다.

---

## 2. 딕셔너리에서 값을 가져오기

딕셔너리의 값을 가져올 때는 **키를 이용한다.**

```python
person = {
    "name": "철수",
    "age": 25
}

print(person["name"])
```

실행 결과:

```text
철수
```

`person["name"]`이라고 작성하면 `"name"`이라는 키에 연결된 값을 가져온다.

```python
print(person["age"])
```

결과:

```text
25
```

따라서 딕셔너리는 리스트처럼 숫자 인덱스로 접근하는 것이 아니라 **키를 이용해서 원하는 값을 찾는 자료 구조**라고 이해해야 한다.

---

## 3. 새로운 데이터 추가하기

딕셔너리에 새로운 키와 값을 추가할 수도 있다.

```python
person = {
    "name": "철수",
    "age": 25
}

person["job"] = "개발자"
```

그러면 딕셔너리는 다음과 같이 변경된다.

```python
{
    "name": "철수",
    "age": 25,
    "job": "개발자"
}
```

즉,

```python
딕셔너리[새로운 키] = 새로운 값
```

형태로 작성하면 새로운 데이터를 추가할 수 있다.

---

## 4. 기존 값 수정하기

이미 존재하는 키에 새로운 값을 넣으면 기존 값이 수정된다.

```python
person = {
    "name": "철수",
    "age": 25
}

person["age"] = 26
```

결과:

```python
{
    "name": "철수",
    "age": 26
}
```

새로운 키를 사용하면 **추가**되고, 기존 키를 사용하면 **수정**된다.

이 차이를 기억하는 것이 중요하다.

```python
person["job"] = "개발자"   # 새로운 키 → 추가

person["age"] = 26         # 기존 키 → 수정
```

---

## 5. 딕셔너리에서 데이터 삭제하기

딕셔너리의 특정 데이터를 삭제할 때는 `del`을 사용할 수 있다.

```python
person = {
    "name": "철수",
    "age": 25,
    "job": "개발자"
}

del person["age"]
```

그러면 `"age"`에 해당하는 데이터가 삭제된다.

```python
{
    "name": "철수",
    "job": "개발자"
}
```

즉,

```python
del 딕셔너리[키]
```

형태로 특정 항목을 삭제할 수 있다.

---

## 6. 딕셔너리의 특징

딕셔너리를 사용할 때 알아야 하는 핵심 특징이 있다.

### ① 키와 값으로 데이터를 저장한다

딕셔너리는 데이터를 다음과 같이 관리한다.

```text
Key → Value
```

예:

```python
user = {
    "id": "user01",
    "name": "홍길동",
    "age": 20
}
```

데이터가 무엇을 의미하는지 바로 확인할 수 있다는 장점이 있다.

---

### ② 키는 중복될 수 없다

딕셔너리에서 키는 데이터를 구분하기 위한 식별자이기 때문에 **같은 키를 여러 번 사용할 수 없다.**

예를 들어:

```python
person = {
    "name": "철수",
    "name": "영희"
}
```

이렇게 작성하면 `"name"` 키가 중복된다.

결과적으로 마지막에 저장된 값이 남게 된다.

따라서 딕셔너리에서 키는 **각 데이터를 구분할 수 있는 고유한 값**이라고 생각하는 것이 좋다.

---

### ③ 값은 다양한 자료형을 사용할 수 있다

딕셔너리의 값에는 문자열, 숫자, 리스트 등 다양한 데이터를 저장할 수 있다.

```python
person = {
    "name": "철수",
    "age": 25,
    "hobbies": ["게임", "영화", "운동"]
}
```

여기서 `"hobbies"`의 값은 리스트이다.

즉, 딕셔너리 안에 또 다른 자료 구조를 넣어서 복잡한 데이터를 관리할 수도 있다.

---

## 7. 딕셔너리와 리스트의 차이

딕셔너리와 리스트는 둘 다 여러 데이터를 저장할 수 있지만 데이터를 찾는 방식이 다르다.

### 리스트

리스트는 **순서와 인덱스**를 기준으로 데이터를 찾는다.

```python
fruits = ["사과", "바나나", "포도"]

print(fruits[0])
```

결과:

```text
사과
```

---

### 딕셔너리

딕셔너리는 **키**를 이용해서 데이터를 찾는다.

```python
person = {
    "name": "철수",
    "age": 25
}

print(person["name"])
```

결과:

```text
철수
```

정리하면:

```text
리스트      → 인덱스로 접근
딕셔너리    → 키로 접근
```

---

## 8. 딕셔너리가 유용한 이유

딕셔너리는 실제 프로그램에서 **특정 대상의 정보를 묶어서 관리할 때** 매우 유용하다.

예를 들어 회원 정보를 관리한다고 생각해보면:

```python
user = {
    "id": "user01",
    "name": "김철수",
    "age": 25,
    "email": "test@example.com"
}
```

리스트로 관리하는 것보다 각각의 데이터가 무엇을 의미하는지 훨씬 명확하다.

특히 데이터를 처리하는 프로그램에서는 다음과 같이 다양한 정보를 하나의 객체처럼 묶어서 사용하는 경우가 많다.

```python
product = {
    "name": "노트북",
    "price": 1200000,
    "brand": "ABC"
}
```

이런 구조는 웹 개발이나 데이터 처리에서도 자주 사용된다.

---

## 9. 오늘 가장 중요하게 느낀 것

오늘 배운 딕셔너리에서 가장 중요한 개념은 **"키(Key)를 이용해서 값(Value)을 찾는다"**는 것이다.

리스트에서는

```python
list[0]
list[1]
list[2]
```

처럼 위치를 기준으로 데이터를 찾았다.

반면 딕셔너리에서는

```python
dictionary["name"]
dictionary["age"]
```

처럼 **데이터의 이름에 해당하는 키를 이용해서 값을 찾는다.**

따라서 딕셔너리를 이해할 때는 단순히

> "중괄호를 사용하는 자료형"

이라고 외우는 것보다

> **"키와 값이 한 쌍으로 연결되어 있는 자료 구조"**

라고 이해하는 것이 중요하다.

---

## 10. 실습·복습 체크리스트

* [ ] 딕셔너리의 기본 구조를 직접 작성해보기
* [ ] Key와 Value의 차이를 설명할 수 있는지 확인하기
* [ ] 키를 이용해서 값을 가져와보기
* [ ] 새로운 Key-Value를 추가해보기
* [ ] 기존 값을 수정해보기
* [ ] `del`을 사용해서 데이터를 삭제해보기
* [ ] 리스트와 딕셔너리의 데이터 접근 방식 차이 이해하기
* [ ] 딕셔너리에서 키가 중복될 수 없는 이유 이해하기
* [ ] 문자열, 숫자, 리스트 등을 값으로 넣어보기
* [ ] 실제 회원 정보나 상품 정보를 딕셔너리로 만들어보기


---

## 11. 오늘의 핵심 정리

```text
딕셔너리 = Key와 Value를 한 쌍으로 저장하는 자료 구조

Key   → 데이터를 찾기 위한 이름
Value → 실제 저장되는 데이터

값 가져오기
dictionary["key"]

추가
dictionary["new_key"] = value

수정
dictionary["existing_key"] = new_value

삭제
del dictionary["key"]
```

### 오늘의 핵심 한 줄

**딕셔너리는 데이터를 Key와 Value의 한 쌍으로 저장하고, Key를 이용해서 원하는 Value를 빠르게 찾아 사용하는 자료 구조이다.**
