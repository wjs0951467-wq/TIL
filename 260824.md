# 2026-08-24 TIL 리스트, 튜플, 집합(Set)

## 오늘 배운 것

오늘은 파이썬에서 여러 개의 데이터를 하나로 묶어서 관리할 때 사용하는 자료구조인 리스트, 튜플, 집합(Set)에 대해 배웠다.

세 자료구조는 여러 개의 값을 저장한다는 공통점이 있지만, 각각 데이터를 다루는 방식이 다르다.

자료구조를 선택할 때는 다음 세 가지를 생각하면 된다.

1. 순서가 중요한가?
2. 값을 수정할 것인가?
3. 중복을 허용할 것인가?

이 세 가지 기준을 이해하는 것이 오늘 내용의 핵심이다.

---

## 핵심 개념

### 1. 리스트(List)

리스트는 파이썬에서 가장 기본적이고 많이 사용하는 자료구조이다.

'''python
colors = ["red", "green", "blue"]
'''

리스트는 대괄호 `[]`를 사용해서 만든다.

리스트의 특징은 다음과 같다.

- 순서가 있다.
- 인덱스를 사용할 수 있다.
- 중복을 허용한다.
- 값을 수정할 수 있다.
- 값을 추가하거나 삭제할 수 있다.

예를 들어:

'''python
colors = ["red", "green", "blue"]

colors[0] = "black"

print(colors)
'''

결과:

'''text
["black", "green", "blue"]
'''

리스트는 데이터를 계속 추가하거나 수정해야 하는 경우에 적합하다.

대표적인 리스트 메소드에는 `append()`, `insert()`, `remove()` 등이 있다.

'''python
colors.append("yellow")
colors.insert(1, "white")
colors.remove("green")
'''

- `append()` : 리스트 마지막에 값 추가
- `insert()` : 원하는 위치에 값 추가
- `remove()` : 특정 값 삭제

---

### 2. 튜플(Tuple)

튜플은 리스트와 비슷하게 여러 개의 값을 저장하지만 가장 중요한 차이점은 **수정할 수 없다는 것**이다.

'''python
colors = ("red", "green", "blue")
'''

튜플은 소괄호 `()`를 사용한다.

튜플의 특징은 다음과 같다.

- 순서가 있다.
- 인덱스를 사용할 수 있다.
- 중복을 허용한다.
- 값을 수정할 수 없다.
- 정해진 값을 그대로 유지해야 할 때 적합하다.

예를 들어 RGB 색상처럼 정해진 값을 저장할 수 있다.

'''python
colors = ("red", "green", "blue")
'''

좌표처럼 한 번 정해지면 변경할 필요가 없는 데이터도 튜플로 표현할 수 있다.

'''python
location = (37.5, 127.0)
'''

위도와 경도처럼 하나의 위치를 나타내는 값의 조합을 고정해서 사용할 수 있다.

---

### 3. 리스트와 튜플의 가장 큰 차이

리스트와 튜플은 모두 순서가 있기 때문에 인덱싱과 슬라이싱이 가능하다.

하지만 리스트는 값을 수정할 수 있고, 튜플은 값을 수정할 수 없다.

'''python
colors = ["red", "green", "blue"]
colors[0] = "black"
print(colors)
'''

리스트에서는 정상적으로 수정된다.

반면 튜플에서는:

'''python
colors = ("red", "green", "blue")
colors[0] = "black"
'''

수정할 수 없기 때문에 `TypeError`가 발생한다.

즉, **변경해야 하는 데이터라면 리스트, 변경하면 안 되는 데이터라면 튜플**을 생각하면 된다.

---

### 4. 요소가 하나인 튜플

튜플에서 특히 주의해야 하는 부분이 있다.

요소가 하나인 튜플을 만들 때는 **쉼표가 반드시 필요하다.**

'''python
value = (10)
'''

이것은 튜플이 아니라 정수 `10`이다.

'''python
value = (10,)
'''

이렇게 쉼표를 넣어야 튜플이 된다.

즉, 튜플을 만들 때 소괄호도 사용하지만, 요소가 하나일 경우에는 **쉼표가 튜플임을 결정한다.**

데이터 타입은 `type()`으로 확인할 수 있다.

'''python
value = (10,)

print(type(value))
'''

결과:

'''text
<class 'tuple'>
'''

---

### 5. 튜플의 인덱싱

튜플은 순서가 있기 때문에 리스트처럼 인덱스로 값을 가져올 수 있다.

'''python
cities = ("서울", "부산", "제주")

print(cities[0])
print(cities[1])
print(cities[-1])
'''

결과:

'''text
서울
부산
제주
'''

인덱스는 0부터 시작한다.

- `0` → 서울
- `1` → 부산
- `2` → 제주
- `-1` → 마지막 값인 제주

---

### 6. 튜플의 슬라이싱

튜플은 리스트와 마찬가지로 슬라이싱도 사용할 수 있다.

'''python
cities = ("서울", "부산", "제주")

print(cities[0:2])
'''

결과:

'''text
("서울", "부산")
'''

슬라이싱은 시작 인덱스부터 마지막 인덱스 바로 앞까지 가져온다.

따라서 `0:2`는 0번과 1번 요소를 가져온다.

---

### 7. len()과 마지막 요소

`len()`은 자료구조에 들어 있는 요소의 개수를 알려주는 함수이다.

'''python
cities = ("서울", "부산", "제주")

print(len(cities))
'''

결과:

'''text
3
'''

마지막 요소를 가져올 때는 `-1`을 사용할 수 있다.

'''python
print(cities[-1])
'''

또는 `len()`을 이용해서 마지막 인덱스를 구할 수도 있다.

'''python
print(cities[len(cities) - 1])
'''

`len(cities) - 1`이 마지막 인덱스가 되는 이유는 인덱스가 0부터 시작하기 때문이다.

예를 들어 요소가 3개라면 인덱스는 `0, 1, 2`이고 마지막 인덱스는 `3 - 1 = 2`이다.

---

## 8. 패킹(Packing)

패킹은 여러 개의 값을 하나의 튜플로 묶는 것이다.

'''python
student = ("홍길동", 20, "Python")
'''

여러 개의 값이 `student`라는 하나의 튜플에 들어갔다.

즉,

여러 값 → 하나의 튜플

로 묶는 것이 패킹이다.

튜플에는 서로 다른 데이터 타입도 함께 저장할 수 있다.

'''python
student = ("홍길동", 20, "Python")
'''

문자열, 정수, 문자열처럼 서로 다른 타입의 값을 하나의 튜플에 저장할 수 있다.

---

## 9. 언패킹(Unpacking)

언패킹은 하나의 튜플에 들어 있는 값을 여러 개의 변수로 나누어 저장하는 것이다.

'''python
student = ("홍길동", 20, "Python")

name, age, language = student
'''

결과적으로 다음과 같이 값이 들어간다.

- `name` → 홍길동
- `age` → 20
- `language` → Python

즉,

하나의 튜플 → 여러 변수

로 나누는 것이 언패킹이다.

언패킹에서는 변수의 개수와 튜플의 값 개수가 맞아야 한다.

'''python
student = ("홍길동", 20, "Python")

name, age = student
'''

위 코드는 튜플에는 3개의 값이 있는데 변수는 2개이기 때문에 `ValueError`가 발생한다.

언패킹은 파이썬에서 여러 값을 반환하는 함수와 함께 사용할 때도 자주 사용된다.

---

## 10. 튜플 값을 직접 접근하기

튜플의 값을 사용하기 위해 반드시 언패킹을 해야 하는 것은 아니다.

인덱스를 사용해서 직접 접근할 수도 있다.

'''python
student = ("홍길동", 20, "Python")

print(student[0])
print(student[1])
print(student[2])
'''

결과:

'''text
홍길동
20
Python
'''

따라서 튜플에서는 상황에 따라 인덱싱 또는 언패킹을 사용할 수 있다.

---

## 11. 집합(Set)

Set은 수학에서 배웠던 집합과 비슷한 개념의 자료구조이다.

가장 중요한 특징은 **중복된 값을 허용하지 않는다는 것**이다.

'''python
numbers = {1, 2, 3}
'''

Set은 중괄호 `{}`를 사용한다.

예를 들어 같은 값이 여러 번 들어 있는 리스트가 있다면:

'''python
texts = ["Python", "AI", "Python", "AI", "SQL"]

unique_texts = set(texts)

print(unique_texts)
'''

Set으로 변환하면 중복된 값이 제거된다.

원래 데이터:

'''text
Python
AI
Python
AI
SQL
'''

Set으로 변환하면:

'''text
Python
AI
SQL
'''

처럼 각각 한 번씩만 남는다.

---

## 12. Set을 사용하는 이유

Set은 데이터의 중복을 제거해야 할 때 매우 유용하다.

예를 들어 일주일 동안 강의실에 들어온 사람들의 기록이 있다고 생각할 수 있다.

'''python
visitors = ["민수", "지형", "민수", "영희", "민수", "지형"]
'''

이 리스트에는 같은 사람이 여러 번 기록되어 있다.

우리가 알고 싶은 것이 "지난주에 한 번이라도 온 사람의 목록"이라면 중복된 이름을 제거해야 한다.

'''python
unique_visitors = set(visitors)
'''

Set을 사용하면 같은 사람이 여러 번 등장하더라도 한 번만 남는다.

이처럼 **고유한 값만 필요할 때 Set을 사용하면 편리하다.**

데이터에서는 이런 고유한 값을 `Distinct` 또는 `Unique`라고 표현하기도 한다.

---

## 13. Set의 특징

Set의 특징은 다음과 같다.

- 중괄호 `{}`를 사용한다.
- 순서가 없다.
- 중복을 허용하지 않는다.
- 인덱스로 접근할 수 없다.
- 중복 제거에 유용하다.
- 집합 연산을 사용할 수 있다.

리스트와 튜플은 순서가 있기 때문에 인덱스로 값을 가져올 수 있지만, Set은 순서가 없기 때문에 인덱싱을 사용할 수 없다.

---

## 14. 빈 Set 만들기

빈 Set을 만들 때는 `set()`을 사용한다.

'''python
empty_set = set()
'''

주의해야 할 점은:

'''python
{}
'''

이것은 빈 Set이 아니다.

파이썬에서 `{}`는 딕셔너리를 의미한다.

따라서 빈 Set은 반드시:

'''python
set()
'''

으로 만들어야 한다.

---

## 15. Set에 값 추가하고 삭제하기

Set에 값을 추가할 때는 `add()`를 사용한다.

'''python
numbers = {1, 2, 3}

numbers.add(4)
'''

값을 삭제할 때는 `remove()`를 사용할 수 있다.

'''python
numbers.remove(4)
'''

`remove()`를 사용할 때 삭제하려는 값이 Set에 없다면 오류가 발생할 수 있다.

---

## 16. 집합 연산

Set에서는 수학에서 배웠던 집합 연산을 사용할 수 있다.

대표적으로 합집합, 교집합, 차집합이 있다.

### 합집합(Union)

두 집합을 합치는 것이다.

'''python
team_a = {"Python", "SQL", "AI"}
team_b = {"Python", "AI", "Django"}

print(team_a | team_b)
'''

두 집합의 모든 값을 합치고 중복된 값은 제거한다.

결과적으로 Python, SQL, AI, Django가 남는다.

---

### 교집합(Intersection)

두 집합에 공통으로 존재하는 값을 찾는 것이다.

'''python
team_a = {"Python", "SQL", "AI"}
team_b = {"Python", "AI", "Django"}

print(team_a & team_b)
'''

두 팀 모두 가지고 있는 기술인 Python과 AI가 결과로 나온다.

---

### 차집합(Difference)

한 집합에는 있지만 다른 집합에는 없는 값을 찾는 것이다.

'''python
team_a = {"Python", "SQL", "AI"}
team_b = {"Python", "AI", "Django"}

print(team_a - team_b)
'''

Team A에는 있지만 Team B에는 없는 SQL이 결과로 나온다.

---

## 17. 리스트와 Set의 중복 제거 차이

리스트에서는 같은 값이 여러 번 들어갈 수 있다.

'''python
texts = ["Python", "AI", "Python", "AI", "SQL"]
'''

이 리스트의 길이는 5이다.

Set으로 변환하면:

'''python
unique_texts = set(texts)
'''

중복이 제거되어 3개의 값만 남는다.

따라서 단순히 중복을 제거하고 싶다면 Set을 사용하는 것이 편리하다.

하지만 오늘 실습에서는 Set을 사용하지 않고 **리스트의 기능만 이용해서 직접 중복을 제거하는 방법**도 생각해 보는 것이 중요했다.

이 과정에서 중요한 것은 단순히 코드를 외우는 것이 아니라,

"중복을 발견하면 어떻게 처리할 것인가?"

라는 논리 구조를 먼저 생각하는 것이다.

---

## 18. 의사 코드(Pseudocode)

문제를 바로 파이썬 코드로 작성하기 어렵다면 의사 코드를 먼저 작성할 수 있다.

의사 코드는 실제 프로그래밍 언어가 아니라 우리가 사용하는 자연어로 프로그램의 논리를 적어보는 방법이다.

예를 들어 중복 제거 과정을 생각한다면:

'''text
리스트를 준비한다.
새로운 빈 리스트를 준비한다.
원래 리스트의 값을 하나씩 확인한다.
새로운 리스트에 이미 있는 값인지 확인한다.
없다면 새로운 리스트에 추가한다.
모든 값을 확인하면 결과를 출력한다.
'''

이런 식으로 먼저 논리 구조를 만든 뒤 파이썬 코드로 바꾸면 된다.

중요한 것은 코드를 바로 작성하는 것이 아니라 **문제를 어떤 순서로 해결할지 생각하는 것**이다.

---

## 19. 리스트, 튜플, Set 비교

| 구분 | 리스트 | 튜플 | Set |
|---|---|---|---|
| 문법 | `[]` | `()` | `{}` |
| 순서 | 있음 | 있음 | 없음 |
| 인덱싱 | 가능 | 가능 | 불가능 |
| 중복 | 허용 | 허용 | 허용하지 않음 |
| 수정 | 가능 | 불가능 | 가능 |
| 주요 용도 | 일반적인 데이터 저장 | 변경되지 않는 데이터 | 중복 제거, 집합 연산 |

---

## 가장 중요하게 느낀 것

오늘 배운 리스트, 튜플, Set은 모두 여러 개의 데이터를 묶어서 저장하는 자료구조이지만 사용하는 목적이 다르다는 것을 알게 되었다.

특히 "수정할 수 있는 것이 무조건 좋은 것"이 아니라는 점이 중요했다.

값이 계속 바뀌어야 한다면 리스트가 적합하지만, 좌표나 RGB 값처럼 정해진 값을 변경하지 않고 유지해야 한다면 튜플이 더 적합하다.

또한 같은 값이 반복되는 데이터에서 중복을 제거하고 고유한 값만 사용하고 싶다면 Set을 사용할 수 있다.

결국 자료구조를 선택할 때는 문법을 먼저 외우는 것이 아니라 다음 세 가지를 먼저 생각해야 한다.

'''text
1. 순서가 중요한가?
2. 값을 수정할 것인가?
3. 중복을 허용할 것인가?
'''

이 세 가지를 기준으로 생각하면 리스트, 튜플, Set을 구분하기 쉬워진다.

---

## 실습·복습 체크리스트

- [ ] 리스트의 특징 이해하기
- [ ] 리스트에서 인덱스로 값에 접근하기
- [ ] 리스트의 값을 수정해 보기
- [ ] `append()` 사용해 보기
- [ ] `insert()` 사용해 보기
- [ ] `remove()` 사용해 보기
- [ ] 튜플의 특징 이해하기
- [ ] 리스트와 튜플의 차이 이해하기
- [ ] 요소가 하나인 튜플에서 쉼표가 필요한 이유 이해하기
- [ ] `type()`으로 데이터 타입 확인하기
- [ ] 튜플에서 인덱싱 사용하기
- [ ] 튜플에서 슬라이싱 사용하기
- [ ] `len()`으로 요소 개수 확인하기
- [ ] `len() - 1`로 마지막 인덱스 구하기
- [ ] 패킹과 언패킹의 차이 이해하기
- [ ] 튜플을 여러 변수로 언패킹해 보기
- [ ] 언패킹할 때 변수 개수와 값 개수 맞추기
- [ ] Set의 특징 이해하기
- [ ] 리스트를 Set으로 변환해서 중복 제거하기
- [ ] 빈 Set을 `set()`으로 만들기
- [ ] `add()`로 Set에 값 추가하기
- [ ] `remove()`로 Set의 값 삭제하기
- [ ] 합집합, 교집합, 차집합 이해하기
- [ ] Set을 사용하지 않고 리스트만으로 중복 제거하는 논리 생각해 보기
- [ ] 문제를 의사 코드로 먼저 작성해 보기
- [ ] 상황에 맞는 자료구조 선택하기

---

## 오늘의 핵심 한 줄

리스트는 수정하면서 사용하는 일반적인 데이터 저장에, 튜플은 변경하지 않을 고정된 데이터에, Set은 중복 제거와 집합 연산에 사용한다.
