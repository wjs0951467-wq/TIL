# 2026-09-03 TIL — 데이터베이스 기본 개념과 DBMS 이해하기

## 오늘 배운 것

오늘은 데이터베이스의 기본 개념과 DBMS의 역할을 시작으로 데이터베이스의 구조, 테이블과 행·열, Primary Key와 Foreign Key, 테이블 간 관계, 정규화와 제약 조건 등을 학습했다.

또한 SQL을 이용해 데이터를 조회하고 관리하는 기본적인 방법과 DDL, DML, CRUD의 개념을 정리했다. 마지막으로 AI를 활용해 SQL이나 데이터베이스 구조를 작성할 때 결과를 그대로 사용하는 것이 아니라 직접 검증하는 과정이 중요하다는 것도 배웠다.

---

## 핵심 개념

### 데이터베이스 기본 개념

#### 1. 데이터와 데이터베이스

* **데이터(Data)**: 의미 있는 정보를 만들기 위해 저장되는 값
* **데이터베이스(Database, DB)**: 데이터를 체계적으로 저장하고 관리하는 공간
* 데이터를 단순히 파일로 저장하는 것보다 여러 사용자가 효율적으로 조회하고 수정할 수 있도록 구조화해서 관리할 수 있다.

예를 들어 학생 정보를 관리한다면 다음과 같은 데이터를 저장할 수 있다.

```text
학생번호 | 이름 | 학과
1        | 홍길동 | 컴퓨터공학과
2        | 김철수 | 경영학과
```

#### 2. DBMS

**DBMS(Database Management System)**는 데이터베이스를 관리하기 위한 프로그램이다.

대표적으로 다음과 같은 DBMS가 있다.

* PostgreSQL
* MySQL
* MariaDB
* Oracle
* SQLite

DBMS를 사용하면 데이터를 저장하고, 조회하고, 수정하고, 삭제하는 작업을 편리하게 수행할 수 있다.

#### 3. PostgreSQL과 DBeaver

* **PostgreSQL**: 데이터를 실제로 저장하고 SQL을 처리하는 데이터베이스 시스템
* **DBeaver**: 데이터베이스에 접속하여 테이블과 데이터를 확인하고 SQL을 실행할 수 있는 도구

즉, PostgreSQL이 **데이터를 관리하는 시스템**이라면 DBeaver는 PostgreSQL을 **쉽게 사용하기 위한 GUI 도구**라고 이해하면 된다.

#### 4. Database / Schema / Table

데이터베이스는 하나의 공간에 모든 데이터를 무작정 저장하는 것이 아니라 구조를 나누어 관리할 수 있다.

```text
Database
 └── Schema
      └── Table
           ├── Column
           └── Row
```

* **Database**: 전체 데이터가 관리되는 큰 공간
* **Schema**: 데이터베이스 안에서 객체를 논리적으로 구분하는 영역
* **Table**: 실제 데이터를 행과 열의 형태로 저장하는 구조

#### 5. Row와 Column

* **Row(행)**: 하나의 데이터 단위
* **Column(열)**: 데이터의 속성
* **Cell(셀)**: 행과 열이 만나는 하나의 값

예를 들어 학생 테이블에서 한 명의 학생 정보가 하나의 Row가 된다.

```text
id | name | age
1  | 홍길동 | 20
```

여기서 `1, 홍길동, 20`이 하나의 Row이고 `id`, `name`, `age`가 Column이다.

---

### 데이터베이스 설계

#### 1. Primary Key

**Primary Key(PK, 기본 키)**는 테이블에서 각 행을 고유하게 식별하기 위한 값이다.

조건:

* 중복되면 안 된다.
* `NULL`이 될 수 없다.
* 각 행을 구분할 수 있어야 한다.

예:

```text
student_id | name
1          | 홍길동
2          | 김철수
3          | 이영희
```

여기서 `student_id`를 PK로 사용할 수 있다.

#### 2. Foreign Key

**Foreign Key(FK, 외래 키)**는 다른 테이블의 PK 등을 참조하여 테이블과 테이블을 연결하는 값이다.

예:

```text
학생 테이블
student_id | name
1          | 홍길동
2          | 김철수
```

```text
수강 테이블
student_id | course
1          | 데이터베이스
2          | 파이썬
```

수강 테이블의 `student_id`가 학생 테이블의 `student_id`를 참조한다면 두 테이블의 관계를 만들 수 있다.

#### 3. PK와 UNIQUE의 차이

둘 다 중복을 방지할 수 있지만 역할이 다르다.

| 구분      | Primary Key | UNIQUE         |
| ------- | ----------- | -------------- |
| 중복      | 불가능         | 불가능            |
| NULL    | 불가능         | DBMS 설정에 따라 가능 |
| 테이블당 개수 | 기본적으로 1개    | 여러 개 가능        |
| 주요 목적   | 행을 대표하는 식별자 | 특정 값의 중복 방지    |

따라서 이메일 주소처럼 중복되면 안 되는 값을 `UNIQUE`로 지정할 수 있고, 각 행을 식별하는 ID는 일반적으로 PK로 사용한다.

#### 4. 테이블 간 관계

테이블은 서로 관계를 가질 수 있다.

대표적인 관계는 다음과 같다.

* **1:1**: 한 데이터가 다른 하나의 데이터와 연결
* **1:N**: 하나의 데이터가 여러 데이터와 연결
* **N:M**: 여러 데이터가 여러 데이터와 연결

예를 들어 한 명의 학생이 여러 과목을 수강한다면 학생과 수강 내역은 **1:N 관계**가 될 수 있다.

N:M 관계는 중간 테이블을 사용하여 관리하는 경우가 많다.

#### 5. 정규화

**정규화(Normalization)**는 데이터 중복을 줄이고 데이터의 일관성을 유지하기 위해 테이블을 적절하게 분리하는 과정이다.

예를 들어 학생 정보와 과목 정보를 하나의 테이블에 계속 반복해서 저장하면 데이터가 중복될 수 있다.

```text
학생 테이블
학생ID | 학생명 | 과목명
1      | 홍길동 | DB
1      | 홍길동 | Python
1      | 홍길동 | SQL
```

이를 적절한 테이블로 분리하면 데이터 중복을 줄이고 관리하기 쉬워진다.

정규화의 핵심 목적은 **중복 최소화와 데이터 무결성 유지**이다.

#### 6. 제약 조건

데이터베이스에서는 잘못된 데이터가 저장되는 것을 막기 위해 **제약 조건(Constraint)**을 사용할 수 있다.

대표적인 제약 조건:

* `PRIMARY KEY`
* `FOREIGN KEY`
* `NOT NULL`
* `UNIQUE`
* `CHECK`
* `DEFAULT`

예:

```sql
CREATE TABLE students (
    student_id INTEGER PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    age INTEGER CHECK (age >= 0)
);
```

#### 7. 데이터 타입

Column마다 저장할 데이터의 종류를 지정해야 한다.

대표적인 데이터 타입:

* `INTEGER`: 정수
* `NUMERIC`: 정확한 숫자
* `VARCHAR`: 문자열
* `TEXT`: 긴 문자열
* `DATE`: 날짜
* `TIMESTAMP`: 날짜와 시간
* `BOOLEAN`: 참/거짓

데이터의 성격에 맞는 타입을 선택하는 것이 중요하다.

---

### SQL과 데이터 관리

#### 1. CRUD

데이터를 관리할 때 가장 기본이 되는 작업이다.

| 작업     | 의미 | SQL      |
| ------ | -- | -------- |
| Create | 생성 | `INSERT` |
| Read   | 조회 | `SELECT` |
| Update | 수정 | `UPDATE` |
| Delete | 삭제 | `DELETE` |

CRUD는 데이터베이스를 사용하는 프로그램에서 매우 기본적인 개념이다.

#### 2. DDL과 DML

SQL은 목적에 따라 여러 종류로 구분할 수 있다.

**DDL(Data Definition Language)**

데이터베이스 구조를 정의하거나 변경한다.

```sql
CREATE
ALTER
DROP
TRUNCATE
```

**DML(Data Manipulation Language)**

테이블에 저장된 데이터를 조작한다.

```sql
SELECT
INSERT
UPDATE
DELETE
```

즉,

```text
DDL → 데이터베이스 구조를 다룸
DML → 테이블의 데이터를 다룸
```

#### 3. SELECT와 FROM

`SELECT`는 조회할 Column을 지정하고 `FROM`은 어느 테이블에서 가져올지 지정한다.

```sql
SELECT name, age
FROM students;
```

의미:

> students 테이블에서 name과 age Column을 조회한다.

모든 Column을 조회하려면 `*`를 사용할 수 있다.

```sql
SELECT *
FROM students;
```

#### 4. WHERE

`WHERE`는 조건에 맞는 데이터만 조회할 때 사용한다.

```sql
SELECT *
FROM students
WHERE age >= 20;
```

20세 이상인 학생만 조회한다.

`WHERE`는 데이터 분석이나 실무에서 원하는 데이터만 골라낼 때 매우 자주 사용한다.

#### 5. ORDER BY

조회 결과를 특정 기준으로 정렬할 때 사용한다.

```sql
SELECT *
FROM students
ORDER BY age;
```

기본적으로 오름차순으로 정렬된다.

내림차순은 `DESC`를 사용한다.

```sql
SELECT *
FROM students
ORDER BY age DESC;
```

#### 6. 테이블과 조회 결과의 차이

SQL을 공부하면서 중요한 부분 중 하나는 **원본 테이블과 조회 결과를 구분하는 것**이다.

테이블에는 실제 데이터가 저장되어 있지만,

```sql
SELECT name, age
FROM students;
```

와 같이 일부 Column만 조회하면 결과에는 선택한 Column만 표시된다.

즉,

```text
원본 테이블
↓
SQL 실행
↓
조건이나 Column을 적용한 조회 결과
```

가 된다.

조회 결과가 원본 테이블 자체를 변경하는 것은 아니다.

---

### AI와 데이터베이스

#### 1. AI로 SQL 작성하기

AI를 이용하면 SQL 작성이나 테이블 설계에 도움을 받을 수 있다.

예를 들어,

```text
학생 정보를 저장할 수 있는 PostgreSQL 테이블을 만들어줘.
학생 ID, 이름, 이메일, 학과가 필요해.
```

와 같이 요구사항을 전달하면 AI가 SQL을 작성해 줄 수 있다.

하지만 AI가 작성한 SQL을 그대로 실행하는 것은 주의해야 한다.

#### 2. AI가 만든 결과 검증하기

AI가 만든 SQL이나 데이터베이스 구조를 사용할 때는 다음 내용을 직접 확인해야 한다.

* Column이 필요한 만큼 존재하는가?
* 데이터 타입이 적절한가?
* PK가 제대로 설정되어 있는가?
* FK 관계가 올바른가?
* `NOT NULL`, `UNIQUE` 등의 제약 조건이 필요한가?
* 테이블 간 관계가 요구사항과 맞는가?
* 데이터가 중복 저장되고 있지는 않은가?
* 실제 SQL을 실행했을 때 오류가 없는가?

AI는 코드를 작성하는 데 도움을 줄 수 있지만 **최종적으로 맞는지 판단하는 것은 개발자의 역할**이다.

#### 3. 데이터베이스 설계 검증 순서

AI에게 DB 설계를 요청했다면 다음과 같은 순서로 확인할 수 있다.

```text
요구사항 확인
↓
테이블 구조 확인
↓
Column과 데이터 타입 확인
↓
PK / FK 확인
↓
테이블 관계 확인
↓
제약 조건 확인
↓
SQL 실행
↓
실제 데이터 조회 및 검증
```

AI를 활용하더라도 데이터베이스의 기본 개념을 알고 있어야 잘못된 결과를 발견할 수 있다.

---

## 실습·복습 체크리스트

* [ ] Database와 DBMS의 차이를 설명할 수 있는가?
* [ ] PostgreSQL과 DBeaver의 역할을 구분할 수 있는가?
* [ ] Database / Schema / Table의 관계를 이해했는가?
* [ ] Row와 Column의 차이를 설명할 수 있는가?
* [ ] Primary Key의 역할을 설명할 수 있는가?
* [ ] Foreign Key의 역할을 설명할 수 있는가?
* [ ] PK와 UNIQUE의 차이를 이해했는가?
* [ ] 1:1, 1:N, N:M 관계를 구분할 수 있는가?
* [ ] 정규화가 필요한 이유를 설명할 수 있는가?
* [ ] 주요 제약 조건을 알고 있는가?
* [ ] 데이터 타입을 적절하게 선택할 수 있는가?
* [ ] CRUD의 의미를 알고 있는가?
* [ ] DDL과 DML의 차이를 설명할 수 있는가?
* [ ] `SELECT`, `WHERE`, `ORDER BY`를 사용할 수 있는가?
* [ ] 원본 테이블과 조회 결과의 차이를 이해했는가?
* [ ] AI가 작성한 SQL과 DB 구조를 직접 검증할 수 있는가?

---

## 오늘의 핵심 한 줄

> 데이터베이스는 단순히 데이터를 저장하는 공간이 아니라, 데이터를 어떤 구조로 저장하고 서로 어떤 관계를 맺을지 설계하고 SQL을 통해 안전하게 관리하는 것이 중요하다.
