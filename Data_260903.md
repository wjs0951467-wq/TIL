# 2026-09-03 TIL — 데이터베이스 기본 개념과 DBMS 이해하기

## 오늘 배운 것

오늘은 **데이터베이스**와 **데이터 분석**에 대해 학습했다.

데이터베이스에서는 데이터를 체계적으로 저장하고 관리하기 위한 DB와 DBMS의 개념, 데이터베이스의 구조, 테이블과 행·열, PK와 FK, 테이블 간 관계, 정규화, 제약 조건과 데이터 타입 등을 배웠다. 또한 SQL을 이용해 데이터를 조회하고 관리하는 기본적인 방법도 학습했다.

데이터 분석에서는 Python을 활용해 데이터를 다루는 이유와 Pandas의 역할, DataFrame의 기본 개념을 이해하고 실제 데이터를 불러와 확인하고 가공하는 과정을 학습했다.

두 수업은 서로 연결되는 부분이 있지만, **데이터베이스는 데이터를 저장하고 관리하는 것**, **데이터 분석은 저장된 데이터를 활용해 필요한 정보를 찾아내는 것**이라는 차이를 구분해서 이해했다.

---

## 핵심 개념

### 데이터베이스

#### 1. 데이터베이스란?

**데이터베이스(Database, DB)**는 데이터를 체계적으로 저장하고 관리하기 위한 공간이다.

데이터를 단순한 파일로 저장하는 것과 달리 데이터베이스를 사용하면 많은 양의 데이터를 효율적으로 관리하고 필요한 데이터를 쉽게 조회하거나 수정할 수 있다.

#### 2. DBMS란?

**DBMS(Database Management System)**는 데이터베이스를 관리하기 위한 프로그램이다.

대표적인 DBMS에는 다음과 같은 것들이 있다.

* PostgreSQL
* MySQL
* MariaDB
* Oracle
* SQLite

DBMS를 이용하면 데이터를 저장, 조회, 수정, 삭제할 수 있다.

#### 3. PostgreSQL과 DBeaver

* **PostgreSQL**: 데이터를 저장하고 SQL을 처리하는 데이터베이스 관리 시스템
* **DBeaver**: 데이터베이스에 접속해 테이블과 데이터를 확인하고 SQL을 실행할 수 있는 GUI 도구

즉, PostgreSQL이 실제 데이터베이스를 관리하는 시스템이라면 DBeaver는 이를 편리하게 사용하기 위한 도구이다.

---

### 데이터베이스 구조

#### 1. Database / Schema / Table

데이터베이스는 하나의 공간에 데이터를 무작정 저장하는 것이 아니라 논리적인 구조를 가지고 관리한다.

```text
Database
 └── Schema
      └── Table
           ├── Column
           └── Row
```

* **Database**: 데이터를 관리하는 전체 공간
* **Schema**: 데이터베이스 안에서 객체를 논리적으로 구분하는 영역
* **Table**: 실제 데이터를 행과 열의 형태로 저장하는 구조

#### 2. Row와 Column

* **Row(행)**: 하나의 데이터 단위
* **Column(열)**: 데이터가 가진 속성
* **Cell(셀)**: 행과 열이 만나는 하나의 값

예를 들어 학생 테이블이 있다면 학생 한 명의 정보가 하나의 Row가 되고, 이름이나 나이와 같은 항목이 Column이 된다.

```text
student_id | name   | age
1          | 홍길동 | 20
2          | 김철수 | 21
```

---

### 데이터베이스 설계

#### 1. Primary Key

**Primary Key(PK, 기본 키)**는 테이블의 각 행을 고유하게 식별하기 위한 값이다.

PK는 중복될 수 없고 `NULL`이 될 수 없다.

```text
student_id | name
1          | 홍길동
2          | 김철수
3          | 이영희
```

여기서 `student_id`를 PK로 사용할 수 있다.

#### 2. Foreign Key

**Foreign Key(FK, 외래 키)**는 다른 테이블의 값을 참조하여 테이블과 테이블을 연결하는 값이다.

예를 들어 학생 테이블의 `student_id`를 수강 테이블에서 참조하면 학생 정보와 수강 정보를 연결할 수 있다.

#### 3. PK와 UNIQUE

PK와 `UNIQUE`는 모두 중복을 방지할 수 있지만 역할이 다르다.

| 구분   | Primary Key   | UNIQUE         |
| ---- | ------------- | -------------- |
| 중복   | 불가능           | 불가능            |
| NULL | 불가능           | DBMS 설정에 따라 가능 |
| 개수   | 테이블당 기본적으로 1개 | 여러 개 가능        |
| 목적   | 행을 고유하게 식별    | 특정 값의 중복 방지    |

예를 들어 학생의 ID는 PK로, 이메일 주소는 `UNIQUE`로 지정할 수 있다.

#### 4. 테이블 간 관계

테이블은 서로 관계를 가질 수 있다.

* **1:1**: 하나의 데이터가 하나의 데이터와 연결
* **1:N**: 하나의 데이터가 여러 데이터와 연결
* **N:M**: 여러 데이터가 여러 데이터와 연결

특히 N:M 관계는 일반적으로 중간 테이블을 사용하여 관리한다.

#### 5. 정규화

**정규화(Normalization)**는 데이터 중복을 줄이고 데이터의 일관성을 유지하기 위해 테이블을 적절하게 분리하는 과정이다.

하나의 테이블에 같은 정보가 반복해서 저장되면 데이터가 중복되고 수정할 때 문제가 발생할 수 있다.

정규화를 통해 데이터를 적절하게 분리하면 다음과 같은 장점이 있다.

* 데이터 중복 감소
* 데이터 무결성 유지
* 데이터 수정 및 관리 용이
* 데이터베이스 구조 개선

#### 6. 제약 조건

**제약 조건(Constraint)**은 데이터베이스에 잘못된 데이터가 저장되는 것을 방지하기 위한 규칙이다.

대표적인 제약 조건은 다음과 같다.

* `PRIMARY KEY`
* `FOREIGN KEY`
* `NOT NULL`
* `UNIQUE`
* `CHECK`
* `DEFAULT`

#### 7. 데이터 타입

Column에는 저장할 데이터의 종류에 맞는 데이터 타입을 지정해야 한다.

대표적인 데이터 타입:

* `INTEGER`: 정수
* `NUMERIC`: 숫자
* `VARCHAR`: 문자열
* `TEXT`: 긴 문자열
* `DATE`: 날짜
* `TIMESTAMP`: 날짜와 시간
* `BOOLEAN`: 참/거짓

데이터의 성격에 맞는 적절한 데이터 타입을 선택하는 것이 중요하다.

---

### SQL

#### 1. SQL이란?

**SQL(Structured Query Language)**은 데이터베이스에 저장된 데이터를 관리하기 위해 사용하는 언어이다.

SQL을 이용하면 데이터베이스의 구조를 만들거나 데이터를 추가하고, 조회·수정·삭제할 수 있다.

#### 2. CRUD

CRUD는 데이터를 관리하는 기본적인 작업을 의미한다.

| 작업     | 의미 | SQL      |
| ------ | -- | -------- |
| Create | 생성 | `INSERT` |
| Read   | 조회 | `SELECT` |
| Update | 수정 | `UPDATE` |
| Delete | 삭제 | `DELETE` |

#### 3. DDL과 DML

**DDL(Data Definition Language)**은 데이터베이스의 구조를 정의하거나 변경한다.

```sql
CREATE
ALTER
DROP
TRUNCATE
```

**DML(Data Manipulation Language)**은 테이블의 데이터를 조작한다.

```sql
SELECT
INSERT
UPDATE
DELETE
```

쉽게 구분하면,

```text
DDL → 데이터베이스 구조를 다룸
DML → 테이블에 저장된 데이터를 다룸
```

#### 4. SELECT

`SELECT`는 데이터를 조회할 때 사용한다.

```sql
SELECT name, age
FROM students;
```

특정 Column만 선택해서 조회할 수도 있고, `*`를 사용하면 모든 Column을 조회할 수 있다.

```sql
SELECT *
FROM students;
```

#### 5. WHERE

`WHERE`는 조건에 맞는 데이터만 조회할 때 사용한다.

```sql
SELECT *
FROM students
WHERE age >= 20;
```

#### 6. ORDER BY

`ORDER BY`는 조회 결과를 특정 기준으로 정렬할 때 사용한다.

```sql
SELECT *
FROM students
ORDER BY age;
```

내림차순으로 정렬하려면 `DESC`를 사용한다.

```sql
SELECT *
FROM students
ORDER BY age DESC;
```

#### 7. 테이블과 조회 결과의 차이

SQL을 실행했을 때 화면에 나타나는 조회 결과와 실제 테이블은 다르다.

```text
원본 테이블
    ↓
SQL 실행
    ↓
조건이나 Column을 적용한 조회 결과
```

예를 들어 `SELECT name, age FROM students`를 실행하면 `name`과 `age`만 조회되지만 원본 테이블의 구조가 변경되는 것은 아니다.

---

### 데이터 분석

#### 1. 데이터 분석이란?

**데이터 분석(Data Analysis)**은 데이터를 수집하고 정리·가공한 뒤, 그 안에서 의미 있는 정보를 찾아내는 과정이다.

단순히 데이터를 보는 것이 아니라 데이터를 분석해서 의사결정이나 문제 해결에 활용하는 것이 목적이다.

#### 2. Python을 데이터 분석에 사용하는 이유

Python은 데이터 분석에 사용할 수 있는 다양한 라이브러리를 제공한다.

특히 데이터를 다루는 과정에서 **Pandas**를 많이 사용한다.

Python을 이용하면 데이터를 코드로 반복해서 처리할 수 있고, 많은 양의 데이터를 효율적으로 다룰 수 있다.

---

### Pandas

#### 1. Pandas란?

**Pandas(판다스)**는 Python에서 데이터를 분석하고 처리하기 위한 라이브러리이다.

Pandas를 이용하면 CSV와 같은 데이터를 불러오고, 데이터를 확인하고, 원하는 조건으로 데이터를 선택하거나 가공할 수 있다.

#### 2. DataFrame

**DataFrame(데이터프레임)**은 Pandas에서 표 형태의 데이터를 다루는 핵심 자료구조이다.

데이터베이스의 테이블처럼 행과 열로 구성되어 있어 데이터를 쉽게 확인하고 처리할 수 있다.

```text
        이름    나이    지역
0      홍길동   20     서울
1      김철수   25     부산
2      이영희   22     대전
```

DataFrame에서는 행과 열을 기준으로 데이터를 선택하거나 수정할 수 있다.

#### 3. Series

**Series(시리즈)**는 Pandas에서 하나의 열과 같은 1차원 데이터를 표현하는 자료구조이다.

DataFrame이 여러 개의 Column으로 구성된 표라면, Series는 그중 하나의 Column과 비슷하게 생각할 수 있다.

```text
DataFrame
 ├── Series
 ├── Series
 └── Series
```

#### 4. 데이터 불러오기

Pandas에서는 `read_csv()` 등을 이용해 CSV 파일을 DataFrame으로 불러올 수 있다.

```python
import pandas as pd

df = pd.read_csv("data.csv")
```

이렇게 불러온 데이터를 Python에서 분석할 수 있다.

#### 5. 데이터 확인하기

데이터를 분석하기 전에 먼저 데이터의 구조와 내용을 확인해야 한다.

대표적으로 다음과 같은 방법을 사용할 수 있다.

```python
df.head()
df.tail()
df.info()
df.describe()
```

* `head()`: 앞부분 데이터 확인
* `tail()`: 뒷부분 데이터 확인
* `info()`: Column과 데이터 타입 등의 정보 확인
* `describe()`: 숫자 데이터의 기본적인 통계 정보 확인

#### 6. 데이터 분석의 기본 흐름

데이터 분석은 일반적으로 다음과 같은 과정으로 진행된다.

```text
데이터 수집
↓
데이터 불러오기
↓
데이터 확인
↓
데이터 정리 및 전처리
↓
데이터 분석
↓
결과 해석
↓
시각화 및 활용
```

분석을 시작하기 전에 데이터의 상태를 먼저 확인하는 것이 중요하다.

---

### AI 활용

#### 1. AI를 활용한 코드 작성

AI를 활용하면 SQL이나 Python 코드를 작성할 때 도움을 받을 수 있다.

예를 들어 원하는 작업을 자연어로 설명하고 코드를 생성하도록 요청할 수 있다.

하지만 AI가 작성한 코드가 항상 정확한 것은 아니기 때문에 코드의 내용을 이해하고 검증하는 과정이 필요하다.

#### 2. AI 결과 검증

AI가 작성한 SQL이나 Python 코드를 사용할 때는 다음을 확인해야 한다.

* 내가 원하는 작업을 수행하는 코드인가?
* 문법 오류는 없는가?
* 데이터의 구조와 코드가 맞는가?
* 실행 결과가 예상한 결과와 같은가?
* 불필요하거나 잘못된 코드가 포함되어 있지는 않은가?

AI를 활용할수록 **코드를 직접 읽고 결과를 검증하는 능력**이 중요하다.

---

## 실습·복습 체크리스트

### 데이터베이스

* [ ] Database와 DBMS의 차이를 설명할 수 있는가?
* [ ] PostgreSQL과 DBeaver의 역할을 구분할 수 있는가?
* [ ] Database / Schema / Table의 관계를 설명할 수 있는가?
* [ ] Row와 Column의 차이를 설명할 수 있는가?
* [ ] Primary Key와 Foreign Key의 역할을 설명할 수 있는가?
* [ ] PK와 UNIQUE의 차이를 설명할 수 있는가?
* [ ] 1:1, 1:N, N:M 관계를 구분할 수 있는가?
* [ ] 정규화가 필요한 이유를 설명할 수 있는가?
* [ ] 주요 제약 조건을 알고 있는가?
* [ ] 데이터 타입을 적절하게 선택할 수 있는가?
* [ ] CRUD의 의미를 알고 있는가?
* [ ] DDL과 DML을 구분할 수 있는가?
* [ ] `SELECT`, `WHERE`, `ORDER BY`를 사용할 수 있는가?

### 데이터 분석

* [ ] 데이터 분석의 목적을 설명할 수 있는가?
* [ ] Python을 데이터 분석에 사용하는 이유를 알고 있는가?
* [ ] Pandas의 역할을 설명할 수 있는가?
* [ ] DataFrame과 Series의 차이를 설명할 수 있는가?
* [ ] CSV 파일을 DataFrame으로 불러올 수 있는가?
* [ ] `head()`, `tail()`, `info()`, `describe()`의 역할을 알고 있는가?
* [ ] 데이터 분석의 기본 흐름을 설명할 수 있는가?

### AI 활용

* [ ] AI에게 원하는 코드를 구체적으로 요청할 수 있는가?
* [ ] AI가 작성한 코드를 직접 확인할 수 있는가?
* [ ] AI 코드의 실행 결과를 검증할 수 있는가?

---

## 오늘의 핵심 한 줄

> 데이터베이스는 데이터를 체계적으로 저장하고 관리하는 기술이고, 데이터 분석은 그렇게 저장된 데이터를 활용해 의미 있는 정보를 찾아내는 과정이다.

