# TIL — 변수와 데이터 타입 / 파이썬 연산자 / 문자열 다루기

**날짜:** 2026-08-20
**주제:** 파이썬 기초 — 변수, 데이터 타입, 연산자, 문자열

---

## 1. 오늘 배운 것

오늘은 파이썬의 가장 기본이 되는 세 가지 내용을 배웠다.

1. **변수와 데이터 타입**
2. **파이썬 연산자**
3. **문자열 다루기**

코드를 작성할 때 결국 중요한 것은
**"어떤 데이터를 저장하고 → 어떻게 계산하고 → 어떻게 가공해서 사용할 것인가"**이다.

---

# 2. 변수와 데이터 타입

## 변수란?

변수는 쉽게 말하면 **데이터를 담아두는 이름표**이다.

```
name = "예진"
age = 29
```

* `name` → 변수
* `"예진"` → 문자열 데이터
* `age` → 변수
* `29` → 숫자 데이터

즉,

```
변수 = 데이터
```

형태로 생각하면 된다.

---

## 데이터 타입

데이터마다 종류가 다르기 때문에 **데이터 타입(Type)**이 존재한다.

| 타입      | 의미   | 예시                 |
| ------- | ---- | ------------------ |
| `int`   | 정수   | `10`, `20`, `-5`   |
| `float` | 실수   | `3.14`, `20.5`     |
| `str`   | 문자열  | `"hello"`, `"파이썬"` |
| `bool`  | 참/거짓 | `True`, `False`    |

예를 들어:

```
age = 29
height = 165.5
name = "예진"
student = True
```

---

## 타입 확인하기

`type()`을 사용하면 데이터의 타입을 확인할 수 있다.

```python
print(type(29))
print(type(20.5))
print(type("파이썬"))
```

결과:

```
<class 'int'>
<class 'float'>
<class 'str'>
```

### 기억할 것

**숫자와 문자열은 다르다.**

```python
2026
```

→ 숫자

```
"2026"
```

→ 문자열

겉보기에는 비슷하지만 컴퓨터에서는 완전히 다른 데이터이다.

---

# 3. 파이썬 연산자

연산자는 데이터를 가지고 **계산하거나 비교하거나 처리할 때 사용하는 기호**이다.

## 산술 연산자

| 연산자  | 의미  | 예시        |
| ---- | --- | --------- |
| `+`  | 더하기 | `10 + 3`  |
| `-`  | 빼기  | `10 - 3`  |
| `*`  | 곱하기 | `10 * 3`  |
| `/`  | 나누기 | `10 / 3`  |
| `//` | 몫   | `10 // 3` |
| `%`  | 나머지 | `10 % 3`  |
| `**` | 제곱  | `2 ** 3`  |

예:

```
a = 10
b = 3

print(a + b)
print(a - b)
print(a * b)
print(a / b)
print(a // b)
print(a % b)
print(a ** b)
```

---

## 특히 `%` 나머지 연산

나머지를 구하는 연산자이다.

```
10 % 3
```

결과:

```
1
```

10을 3으로 나누면 몫은 3이고 **나머지가 1**이기 때문이다.

나중에 조건문에서

* 홀수/짝수 확인
* 특정 숫자의 배수 확인

등에 많이 사용된다.

---

# 4. 비교 연산자

두 값을 비교할 때 사용한다.

| 연산자  | 의미     |
| ---- | ------ |
| `==` | 같다     |
| `!=` | 다르다    |
| `>`  | 크다     |
| `<`  | 작다     |
| `>=` | 크거나 같다 |
| `<=` | 작거나 같다 |

예:

```
age = 20

print(age >= 19)
```

결과:

```
True
```

비교 연산의 결과는 **True 또는 False**가 된다.

---

## `=`와 `==` 구분

초보자에게 매우 중요하다.

```
age = 20
```

→ 20을 `age`에 **저장**

```
age == 20
```

→ `age`가 20과 **같은지 비교**

즉,

> `=` → 저장
> `==` → 비교

---

# 5. 논리 연산자

여러 조건을 연결할 때 사용한다.

### `and`

둘 다 참이어야 `True`

```python
age >= 20 and age <= 30
```

### `or`

하나라도 참이면 `True`

```
age < 20 or age > 30
```

### `not`

결과를 반대로 만든다.

```
not True
```

→ `False`

---

# 6. 문자열(String)

문자열은 **문자나 문장을 저장하는 데이터 타입**이다.

문자열은 작은따옴표 또는 큰따옴표로 감싼다.

```
"Hello"
'Hello'

"파이썬"
'파이썬'
```

둘 다 `str` 타입이다.

---

## 문자열과 숫자 구분하기

```
2026
```

→ `int`

```
"2026"
```

→ `str`

따옴표가 있으면 문자열이라고 생각하면 된다.

---

# 7. 문자열 연결하기

문자열에서 `+`는 **문자열을 이어 붙이는 역할**을 한다.

```
first = "Hello"
second = "Python"

print(first + second)
```

결과:

```
HelloPython
```

띄어쓰기를 하고 싶다면 공백도 직접 넣어야 한다.

```
print(first + " " + second)
```

결과:

```
Hello Python
```

### 공백도 문자다.

```
"Hello Python"
```

여기에서 띄어쓰기 한 칸도 문자열의 한 문자로 계산된다.

---

# 8. 문자열 반복하기

문자열에 `*`를 사용하면 반복할 수 있다.

```
word = "AI"

print(word * 5)
```

결과:

```
AIAIAIAIAI
```

구분선을 만들 때도 활용할 수 있다.

```
print("-" * 20)
```

---

# 9. 문자열 길이 — `len()`

문자열의 길이를 구할 때 사용하는 매우 중요한 함수이다.

```
word = "Python"

print(len(word))
```

결과:


```
6
```

`Python`은 6개의 문자로 이루어져 있기 때문이다.

### 중요한 점

**공백도 문자로 센다.**

```python
text = "AI Python"

print(len(text))
```

`AI` 2개 + 공백 1개 + `Python` 6개

→ 총 **9**

---

# 10. 문자열 인덱싱(Indexing)

문자열은 문자 하나하나에 **번호(인덱스)**가 있다.

파이썬에서는 **0부터 시작**한다.

```
P  y  t  h  o  n
0  1  2  3  4  5
```

따라서:

```
word = "Python"

print(word[0])
```

→ `P`

```
print(word[3])
```

→ `h`

```
print(word[5])
```

→ `n`

---

## 마지막 문자 가져오기

뒤에서부터도 번호를 사용할 수 있다.

```text
P   y   t   h   o   n
-6 -5  -4  -3  -2  -1
```

따라서:

```
print(word[-1])
```

→ `n`

**`-1`은 항상 마지막 문자**를 의미한다.

---

# 11. 마지막 인덱스 구하기

문자열의 길이가 6이라면 인덱스는:

```
0 1 2 3 4 5
```

따라서 마지막 인덱스는:

```
len(word) - 1
```

이다.

```
word = "Python"

print(len(word) - 1)
```

결과:

```
5
```

### 공식

> **마지막 인덱스 = 문자열 길이 - 1**

길이가 100이어도:

```
len(word) - 1
```

로 마지막 인덱스를 구할 수 있다.

---

# 12. 인덱스 에러

존재하지 않는 인덱스를 사용하면 오류가 발생한다.

```
word = "Python"

print(word[6])
```

`Python`의 마지막 인덱스는 5이므로 6은 존재하지 않는다.

→ `IndexError`

### 기억

문자열 길이가 6이라고 해서 인덱스가 1~6인 것이 아니다.

```
길이: 6
인덱스: 0 ~ 5
```

---

# 13. 문자열 슬라이싱(Slicing)

문자열의 일부분을 잘라낼 수 있다.

기본 형태:

```
문자열[시작:끝]
```

**끝 번호는 포함하지 않는다.**

```
word = "Python"

print(word[0:2])
```

결과:

```
Py
```

왜냐하면:

```
0 → P
1 → y
2 → t
```

`2`번은 포함하지 않고 **2번 바로 앞까지** 가져오기 때문이다.

---

## 시작이나 끝을 생략할 수도 있다.

```
word[:3]
```

→ 처음부터 3번 바로 앞까지

```
Pyt
```

```
word[3:]
```

→ 3번부터 끝까지

```
hon
```

### 슬라이싱 핵심

```
[시작 : 끝]
```

→ **시작은 포함 / 끝은 제외**

---

# 14. 문자열 메서드

문자열에는 자주 사용하는 기능들이 있다.

문자열 뒤에 `.`을 붙여 사용할 수 있다.

```
text.upper()
```

---

## `upper()` — 대문자로 변경

```
text = "Python"

print(text.upper())
```

→ `PYTHON`

---

## `lower()` — 소문자로 변경

```
text = "Python"

print(text.lower())
```

→ `python`

---

## `strip()` — 앞뒤 공백 제거

```
text = "  Python  "

print(text.strip())
```

→ `Python`

**중간의 공백은 제거하지 않는다.**

주로 사용자가 입력한 데이터나 CSV 데이터에 불필요한 앞뒤 공백이 있을 때 유용하다.

---

## `replace()` — 문자열 바꾸기

```
text = "I like Java"

print(text.replace("Java", "Python"))
```

결과:

```
I like Python
```

---

## `find()` — 문자열 위치 찾기

```
text = "banana"

print(text.find("na"))
```

찾은 문자열이 처음 등장하는 **인덱스 위치**를 반환한다.

찾지 못하면:

```
-1
```

이 나온다.

---

## `count()` — 몇 번 나오는지 세기

```
text = "banana"

print(text.count("a"))
```

→ `3`

`a`가 몇 번 등장하는지 세어준다.

---

# 15. F-string

문자열 안에 변수를 넣을 때 매우 편리한 방법이다.

기본 형태:

```
f"{변수}"
```

예:

```
name = "홍길동"
age = 20

print(f"{name}님의 나이는 {age}세입니다.")
```

결과:

```
홍길동님의 나이는 20세입니다.
```

기존 방식처럼 문자열을 여러 개 `+`로 연결하고 숫자를 `str()`로 바꿀 필요가 없다.

### 기억할 것

문자열 앞에 `f`를 붙인다.

```
f"문자열 {변수}"
```

변수를 넣고 싶은 위치에:

```
{변수명}
```

을 작성한다.

---

# 16. F-string에서는 계산도 가능하다.

```
price = 10000
count = 3

print(f"총 금액은 {price * count}원입니다.")
```

결과:

```
총 금액은 30000원입니다.
```

---

# 17. 특수문자

문자열에서 `\`(백슬래시)를 이용해 특수한 기능을 표현할 수 있다.

| 표현   | 의미      |
| ---- | ------- |
| `\n` | 줄바꿈     |
| `\t` | 탭       |
| `\"` | 큰따옴표 출력 |
| `\\` | 백슬래시 출력 |

예:

```
print("서울\n부산")
```

결과:

```
서울
부산
```

---

# 18. 오늘 배운 내용에서 가장 중요한 것

오늘 내용에서 단순히 문법을 외우는 것보다 **데이터를 어떻게 바라보는지**가 중요하다.

```
변수
 ↓
데이터 저장
 ↓
데이터 타입 확인
 ↓
연산자로 계산/비교
 ↓
문자열이라면 문자열 기능으로 가공
 ↓
필요한 결과 출력
```

예를 들어:

```
name = "  Python  "

clean_name = name.strip().upper()

print(f"사용 언어: {clean_name}")
```

이처럼 지금까지 배운 내용들이 하나씩 연결된다.

---

# 19. 실습·복습 체크리스트

### 변수 & 데이터 타입

* [ ] 변수의 의미를 설명할 수 있다.
* [ ] `int`, `float`, `str`, `bool`을 구분할 수 있다.
* [ ] `type()`으로 데이터 타입을 확인할 수 있다.
* [ ] 숫자 `2026`과 문자열 `"2026"`의 차이를 설명할 수 있다.

### 연산자

* [ ] `+`, `-`, `*`, `/`를 사용할 수 있다.
* [ ] `//`와 `%`의 차이를 안다.
* [ ] `**`가 제곱이라는 것을 안다.
* [ ] `=`과 `==`의 차이를 설명할 수 있다.
* [ ] 비교 연산의 결과가 `True` / `False`라는 것을 안다.
* [ ] `and`, `or`, `not`의 기본적인 의미를 안다.

### 문자열

* [ ] 작은따옴표와 큰따옴표로 문자열을 만들 수 있다.
* [ ] 문자열끼리 `+`로 연결할 수 있다.
* [ ] 문자열을 `*`로 반복할 수 있다.
* [ ] `len()`으로 문자열 길이를 구할 수 있다.
* [ ] 공백도 하나의 문자라는 것을 안다.
* [ ] 인덱스가 `0`부터 시작한다는 것을 안다.
* [ ] `-1`이 마지막 문자라는 것을 안다.
* [ ] `len(문자열) - 1`로 마지막 인덱스를 구할 수 있다.
* [ ] `[시작:끝]` 형태로 슬라이싱할 수 있다.
* [ ] 슬라이싱에서 **끝 번호는 포함되지 않는다는 것**을 안다.
* [ ] `upper()`, `lower()`, `strip()`, `replace()`의 용도를 안다.
* [ ] `find()`와 `count()`의 차이를 안다.
* [ ] F-string으로 변수와 문자열을 함께 출력할 수 있다.
* [ ] `\n`, `\t`의 의미를 안다.

---

# 20. 오늘의 핵심 한 줄

> **파이썬은 데이터를 변수에 저장하고, 데이터 타입에 맞는 연산과 기능을 사용해서 원하는 결과를 만들어가는 과정이다.**

특히 문자열에서는 이것만 먼저 확실하게 기억하자.

> **문자열의 인덱스는 0부터 시작하고, 슬라이싱은 `[시작:끝]`에서 끝을 포함하지 않는다.**
