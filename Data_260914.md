# 2026-09-14 TIL 타이타닉 데이터로 머신러닝 전체 흐름 이해하기

## 주제

타이타닉 데이터를 활용해 데이터 전처리와 EDA부터 머신러닝 모델 학습, 평가, 예측 및 Streamlit을 이용한 모델 활용까지 전체 데이터 분석 흐름을 실습했다.

---

## 데이터 분석 전체 흐름

이번 실습에서 가장 중요했던 것은 개별 코드를 외우는 것보다 **데이터 분석이 어떤 순서로 진행되는지 이해하는 것**이었다.

```text
데이터 준비
→ 데이터 확인
→ 전처리
→ EDA
→ 통계 검정
→ 피처 설계
→ Train/Test 분리
→ 머신러닝 전처리
→ 모델 학습
→ 모델 평가
→ 예측
→ 모델 저장 및 활용
```

---

## 데이터 확인과 전처리

CSV 데이터를 pandas의 DataFrame으로 불러온 뒤 데이터의 구조와 품질을 먼저 확인했다.

주로 확인한 내용은 다음과 같다.

* `shape` : 행과 열의 개수
* `columns` : 컬럼 목록
* `head()` : 실제 데이터 확인
* `info()` : 데이터 타입과 결측값 확인
* `describe()` : 기술 통계 확인
* `value_counts()` : 값별 빈도 확인

전처리를 시작할 때는 원본 데이터를 보호하기 위해 복사본을 만들어 작업했다.

```python
df_work = df.copy()
```

결측값은 무조건 삭제하는 것이 아니라 데이터의 특성에 따라 처리 방법을 결정했다.

* `Age` → 중앙값
* `Embarked` → 최빈값

또한 `PassengerId`, `Name`, `Ticket`, `Cabin`처럼 분석에 바로 사용할지 판단이 필요한 컬럼은 **사용 / 제외 / 추가 검토** 등의 정책을 정해야 한다는 점도 확인했다.

---

## 타겟과 피처

이번 데이터의 타겟은 `Survived`였다.

```text
0 = 사망
1 = 생존
```

머신러닝에서

```text
X = 피처(문제)
y = 타겟 또는 Label(정답)
```

으로 구분한다.

즉, 객실 등급, 성별, 나이, 운임 등의 정보를 이용해 최종적으로 생존 여부를 예측하는 구조이다.

---

## 범주형 데이터와 인코딩

머신러닝에서는 문자열 데이터를 그대로 사용하기 어렵기 때문에 숫자 형태로 변환해야 한다.

이번 실습에서는 `Sex`, `Embarked`와 같은 범주형 데이터를 One-Hot Encoding 방식으로 변환했다.

```text
문자형 범주 데이터
→ Encoding
→ 머신러닝에서 사용할 수 있는 숫자 데이터
```

---

## EDA와 통계 검정

전처리한 데이터를 이용해 시각화와 그룹 분석을 진행했다.

확인한 대표적인 패턴은 다음과 같다.

* 여성의 생존율이 남성보다 높게 나타남
* 1등급 승객의 생존율이 2·3등급보다 높게 나타남
* 성별과 객실 등급을 함께 비교하면 생존율 차이가 더 명확하게 나타남

이렇게 데이터를 여러 관점에서 탐색하는 과정을 **EDA(탐색적 데이터 분석)**라고 한다.

하지만 차트에서 차이가 보인다고 해서 바로 사실이라고 단정할 수는 없다.

따라서 생존자와 사망자의 평균 운임 차이를 통계 검정으로 확인했다.

```text
H0 : 두 그룹의 평균 운임에는 차이가 없다.
H1 : 두 그룹의 평균 운임에는 차이가 있다.
```

기준은 일반적으로 다음과 같이 판단한다.

```text
p-value < 0.05
→ 귀무가설 기각
→ 통계적으로 유의한 차이가 있다고 판단
```

이번 결과에서는 p-value가 0.05보다 매우 작아 생존자와 비생존자의 평균 운임 차이가 통계적으로 유의하다고 판단했다.

즉,

```text
EDA로 패턴 발견
→ 통계 검정으로 근거 확인
```

의 흐름을 이해했다.

---

## 피처 엔지니어링

기존 컬럼을 이용해 새로운 정보를 만드는 **피처 엔지니어링**도 진행했다.

이번에 만든 대표적인 파생 피처는 다음과 같다.

### FamilySize

```text
SibSp + Parch + 본인
```

을 이용해 전체 가족 규모를 만들었다.

### IsAlone

FamilySize를 기준으로 혼자 탑승했는지를 나타내는 컬럼을 만들었다.

기존 데이터를 그대로 사용하는 것뿐만 아니라 **예측에 도움이 될 새로운 정보를 만드는 것도 머신러닝에서 중요한 과정**이라는 것을 알게 되었다.

---

## Train / Test 데이터 분리

전체 데이터를 학습용과 테스트용으로 분리했다.

```text
Train : 80%
Test  : 20%
```

수업에서는 이를 다음과 같이 이해했다.

```text
Train = 공부
Test = 모의고사
```

또한 `random_state` 값을 고정하면 실행할 때마다 동일한 데이터 분할 결과를 얻을 수 있어 재현성을 확보할 수 있다.

데이터를 나눈 뒤에는 전체 데이터와 Train/Test 데이터의 생존 비율도 확인했다.

정답 비율이 한쪽으로 크게 치우쳐 있으면 정확도만으로 모델의 성능을 판단하기 어렵기 때문이다.

---

## 머신러닝을 위한 추가 전처리

피처를 숫자형과 범주형으로 나누어 각각 필요한 처리를 적용했다.

```text
숫자형
→ 결측값 처리
→ Scaling

범주형
→ 결측값 처리
→ Encoding
```

특히 숫자형 데이터는 컬럼마다 값의 범위가 다르기 때문에 스케일을 맞추는 과정이 필요했다.

대표적인 방법은 다음과 같다.

* `MinMaxScaler` : 값을 주로 0~1 범위로 조정
* `StandardScaler` : 평균 0, 표준편차 1을 기준으로 조정

스케일링을 통해 값 자체의 크기 차이가 모델 학습에 불필요한 영향을 주는 것을 줄일 수 있다.

---

## 머신러닝 모델 학습

첫 번째 모델로 `Logistic Regression`을 사용했다.

이번 문제는

```text
생존 / 사망
```

을 구분하는 **이진 분류(Binary Classification)** 문제이다.

모델 학습은 다음과 같은 구조로 진행된다.

```python
model.fit(X_train, y_train)
```

```text
X_train = 학습할 문제
y_train = 학습할 정답
```

즉, 문제와 정답을 함께 제공해 모델이 패턴을 학습하도록 한다.

---

## 모델 성능 평가

학습한 모델은 Test 데이터를 이용해 성능을 평가했다.

대표적인 평가 지표는 다음과 같다.

* Accuracy
* Precision
* Recall
* F1 Score

여기서 중요한 점은 **Accuracy 하나만 보고 모델이 좋다고 판단하면 안 된다는 것**이다.

예를 들어 한 정답이 전체 데이터의 90%를 차지한다면 모델이 모든 답을 그 값으로만 예측해도 정확도가 90%가 될 수 있다.

따라서 Confusion Matrix를 이용해 모델이 어떤 경우를 맞히고 틀리는지도 확인해야 한다.

```text
TP : Positive를 맞춤
TN : Negative를 맞춤
FP : Negative를 Positive로 잘못 예측
FN : Positive를 Negative로 잘못 예측
```

어떤 오류가 더 중요한지는 모델이 사용되는 목적에 따라 달라진다.

예를 들어 의료 진단에서는 실제 질병이 있는데 정상이라고 판단하는 오류가 더 위험할 수 있다.

---

## 모델 비교와 최종 선택

하나의 모델만 사용하는 것이 아니라 다른 알고리즘과 성능을 비교할 수 있다.

이번 실습에서는

```text
Logistic Regression
Random Forest
```

를 비교했다.

모델 성능이 좋지 않다면 단순히 다른 모델로 바꾸는 것뿐만 아니라

* 피처 추가 또는 제거
* 전처리 방법 변경
* 파생 피처 수정
* 모델 변경

등을 다시 검토할 수 있다.

즉, 머신러닝은 한 번 학습하고 끝나는 것이 아니라 **평가 결과를 보고 다시 개선하는 반복 과정**이다.

---

## 새로운 데이터 예측과 모델 활용

학습한 모델에 새로운 승객 정보를 입력해 생존 여부를 예측했다.

결과는 단순히

```text
0 = 사망
1 = 생존
```

뿐만 아니라 생존 확률까지 확인할 수 있었다.

이후 모델과 모델 사용 정보를 파일로 저장하고 Streamlit 앱에서 불러와 실제 사용자 입력을 기반으로 예측할 수 있도록 연결했다.

```text
사용자 입력
→ 동일한 전처리
→ 저장된 모델
→ 생존 여부 및 확률 예측
```

여기에서 중요한 점은 **학습할 때 사용한 전처리와 실제 예측할 때 사용하는 전처리가 반드시 같아야 한다는 것**이다.

---

## 오늘의 핵심 정리

이번 실습을 통해 머신러닝은 단순히 모델을 선택해서 학습시키는 과정이 아니라는 것을 알게 되었다.

```text
데이터를 이해하고
→ 필요한 형태로 정리하고
→ EDA로 패턴을 찾고
→ 통계적으로 확인하고
→ 의미 있는 피처를 만들고
→ 모델을 학습하고
→ 여러 지표로 평가하고
→ 실제 서비스에서 사용할 수 있도록 연결하는 것
```

까지가 하나의 전체 과정이다.

특히 이번 수업에서는 **현재 내가 전체 데이터 분석 과정 중 어느 단계에서 무엇을 하고 있는지를 이해하는 것이 중요하다**는 점을 다시 확인했다.
