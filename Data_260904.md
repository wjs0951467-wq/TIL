# [TIL] 2026-09-04 — PostgreSQL 데이터베이스 기초와 Pandas 데이터 분석

## 오늘 배운 내용

오늘은 **데이터베이스 강의**와 **데이터 분석 강의**를 각각 학습했다.

두 분야는 사용하는 목적과 도구가 다르기 때문에 하나의 흐름으로 섞지 않고 각각 정리했다.

---

# 데이터베이스

## 1. PostgreSQL과 DBMS

### DBMS란?

**DBMS(Database Management System)**는 데이터를 저장하고 관리할 수 있도록 해주는 데이터베이스 관리 시스템이다.

오늘 실습에서는 **PostgreSQL**을 DBMS로 사용했다.

* PostgreSQL → DBMS
* DBeaver → 데이터베이스에 접속하여 SQL을 실행하고 관리하는 도구
* Database → 데이터를 논리적으로 저장하는 공간
* Schema → Database 안에서 테이블 등의 객체를 구분하는 공간
* Table → 실제 데이터를 행과 열로 저장하는 구조

따라서 다음은 서로 같은 개념이 아니다.

```text
PostgreSQL ≠ Database ≠ DBeaver
```

PostgreSQL은 데이터를 관리하는 DBMS이고, DBeaver는 PostgreSQL에 접속하여 데이터베이스를 다루기 위한 클라이언트 도구이다.

---

## 2. Database → Schema → Table 구조

데이터베이스의 구조를 다음과 같이 이해할 수 있다.

```text
PostgreSQL
└── Database
    └── Schema
        └── Table
            ├── Column
            └── Row
```

### Row와 Column

* **Row(행)** → 하나의 데이터
* **Column(열)** → 데이터가 가지고 있는 속성

테이블을 볼 때는 단순히 컬럼 이름만 보는 것이 아니라 **"이 테이블의 한 행은 무엇을 의미하는가?"**를 먼저 생각해야 한다.

---

## 3. PostgreSQL 연결 및 환경 확인

DBeaver에서 PostgreSQL에 정상적으로 연결되었는지 확인하기 위해 다음 SQL을 실행했다.

```sql
SELECT version();

SELECT current_database();

SELECT current_user;

SELECT current_schema();

SHOW search_path;
```

각 명령을 통해 현재 사용 중인 PostgreSQL의 버전, 데이터베이스, 사용자, 스키마와 `search_path`를 확인할 수 있다.

특히 `search_path`는 SQL에서 스키마 이름을 생략했을 때 PostgreSQL이 어떤 스키마부터 찾을지를 결정한다.

---

## 4. Schema와 Table 확인

현재 데이터베이스에 어떤 스키마가 존재하는지 확인할 수 있다.

```sql
SELECT schema_name
FROM information_schema.schemata
ORDER BY schema_name;
```

사용자 테이블은 다음과 같이 확인할 수 있다.

```sql
SELECT table_schema, table_name
FROM information_schema.tables
WHERE table_type = 'BASE TABLE'
  AND table_schema NOT IN ('pg_catalog', 'information_schema')
ORDER BY table_schema, table_name;
```

이를 통해 현재 데이터베이스 안에 실제로 어떤 테이블이 존재하는지 확인할 수 있다.

중요한 점은 **PostgreSQL을 설치했다고 해서 수업에서 사용하는 테이블까지 자동으로 만들어지는 것은 아니라는 것**이다.

---

## 5. TEMP TABLE 실습

실습에서는 임시 테이블을 직접 생성하여 데이터베이스의 구조를 확인했다.

```text
ch02_students
ch02_courses
ch02_enrollments
```

각 테이블은 서로 다른 데이터를 의미한다.

```text
ch02_students
→ 학생 정보

ch02_courses
→ 강의 정보

ch02_enrollments
→ 학생의 강의 수강신청 정보
```

특히 `ch02_enrollments`에서는 한 행이 **학생 한 명의 정보가 아니라 특정 학생의 특정 강의 수강신청 한 건**을 의미한다.

테이블 설계에서는 이러한 **한 행의 의미**를 명확하게 정의하는 것이 중요하다.

---

## 6. Primary Key와 Foreign Key

### Primary Key(PK)

**Primary Key(기본 키)**는 테이블에서 각각의 행을 고유하게 식별하기 위한 값이다.

특징:

* 중복될 수 없다.
* NULL이 될 수 없다.
* 각 행을 고유하게 구분한다.

예를 들어 학생 테이블의 `id`가 PK라면 같은 `id`를 가진 학생을 두 명 저장할 수 없다.

---

### Foreign Key(FK)

**Foreign Key(외래 키)**는 다른 테이블의 값을 참조하여 테이블 사이의 관계를 표현한다.

예를 들어 수강신청 테이블에서

```text
student_id
course_id
```

를 사용하면 학생과 강의 테이블을 연결할 수 있다.

FK는 반드시 UNIQUE일 필요가 없다.

```text
학생 1명
↓
수강신청 여러 건
```

과 같은 **1:N 관계**를 표현할 수 있기 때문이다.

---

## 7. N:M 관계와 연결 테이블

학생과 강의의 관계를 생각하면 다음과 같다.

```text
학생 여러 명 ↔ 강의 여러 개
```

한 학생은 여러 강의를 수강할 수 있고, 하나의 강의도 여러 학생이 수강할 수 있다.

이러한 관계를 **N:M 관계**라고 한다.

이를 데이터베이스에서는 연결 테이블을 사용하여 표현할 수 있다.

```text
students
    ↓
enrollments
    ↓
courses
```

`enrollments`가 학생과 강의를 연결하는 역할을 한다.

---

## 8. JOIN

각 테이블에 데이터를 따로 저장해도 `JOIN`을 사용하면 서로 연결해서 조회할 수 있다.

```sql
SELECT
    e.id AS enrollment_id,
    s.name AS student_name,
    c.title AS course_title,
    e.status
FROM ch02_enrollments AS e
JOIN ch02_students AS s
    ON s.id = e.student_id
JOIN ch02_courses AS c
    ON c.id = e.course_id
ORDER BY e.id;
```

이를 통해 수강신청 정보와 학생 정보, 강의 정보를 하나의 결과로 확인할 수 있다.

---

## 9. 테이블과 SELECT 결과의 차이

데이터베이스에서 중요한 개념 중 하나는 **원본 테이블과 조회 결과는 다르다는 것**이다.

```text
원본 테이블
↓
SELECT + 조건
↓
조회 결과
```

`SELECT`는 원본 테이블 자체를 변경하는 것이 아니라 필요한 행이나 열을 조회하여 결과를 만들어낸다.

따라서 화면에 표시된 결과가 곧 테이블 전체라고 생각하면 안 된다.

---

## 10. 데이터베이스에서 AI 활용 시 주의점

AI에게 SQL이나 테이블 설계를 요청할 수 있지만, AI가 만든 결과를 그대로 사용하는 것은 위험할 수 있다.

확인해야 할 내용은 다음과 같다.

* 한 행이 무엇을 의미하는가?
* PK가 적절하게 설정되어 있는가?
* FK 관계가 올바른가?
* 중복 데이터가 발생하지 않는가?
* 1:N 또는 N:M 관계가 올바르게 표현되었는가?
* 실제 PostgreSQL에서 실행되는가?

결국 중요한 것은

```text
AI가 작성
→ 사람이 확인
→ 실제 DB에서 실행
→ 결과 검증
```

이라는 과정이다.

---

# 데이터 분석

## 1. 데이터 분석과 전처리

데이터 분석에서는 데이터를 바로 분석하기보다 먼저 **데이터의 구조와 품질을 확인하는 과정**이 필요하다.

전체적인 흐름은 다음과 같다.

```text
데이터 확인
→ 구조 확인
→ 데이터 타입 확인
→ 결측치 및 값 확인
→ 전처리
→ 분석
→ 결과 검증
```

전처리가 제대로 되지 않으면 이후 분석 결과도 정확하지 않을 수 있다.

---

## 2. CSV와 Pandas

데이터 분석에서는 CSV 파일 형태의 데이터를 자주 사용한다.

Python에서는 **Pandas(판다스)**를 이용해 CSV 파일을 읽어올 수 있다.

```python
import pandas as pd

df = pd.read_csv("파일경로.csv")
```

CSV 데이터를 Pandas의 **DataFrame(데이터프레임)** 형태로 가져오면 표 형태로 데이터를 분석할 수 있다.

---

## 3. DataFrame과 Series

Pandas에서 가장 중요한 자료구조는 `DataFrame`이다.

```text
DataFrame
→ 행과 열로 이루어진 표 형태의 데이터
```

하나의 컬럼을 선택하면 일반적으로 `Series`가 된다.

```python
customers["city"]
```

→ Series

```python
customers[["city"]]
```

→ DataFrame

즉, 대괄호의 사용 방식에 따라 반환되는 자료구조가 달라진다.

---

## 4. 데이터 구조 먼저 확인하기

CSV를 불러온 직후 바로 분석하기보다 데이터의 기본 구조를 확인해야 한다.

```python
df.head()
df.shape
df.info()
df.dtypes
```

### 주요 기능

* `head()` → 데이터 앞부분 확인
* `shape` → 행과 열의 개수 확인
* `info()` → 컬럼, 데이터 타입, 결측치 등 확인
* `dtypes` → 각 컬럼의 데이터 타입 확인

데이터 타입이 숫자인지 문자형인지 날짜형인지에 따라 사용할 수 있는 연산이 달라지기 때문에 반드시 확인해야 한다.

---

## 5. 실제 컬럼명 확인

AI에게 Pandas 코드를 작성하도록 요청할 때 특히 주의해야 하는 부분이다.

먼저 실제 데이터의 컬럼명을 확인한다.

```python
customers.columns.tolist()
```

실제 컬럼 이름과 코드에서 사용하는 컬럼 이름이 다르면 오류가 발생한다.

예를 들어 실제 컬럼명이

```text
customer_id
```

인데 코드에서

```text
cust_id
```

라고 작성하면 `KeyError`가 발생할 수 있다.

따라서

```text
실제 컬럼 확인
→ 코드 작성
→ 실행
→ 결과 확인
```

순서가 중요하다.

---

## 6. 조건에 따른 데이터 필터링

특정 조건에 맞는 행만 선택할 수 있다.

```python
customers[customers["age"] >= 30]
```

여기서

```python
customers["age"] >= 30
```

은 각 행에 대해 `True` 또는 `False`를 반환한다.

이러한 방식을 **Boolean Mask(불리언 마스크)**라고 한다.

주요 비교 연산자는 다음과 같다.

| 연산자  | 의미     |
| ---- | ------ |
| `==` | 같다     |
| `!=` | 다르다    |
| `>`  | 크다     |
| `>=` | 크거나 같다 |
| `<`  | 작다     |
| `<=` | 작거나 같다 |

---

## 7. 여러 조건 사용하기

여러 조건을 동시에 적용할 수 있다.

```python
customers[
    (customers["age"] >= 30)
    & (customers["city"] == "서울")
]
```

주요 연산자는 다음과 같다.

```text
& → 그리고
| → 또는
~ → 부정
```

여러 조건을 사용할 때는 각각의 조건을 괄호로 묶어주는 것이 중요하다.

여러 값 중 하나에 해당하는 데이터를 찾을 때는 `isin()`을 사용할 수 있다.

```python
customers[
    customers["city"].isin(["서울", "부산"])
]
```

---

## 8. 실제 데이터 값 확인하기

필터링 조건을 만들기 전에 실제 데이터에 어떤 값이 들어 있는지 확인하는 것도 중요하다.

```python
customers["city"].value_counts(dropna=False)

orders["order_status"].value_counts(dropna=False)
```

이렇게 하면 실제 데이터에 어떤 범주가 존재하는지 확인할 수 있다.

AI가 예상한 값과 실제 데이터의 값이 다를 수도 있기 때문에 **실제 값을 먼저 확인하는 습관**이 중요하다.

---

## 9. 데이터 정렬

`sort_values()`를 이용하면 특정 컬럼을 기준으로 데이터를 정렬할 수 있다.

```python
products.sort_values(
    "price",
    ascending=False
)
```

`ascending=False`는 내림차순을 의미한다.

여러 컬럼을 기준으로 정렬할 수도 있다.

```python
products.sort_values(
    ["category", "price"],
    ascending=[True, False]
)
```

---

## 10. 파생 컬럼 만들기

기존 컬럼을 이용해서 새로운 컬럼을 만들 수 있다.

예를 들어 주문 수량과 주문 당시 단가를 이용해 상품별 판매 금액을 계산할 수 있다.

```python
order_items["line_total"] = (
    order_items["quantity"]
    * order_items["unit_price"]
)
```

이처럼 기존 데이터를 이용하여 분석에 필요한 새로운 값을 만드는 것을 **파생 컬럼**이라고 한다.

---

## 11. 여러 데이터 연결하기

실제 데이터는 하나의 파일에 모든 정보가 들어 있지 않고 여러 CSV 파일로 나뉘어 있는 경우가 많다.

예를 들어 쇼핑몰 데이터는 다음과 같이 구성될 수 있다.

```text
customers.csv
products.csv
orders.csv
order_items.csv
```

각 파일에서 한 행이 의미하는 것도 다르다.

```text
customers
→ 고객 한 명

products
→ 상품 한 개

orders
→ 주문 한 건

order_items
→ 주문에 포함된 상품 한 항목
```

따라서 데이터를 연결할 때는 단순히 컬럼 이름만 맞추는 것이 아니라 **각 데이터의 관계와 한 행의 의미를 이해해야 한다.**

---

## 12. merge를 이용한 데이터 결합

Pandas에서는 `merge()`를 이용해 DataFrame을 연결할 수 있다.

예를 들어 주문 상세 데이터와 주문 데이터를 연결할 때는 `order_id`를 기준으로 사용할 수 있다.

```text
order_items.order_id
        ↓
orders.order_id
```

상품 정보는

```text
order_items.product_id
        ↓
products.product_id
```

를 통해 연결할 수 있다.

`merge()`를 사용할 때는 다음 내용을 확인해야 한다.

* 어떤 컬럼을 기준으로 연결하는가?
* 해당 컬럼은 PK인가 FK인가?
* 병합 전 행 수는 얼마인가?
* 병합 후 행 수는 어떻게 변했는가?
* 연결되지 않은 데이터가 존재하는가?

---

## 13. 완료 주문 기준 매출 분석

매출을 계산할 때는 먼저 어떤 주문을 분석 대상으로 할 것인지 기준을 정해야 한다.

예를 들어 완료된 주문만 매출에 포함한다면 다음과 같이 기준을 잡을 수 있다.

```text
매출
→ completed 주문만 포함

상품 금액
→ quantity × unit_price

주문 수
→ 고유 order_id 개수

고객 수
→ 고유 customer_id 개수

취소·환불 주문
→ 매출에서 제외
```

따라서 단순히 모든 주문의 금액을 더하는 것과 **완료 주문 기준 매출**은 결과가 다를 수 있다.

---

## 14. groupby를 이용한 집계

데이터를 특정 기준으로 그룹화하여 통계를 계산할 수 있다.

주요 기능:

```python
groupby()
agg()
value_counts()
```

예를 들어 카테고리별 매출을 계산하면 결과의 한 행은 **카테고리 하나**를 의미한다.

```text
category
total_sales
order_count
customer_count
quantity_sold
```

각 컬럼의 의미를 정확하게 정의하는 것이 중요하다.

* `category` → 상품 카테고리
* `total_sales` → 완료 주문 기준 매출 합계
* `order_count` → 고유 주문 수
* `customer_count` → 고유 고객 수
* `quantity_sold` → 판매 수량 합계

---

## 15. count(), size(), nunique() 차이

집계할 때 사용하는 함수가 어떤 값을 세는지 이해해야 한다.

```text
count()
→ 결측치를 제외하고 값의 개수를 계산

size()
→ 그룹에 포함된 행의 개수를 계산

nunique()
→ 중복을 제외한 고유값의 개수를 계산
```

예를 들어 주문 수를 구할 때 단순 `count()`를 사용하면 중복된 주문 상세 행까지 계산될 수 있다.

따라서 **무엇을 세고 싶은지 먼저 정의한 뒤 적절한 집계 함수를 선택해야 한다.**

---

## 16. 데이터 분석 결과 검증

오늘 데이터 분석에서 가장 중요한 부분은 **코드가 실행된다고 해서 분석 결과가 정확한 것은 아니라는 점**이다.

특히 AI가 작성한 코드를 사용할 때는 다음을 확인해야 한다.

```text
실제 DataFrame 이름 확인
→ 실제 컬럼명 확인
→ 실제 범주값 확인
→ 한 행의 의미 확인
→ 필터 조건 확인
→ 계산식 확인
→ merge 기준 확인
→ 병합 전후 행 수 확인
→ 미매칭 데이터 확인
→ 집계 결과 확인
→ 원본 데이터와 결과 비교
```

즉,

```text
코드 실행 성공
≠
분석 결과 정확
```

이다.

---

# 오늘의 핵심 정리

## 데이터베이스

```text
PostgreSQL
→ DBMS

DBeaver
→ PostgreSQL 접속 및 관리 도구

Database
→ 데이터 저장 공간

Schema
→ Database 안의 객체를 구분

Table
→ 데이터를 행과 열로 저장
```

핵심 개념:

* DBMS와 Database는 다르다.
* DBeaver는 데이터베이스가 아니다.
* 테이블의 한 행이 무엇을 의미하는지 명확해야 한다.
* PK는 행을 고유하게 식별한다.
* FK는 테이블 사이의 관계를 표현한다.
* FK는 중복될 수 있다.
* 1:N 관계와 N:M 관계를 구분해야 한다.
* N:M 관계는 연결 테이블을 이용하여 표현할 수 있다.
* `JOIN`으로 여러 테이블의 데이터를 연결해서 조회할 수 있다.
* SELECT 결과와 원본 테이블은 다르다.
* AI가 만든 SQL과 테이블 구조도 실제 DB에서 검증해야 한다.

## 데이터 분석

```text
CSV
→ Pandas
→ DataFrame
→ 구조 확인
→ 데이터 전처리
→ 필터링
→ 정렬
→ 파생 컬럼
→ merge
→ groupby
→ 결과 검증
```

핵심 개념:

* 분석 전에 데이터 구조와 품질을 확인한다.
* DataFrame과 Series를 구분한다.
* `head()`, `shape`, `info()`, `dtypes`를 활용한다.
* 실제 컬럼명을 먼저 확인한다.
* Boolean Mask를 이용하여 조건에 맞는 데이터를 필터링한다.
* `&`, `|`, `~`, `isin()`으로 조건을 조합한다.
* `sort_values()`로 데이터를 정렬한다.
* 기존 컬럼을 이용해 파생 컬럼을 만들 수 있다.
* `merge()`에서는 연결 키와 데이터 관계를 확인한다.
* `groupby()`로 그룹별 집계를 수행할 수 있다.
* `count()`, `size()`, `nunique()`의 차이를 이해해야 한다.
* 코드가 실행되는 것과 분석 결과가 정확한 것은 다르다.
* AI가 작성한 분석 코드도 실제 데이터와 결과를 비교하여 검증해야 한다.
