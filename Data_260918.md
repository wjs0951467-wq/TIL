# 2026-09-18 TIL 도서 제목 전처리와 텍스트 빈도 분석

## 오늘 배운 것

교보문고 베스트셀러 데이터를 분석하기 좋은 형태로 전처리하고, 도서 제목에서 의미 있는 단어를 추출해 빈도와 워드클라우드로 표현했다.

텍스트 분석도 바로 시각화하는 것이 아니라 데이터 구조를 확인하고, 분석 목적에 맞게 정제하는 과정이 먼저 필요하다는 것을 배웠다.

## 핵심 개념

### 1. 도서 데이터 기본 전처리

데이터를 불러온 뒤 다음 내용을 확인했다.

- `head()`로 실제 데이터 확인
- `shape`로 행과 열의 개수 확인
- `columns`로 컬럼명 확인
- `dtypes`, `info()`로 자료형 확인
- `isna().sum()`으로 결측치 확인

원본 데이터는 도서 199개와 13개 컬럼으로 구성되어 있었다.

분석에 필요한 순위, 상품 ID, 상품명, 판매가, 저자, 출판사, 출판일자, 분야 등의 컬럼만 선택했다.

### 2. 자료형과 문자열 정리

분석에 바로 사용하기 어려운 값을 다음과 같이 정리했다.

- 컬럼명을 이해하기 쉬운 이름으로 변경
- 상품명, 저자, 출판사, 분야의 앞뒤 공백 제거
- 판매가의 쉼표를 제거하고 숫자형으로 변환
- 출판일자를 날짜형으로 변환
- 결측치와 중복 데이터 확인

판매가가 문자열이면 합계나 평균을 계산할 수 없다. 출판일자가 문자열이면 연도와 월을 기준으로 분석하기 어렵다.

따라서 데이터가 가진 의미에 맞는 자료형으로 바꾸는 과정이 필요하다.

### 3. 전처리 결과 저장

전처리가 끝난 데이터는 CSV 파일로 저장했다.

정제된 데이터를 저장해 두면 다음 분석에서 원본 데이터를 다시 전처리하지 않고 바로 사용할 수 있다.

전처리 후에도 다음 내용을 다시 확인해야 한다.

- 데이터 개수
- 컬럼별 자료형
- 결측치
- 중복 데이터
- 변환된 값

### 4. 도서 제목 단어 빈도 분석

도서 제목에서 자주 등장하는 단어를 알아보기 위해 제목을 단어 단위로 나누고 빈도를 계산했다.

단어 빈도는 다음과 같은 방법으로 구할 수 있다.

- 반복문과 딕셔너리
- `dict.get()`
- `collections.Counter`
- pandas의 `value_counts()`

이 중 `Counter`는 코드가 간결하고 `most_common()`을 사용해 상위 단어를 쉽게 확인할 수 있어 텍스트 분석에 편리하다.

상위 N개 단어를 선택할 때는 같은 빈도수를 가진 단어가 있을 수 있다. 따라서 단순히 개수만 자를 것인지, 공동 순위까지 포함할 것인지 기준을 정해야 한다.

### 5. 정규표현식을 이용한 토큰화

단순히 공백으로 제목을 나누면 숫자, 괄호, 특수문자 등이 단어에 포함될 수 있다.

정규표현식을 사용하면 한글과 영문처럼 필요한 문자만 남겨 제목을 비교적 깔끔하게 분리할 수 있다.

하지만 정규표현식만으로는 다음 단어를 같은 단어로 처리하기 어렵다.

- 데이터는
- 데이터의
- 데이터가

한국어는 단어에 조사와 어미가 붙기 때문에 정확한 분석을 위해 형태소 분석이 필요하다.

### 6. Kiwi 형태소 분석

Kiwi는 문장을 형태소 단위로 나누고 각 단어의 품사를 구분하는 한국어 형태소 분석기다.

이번 실습에서는 다음과 같은 단어를 중심으로 추출했다.

- 일반 명사
- 고유 명사
- 필요한 영문 단어

형태소 분석을 적용하면 조사와 어미를 제거하고 실제 의미를 가진 명사 중심으로 단어를 모을 수 있다.

샘플 문장으로 함수의 동작을 먼저 확인한 뒤 전체 도서 제목에 적용했다.

### 7. 불용어 제거

자주 등장하는 단어가 모두 분석에 중요한 것은 아니다.

다음과 같은 단어는 도서의 주제보다 상품 구성이나 판본 정보를 나타낼 수 있다.

- 세트
- 개정판
- 에디션
- 리커버
- 기념
- 한정판

이처럼 분석 목적과 관련성이 낮은 단어를 불용어로 지정해 제거했다.

한 글자 단어도 의미가 불분명한 경우가 많아 함께 제외했다.

불용어는 무조건 정해진 목록을 사용하는 것이 아니라 실제 빈도 결과를 확인하면서 분석 목적에 맞게 조정해야 한다.

### 8. 분석 결과와 시각화

정제된 단어의 빈도를 계산하고 상위 30개 단어를 CSV 파일로 저장했다.

최종 결과는 다음과 같다.

- 분석한 도서 제목: 199개
- 최종 추출 단어: 429개
- 상위 단어: 토익, 해커스, 스페셜, 필사, 인생, 수업, 시대, LC, RC 등
- 상위 30개 단어 CSV 저장
- 워드클라우드 이미지 저장

막대그래프는 단어별 빈도를 정확하게 비교하기 좋고, 워드클라우드는 전체적으로 어떤 단어가 많이 등장하는지 빠르게 확인하기 좋다.

워드클라우드에서 한글이 깨지지 않도록 한글 폰트 설정도 필요하다.

## 가장 중요하게 느낀 것

텍스트 분석 결과는 전처리 기준에 따라 크게 달라진다.

단순히 많이 나온 단어를 보여 주는 것보다 어떤 품사를 선택했고, 어떤 단어를 불용어로 제거했는지 설명할 수 있어야 한다.

또한 전처리 전후에 데이터 개수, 결측치, 중복, 자료형을 다시 확인해야 데이터가 잘못 제거되거나 변형되지 않았는지 검증할 수 있다.

## 실습·복습 체크리스트

- [x] 엑셀 데이터를 DataFrame으로 불러오기
- [x] 데이터 크기, 컬럼, 자료형 확인하기
- [x] 필요한 컬럼만 선택하기
- [x] 컬럼명을 이해하기 쉽게 변경하기
- [x] 판매가를 숫자형으로 변환하기
- [x] 출판일자를 날짜형으로 변환하기
- [x] 문자열의 앞뒤 공백 제거하기
- [x] 결측치와 중복 데이터 확인하기
- [x] 정제된 데이터를 CSV로 저장하기
- [x] 제목을 단어 단위로 토큰화하기
- [x] `Counter`로 단어 빈도 계산하기
- [x] Kiwi로 명사와 고유 명사 추출하기
- [x] 한 글자 단어와 불용어 제거하기
- [x] 상위 30개 단어를 CSV로 저장하기
- [x] 막대그래프와 워드클라우드로 결과 확인하기

## 오늘의 핵심 한 줄

텍스트 분석의 품질은 형태소 분석보다 먼저 데이터 구조를 확인하고 전처리 기준을 세우는 과정에서 결정된다.
