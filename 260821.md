# 2026-08-21 TIL 파이썬 기초 문법 — 변수와 데이터 타입부터 연산자, 문자열, 반복문

## 오늘 배운 것

오늘은 파이썬의 기본 문법을 배우면서 **데이터를 저장하고, 계산하고, 조건에 따라 처리하고, 반복해서 실행하는 방법**을 익혔다.

처음에는 각각의 문법이 별개의 내용처럼 보였지만, 결국 프로그램은 **데이터를 저장 → 처리 → 흐름을 제어 → 반복 실행**하는 방식으로 만들어진다는 것을 이해했다.

---

## 핵심 개념

### 1. 변수와 데이터 타입

변수는 데이터를 저장해두고 필요할 때 꺼내 사용하는 이름이다.

```python
name = "예진"
age = 29
```

여기서 `name`, `age`가 변수이고, 저장된 데이터의 종류를 **데이터 타입**이라고 한다.

| 타입      | 의미   | 예시              |
| ------- | ---- | --------------- |
| `int`   | 정수   | `10`            |
| `float` | 실수   | `10.5`          |
| `str`   | 문자열  | `"Python"`      |
| `bool`  | 참/거짓 | `True`, `False` |

데이터 타입은 필요에 따라 변환할 수도 있다.

```python
int("20")
float("20")
str(20)
```

특히 `input()`으로 입력받은 값은 기본적으로 **문자열(`str`)**이기 때문에 숫자로 계산하려면 `int()`나 `float()` 등을 이용해 변환해야 한다.

---

### 2. 연산자를 이용한 데이터 처리

변수에 저장한 데이터를 계산하거나 비교할 때 연산자를 사용한다.

#### 산술 연산자

```python
10 + 3   # 더하기
10 - 3   # 빼기
10 * 3   # 곱하기
10 / 3   # 나누기
10 // 3  # 몫
10 % 3   # 나머지
10 ** 2  # 제곱
```

#### 비교 연산자

비교 결과는 `True` 또는 `False`로 나온다.

```python
10 > 5
10 < 5
10 == 10
10 != 5
```

* `>` : 크다
* `<` : 작다
* `>=` : 크거나 같다
* `<=` : 작거나 같다
* `==` : 같다
* `!=` : 같지 않다

여기서 **`=`와 `==`는 다르다.**

```python
x = 10      # 값을 저장
x == 10     # 같은지 비교
```

---

### 3. 값을 계속 변경하기

변수에 기존 값을 유지하면서 새로운 값을 더할 수도 있다.

```python
x = 10
x += 1
```

`x += 1`은 다음과 같은 의미이다.

```python
x = x + 1
```

즉, **기존 값에 새로운 값을 더한 후 다시 저장**하는 방식이다.

이 개념은 나중에 반복문에서 값을 계속 누적할 때 매우 중요하게 사용된다.

---

### 4. 문자열 다루기

문자열은 문자들을 이어서 표현한 데이터이다.

```python
text = "Python"
```

문자열과 변수를 함께 사용할 때는 **F-string**을 사용할 수 있다.

```python
name = "예진"
print(f"안녕하세요 {name}님")
```

`f"문자열 {변수}"` 형태로 작성하면 변수의 값이 문자열 안에 들어간다.

---

### 5. 반복문

같은 작업을 여러 번 해야 할 때는 반복문을 사용한다.

예를 들어 같은 문장을 100번 출력해야 한다면 코드를 100줄 작성하는 것은 비효율적이다.

```python
for i in range(100):
    print("Python")
```

반복문을 사용하면 **코드의 중복을 줄이고 같은 작업을 효율적으로 반복**할 수 있다.

파이썬의 대표적인 반복문에는 `for`문과 `while`문이 있다.

---

### 6. for문과 range()

`for`문의 기본 형태는 다음과 같다.

```python
for 변수 in 반복할_대상:
    실행할 코드
```

`range(5)`는 `0, 1, 2, 3, 4`라는 다섯 개의 값을 만든다.

```python
for i in range(5):
    print(i)
```

결과:

```text
0
1
2
3
4
```

여기서 `i`는 `range(5)`에서 숫자를 하나씩 받아오는 변수라고 생각하면 된다.

```text
i = 0 → 실행
i = 1 → 실행
i = 2 → 실행
i = 3 → 실행
i = 4 → 실행
```

따라서 `range(5)`는 **1부터 5가 아니라 0부터 4까지**라는 것을 기억해야 한다.

---

### 7. 반복문을 이용한 누적

반복문에서는 반복할 때마다 값을 더하거나 쌓는 작업도 할 수 있다.

```python
result = 0

for i in range(5):
    result += i

print(result)
```

실행 과정을 따라가면:

```text
처음 result = 0

i = 0 → result = 0
i = 1 → result = 1
i = 2 → result = 3
i = 3 → result = 6
i = 4 → result = 10
```

최종 결과는 `10`이다.

여기서 중요한 것은 `result`가 반복될 때마다 사라지는 것이 아니라 **이전 값을 유지하면서 계속 누적된다**는 것이다.

---

### 8. 중첩 반복문

반복문 안에 또 다른 반복문을 넣을 수도 있다.

```python
for row in range(3):
    for col in range(4):
        print("*", end="")
    print()
```

결과:

```text
****
****
****
```

이때는 바깥쪽 반복문이 한 번 실행될 때마다 **안쪽 반복문이 끝까지 실행**된다.

따라서 코드를 볼 때는 단순히 위에서 아래로 읽는 것보다

> **현재 바깥 반복이 몇 번째인지 → 안쪽 반복이 몇 번 실행되는지**

를 하나씩 따라가야 한다.

---

## 가장 중요하게 느낀 것

이번에 배운 문법들은 각각 따로 외우는 것보다 **서로 연결해서 사용하는 것이 중요하다.**

예를 들어,

```text
변수
 ↓
데이터 저장
 ↓
연산자로 계산·비교
 ↓
조건에 따라 처리
 ↓
반복문으로 여러 번 실행
```

이런 식으로 하나의 프로그램 흐름이 만들어진다.

특히 반복문은 처음에는 코드가 어떻게 움직이는지 헷갈릴 수 있기 때문에 **실행 결과만 보는 것이 아니라 변수의 값이 반복할 때마다 어떻게 변하는지 직접 따라가는 연습**이 필요하다.

또한 `result += i`처럼 지금까지 배운 문법을 두세 개 조합하는 순간 난이도가 확 올라갈 수 있다는 것도 느꼈다.

결국 프로그래밍에서 중요한 것은 문법을 많이 외우는 것이 아니라, **배운 문법을 조합해서 문제를 해결할 수 있는 능력**이라는 것을 알게 되었다.

---

## 🧪 실습 예시 코드

### 숫자를 반복해서 출력하기

```python
for i in range(5):
    print(i)
```

### 숫자의 합 구하기

```python
result = 0

for i in range(5):
    result += i

print(result)
```

### 중첩 반복문으로 별 출력하기

```python
for row in range(3):
    for col in range(4):
        print("*", end="")
    print()
```

### F-string 사용하기

```python
name = "예진"
age = 29

print(f"{name}님의 나이는 {age}살입니다.")
```

---

## ✅ 실습·복습 체크리스트

* [ ] 변수와 데이터 타입의 차이 이해하기
* [ ] `int`, `float`, `str`, `bool` 구분하기
* [ ] `int()`, `float()`, `str()`로 타입 변환해보기
* [ ] 산술 연산자 사용해보기
* [ ] 비교 연산자의 결과가 `True` / `False`라는 것 이해하기
* [ ] `=`와 `==`의 차이 이해하기
* [ ] `+=`의 의미 이해하기
* [ ] 문자열과 변수 함께 사용해보기
* [ ] F-string 직접 작성해보기
* [ ] `for`문의 기본 구조 이해하기
* [ ] `range(5)`가 `0~4`라는 것 기억하기
* [ ] 반복하면서 변수의 값이 바뀌는 과정 따라가기
* [ ] 반복문을 이용해 합계 구해보기
* [ ] 중첩 `for`문의 실행 순서 직접 따라가보기
* [ ] 코드를 실행하기 전에 결과를 먼저 예상해보기

---

## 🎯 오늘의 핵심 한 줄

> **파이썬은 데이터를 변수에 저장하고, 연산자로 처리한 뒤, 조건과 반복을 이용해 프로그램의 흐름을 제어하는 언어이며, 중요한 것은 문법 암기보다 배운 내용을 조합해 활용하는 것이다.**
