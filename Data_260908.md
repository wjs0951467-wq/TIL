# 2026-09-08 TIL 데이터베이스 모델링 기초와 Python 프로젝트 환경 구성

# 데이터베이스

## 오늘 배운 것

- 요구사항에서 데이터 찾기
- 테이블과 속성 구분하기
- PK와 FK
- 테이블 간 관계와 Cardinality
- 1:N 관계
- N:M 관계와 중간 테이블
- ERD 작성
- Master Data와 Transaction Data
- 스키마 생성
- 테이블 생성 순서
- INSERT와 SELECT
- 주문과 주문 상세 데이터 구조
- JOIN이 필요한 이유
- GROUP BY와 집계의 기본 개념

## 핵심 개념

### 1. 데이터베이스 설계는 업무에서 데이터를 찾는 것부터 시작한다

요구사항을 읽을 때 먼저

> 무엇을 데이터로 관리해야 하는가?

를 찾아야 한다.

그다음 데이터에서

- 테이블
- 속성
- 관계

를 찾아낸다.

---

### 2. 테이블, 행, 열

테이블은 데이터를 보관하는 구조이다.

행(Row)은 하나의 실제 데이터 또는 레코드이고,
열(Column)은 그 데이터의 속성이다.

예를 들어 고객 테이블이라면

```text
고객
├─ 이름
├─ 전화번호
├─ 이메일
└─ 가입일
````

처럼 구성할 수 있다.

여기서 고객은 테이블이고
이름, 전화번호, 이메일, 가입일은 속성이다.

---

### 3. Master Data와 Transaction Data

Master Data는 비교적 자주 변하지 않고
다른 업무 데이터의 기준이 되는 데이터이다.

예:

```text
고객
메뉴
제품
```

Transaction Data는 업무가 발생할 때 계속 생성되는 데이터이다.

예:

```text
주문
결제
예약
```

카페에서는 고객과 메뉴가 먼저 존재해야 주문이 발생할 수 있다.

---

### 4. PK와 FK

PK(Primary Key)는 각 행을 고유하게 식별하는 값이다.

예:

```text
customers.id
menus.id
orders.id
```

FK(Foreign Key)는 다른 테이블의 PK를 참조하는 값이다.

예:

```text
orders.customer_id
```

은

```text
customers.id
```

를 참조한다.

관계에서 일반적으로

```text
1 쪽 → PK
N 쪽 → FK
```

의 구조가 만들어진다.

---

### 5. Cardinality

테이블 간 데이터 개수의 관계를 Cardinality라고 한다.

대표적인 관계는

```text
1 : 1
1 : N
N : M
```

이다.

카페에서는 고객 한 명이 여러 번 주문할 수 있기 때문에

```text
customers 1 : N orders
```

관계이다.

---

### 6. N:M 관계는 중간 테이블로 해결한다

한 주문에는 여러 메뉴가 포함될 수 있고,
하나의 메뉴도 여러 주문에 포함될 수 있다.

따라서

```text
orders N : M menus
```

관계가 발생한다.

관계형 데이터베이스에서는 이를 중간 테이블로 풀어낸다.

```text
orders
   1
   |
   N
order_details
   N
   |
   1
menus
```

`order_details`가 주문과 메뉴를 연결하는 역할을 한다.

---

### 7. 카페 주문 시스템 기본 테이블

수업에서는 다음 네 테이블을 중심으로 모델링했다.

```text
customers
menus
orders
order_details
```

관계는 다음과 같다.

```text
customers
    1
    |
    N
orders
    1
    |
    N
order_details
    N
    |
    1
menus
```

---

### 8. 주문 당시 가격을 저장해야 하는 이유

메뉴 테이블에는 현재 메뉴 가격이 저장된다.

하지만 메뉴 가격은 나중에 변경될 수 있다.

예를 들어 어제 4,500원에 판매한 아메리카노가
오늘 5,000원으로 변경되었다고 해서
어제의 주문 금액까지 5,000원으로 바뀌면 안 된다.

따라서 `order_details`에는

```text
quantity
unit_price
```

처럼 주문 당시의 수량과 가격을 저장하는 것이 중요하다.

---

### 9. ERD를 보는 방법

ERD를 작성하거나 확인할 때 다음을 점검한다.

```text
모든 테이블에 PK가 있는가?

FK가 올바른 테이블에 있는가?

1:N 관계의 방향이 자연스러운가?

N:M 관계가 그대로 남아 있지 않은가?

한 컬럼에 여러 값을 넣고 있지는 않은가?
```

특히 하나의 컬럼에

```text
아메리카노, 라떼, 소금빵
```

처럼 여러 값을 함께 저장하면 안 된다.

필요하면 별도의 테이블로 분리해야 한다.

---

### 10. Snapshot이 아니라 Long-term 관점으로 관계를 본다

한 장의 영수증만 보면 메뉴와 주문 상세의 관계가
1:1처럼 보일 수 있다.

하지만 데이터베이스는 한 건의 데이터만 관리하지 않는다.

시간이 지나 수천 건의 주문이 누적되면
하나의 메뉴는 여러 `order_details`에 등장한다.

따라서 데이터 관계는 한 순간의 Snapshot보다
장기간 데이터가 축적되는 관점에서 판단해야 한다.

---

### 11. 테이블 생성 순서

FK가 있는 테이블은 참조할 부모 테이블이 먼저 존재해야 한다.

따라서 카페 예제에서는 대략 다음 순서로 생성한다.

```text
1. customers
2. menus
3. orders
4. order_details
```

부모 테이블을 먼저 만들고
자식 테이블을 나중에 만든다.

---

### 12. 데이터 입력 순서

데이터도 동일한 흐름을 가진다.

```text
고객 등록
↓
메뉴 등록
↓
주문 생성
↓
주문 상세 생성
```

주문에는 `customer_id`가 필요하기 때문에
고객이 먼저 존재해야 한다.

주문 상세에는

```text
order_id
menu_id
quantity
unit_price
```

등이 필요하기 때문에
주문과 메뉴가 먼저 존재해야 한다.

---

### 13. INSERT

한 건의 주문은 다음과 같은 구조로 입력할 수 있다.

```sql
INSERT INTO orders (customer_id, status)
VALUES (1, '주문');
```

여러 개의 주문 상세는 한 번의 INSERT로 입력할 수도 있다.

```sql
INSERT INTO order_details
    (order_id, menu_id, quantity, unit_price)
VALUES
    (1, 1, 2, 4500),
    (1, 2, 3, 4000),
    (1, 3, 1, 5000);
```

---

### 14. SELECT와 LIMIT

데이터를 확인할 때는

```sql
SELECT *
FROM orders
LIMIT 10;
```

처럼 필요한 일부 데이터만 확인하는 습관이 중요하다.

운영 데이터는 수천 건, 수만 건 이상 존재할 수 있기 때문에
무조건 전체 데이터를 조회하는 습관은 좋지 않다.

---

### 15. JOIN이 필요한 이유

`order_details`에는 메뉴의 ID가 있지만
메뉴 이름까지 직접 저장할 필요는 없다.

메뉴 이름이 필요하면 `menus` 테이블과 JOIN한다.

즉,

```text
어떤 데이터가 필요한가
↓
어떤 테이블에 있는가
↓
한 테이블에서 해결할 수 있는가
↓
없다면 어떤 테이블을 JOIN해야 하는가
```

순서로 생각해야 한다.

---

### 16. 집계할 때 먼저 테이블을 찾는다

예를 들어 다음 요구사항이 있다고 하자.

```text
오늘 주문 건수
오늘 총 주문 금액
가장 많이 판매된 메뉴
메뉴 종류별 판매 금액
고객별 주문 횟수
주문 상태별 건수
```

쿼리를 바로 작성하기 전에
먼저 필요한 테이블과 컬럼을 생각해야 한다.

예:

```text
주문 건수
→ orders

주문 상태별 건수
→ orders

많이 판매된 메뉴 ID
→ order_details

많이 판매된 메뉴 이름
→ order_details + menus

고객별 주문 횟수
→ customers + orders
```

테이블 구조를 이해해야 올바른 SQL을 작성할 수 있다.

---

## 가장 중요하게 느낀 것

SQL 문법을 외우는 것보다 먼저

> 요구사항에서 테이블과 속성을 찾고
> 테이블 사이의 관계를 이해하는 능력

이 중요하다.

ERD를 제대로 이해하면
어떤 데이터를 어느 테이블에서 가져와야 하는지 알 수 있고
그다음 SELECT, JOIN, GROUP BY 같은 SQL을 작성할 수 있다.

---

## 데이터베이스 복습 체크리스트

* [ ] Table, Row, Column의 차이를 설명할 수 있다.
* [ ] Master Data와 Transaction Data를 구분할 수 있다.
* [ ] PK와 FK의 역할을 설명할 수 있다.
* [ ] 1:N 관계를 설명할 수 있다.
* [ ] N:M 관계에 중간 테이블이 필요한 이유를 설명할 수 있다.
* [ ] 고객-주문-주문상세-메뉴 관계를 그릴 수 있다.
* [ ] 주문 당시 가격을 별도로 저장하는 이유를 설명할 수 있다.
* [ ] ERD를 보고 부모 테이블과 자식 테이블을 구분할 수 있다.
* [ ] 테이블 생성 순서를 판단할 수 있다.
* [ ] 데이터 INSERT 순서를 판단할 수 있다.
* [ ] 필요한 데이터가 어떤 테이블에 있는지 찾을 수 있다.
* [ ] JOIN이 필요한 상황을 판단할 수 있다.
* [ ] `SELECT`, `INSERT`, `LIMIT`의 기본 사용법을 이해했다.

---

# 데이터 분석

## 오늘 배운 것

* Python 프로젝트와 Jupyter Notebook의 실행 구조
* Python 인터프리터와 Jupyter Kernel의 역할
* 현재 작업 경로와 프로젝트 루트의 차이
* `pathlib.Path`를 이용한 경로 처리
* 프로젝트 루트를 찾는 로직
* 반복문과 조건문을 이용한 상위 폴더 탐색
* 공통 기능의 함수화
* 공통 함수의 모듈화
* `__init__.py`의 역할
* Notebook에서 프로젝트 패키지를 찾지 못하는 이유
* `pyproject.toml`과 editable install을 이용한 프로젝트 설정

## 핵심 개념

### 1. Python 프로젝트와 Jupyter Notebook

현재 프로젝트는 Jupyter Notebook 자체가 하나의 프로젝트인 것이 아니라

> Python 프로젝트 안에서 Jupyter Notebook을 사용하는 구조

이다.

실제 Python 코드를 실행하는 것은 Python 인터프리터이고,
Jupyter Notebook은 Kernel을 통해 Python 인터프리터와 연결된다.

이 때문에 Python 프로젝트가 인식하는 기준 경로와
Notebook Kernel이 인식하는 실행 경로가 달라질 수 있다.

---

### 2. 현재 경로와 프로젝트 루트는 다를 수 있다

```python
from pathlib import Path

current = Path.cwd()
print(current)
```

`Path.cwd()`는 프로젝트 루트가 아니라
현재 실행 중인 위치를 반환한다.

예를 들어 다음과 같은 구조가 있다고 하자.

```text
project-root/
├─ data/
│  └─ raw/
├─ notebooks/
│  └─ ch04/
└─ course_utils/
```

Notebook이 `notebooks/ch04`에서 실행된다면

```python
Path.cwd()
```

의 결과도 `notebooks/ch04`가 될 수 있다.

따라서 현재 위치를 무조건 프로젝트 루트라고 생각하면 안 된다.

---

### 3. 프로젝트 루트를 찾는 기본 아이디어

프로젝트 루트를 찾기 위해서는 현재 위치부터 시작하여
부모 폴더를 하나씩 확인할 수 있다.

수도코드로 표현하면 다음과 같다.

```text
현재 폴더에서 시작한다.

현재 폴더 안에 data 폴더가 있는지 확인한다.

있다면
    현재 폴더를 프로젝트 루트로 사용한다.
    탐색을 종료한다.

없다면
    부모 폴더로 한 단계 올라간다.

위 과정을 반복한다.
```

상위 폴더가 몇 단계인지 미리 알 수 없기 때문에
`for`보다 `while` 반복문이 적합한 경우다.

---

### 4. pathlib의 주요 기능

현재 위치 확인

```python
Path.cwd()
```

부모 폴더 이동

```python
current.parent
```

경로 연결

```python
current / "data"
```

디렉터리 존재 여부 확인

```python
(current / "data").is_dir()
```

절대 경로 확인

```python
current.resolve()
```

문자열을 `+`로 연결해서 경로를 만들기보다는
`Path` 객체와 `/` 연산자를 사용하는 것이 안전하다.

---

### 5. 반복해서 사용하는 기능은 함수로 만든다

프로젝트 루트 탐색은 여러 Notebook과 Python 파일에서 반복해서 사용할 수 있다.

따라서 코드 전체를 반복해서 작성하는 것보다

```text
get_project_root()
get_data_dir()
```

같은 함수로 만들어 사용하는 것이 좋다.

함수를 만들 때는 두 가지를 먼저 생각한다.

1. 어떤 값을 입력받아야 하는가?
2. 어떤 값을 반환해야 하는가?

재사용 가능한 기능을 함수로 만드는 것이 중요하다.

---

### 6. 함수에서 모듈로 확장하기

하나의 Notebook에서만 함수를 정의하면
다른 Notebook에서는 그대로 사용할 수 없다.

따라서 공통 기능은 별도의 파일로 분리한다.

```text
project-root/
├─ course_utils/
│  ├─ __init__.py
│  └─ path.py
├─ data/
└─ notebooks/
```

`path.py`에는 경로 관련 함수를 모아 둘 수 있다.

이렇게 하면 여러 파일에서 같은 기능을 재사용할 수 있다.

---

### 7. `__init__.py`

Python 폴더를 패키지 구조로 사용할 때

```text
__init__.py
```

파일을 둘 수 있다.

예:

```text
course_utils/
├─ __init__.py
└─ path.py
```

이를 통해 `course_utils` 안의 기능을 모듈처럼 관리할 수 있다.

---

### 8. Notebook에서 모듈을 찾지 못하는 문제

예를 들어 다음 import가 실패할 수 있다.

```python
from course_utils.path import get_project_root
```

문제는 `path.py` 함수 자체가 아니라
Jupyter Kernel이 프로젝트 루트의 `course_utils` 패키지를
탐색 경로에서 찾지 못하는 데 있을 수 있다.

수업 중에는 `sys.path`에 프로젝트 루트를 추가하는 방법도 확인했다.

하지만 이 방법은 Notebook마다 코드를 반복해서 추가해야 한다는 단점이 있다.

---

### 9. 프로젝트 수준에서 해결하기

최종적으로는 Notebook마다 `sys.path`를 수정하는 방식보다
Python 프로젝트 자체를 올바르게 설치하는 방법이 더 깔끔하다.

프로젝트 루트에

```text
pyproject.toml
```

을 구성하고 프로젝트 루트에서 다음 명령을 실행한다.

```bash
pip install -e .
```

`-e`는 editable install을 의미한다.

개발 중인 프로젝트를 설치된 Python 프로젝트처럼 사용할 수 있으면서
소스 코드를 수정하면 변경 내용도 바로 반영할 수 있다.

이를 통해 Python 인터프리터와 Jupyter Notebook Kernel 사이의
프로젝트 인식 차이를 줄일 수 있다.

---

## 가장 중요하게 느낀 것

단순히 에러가 없어지도록 `sys.path`를 수정하는 것보다

> 왜 Notebook이 프로젝트의 모듈을 찾지 못하는지를 이해하고
> 프로젝트 구조 자체에서 문제를 해결하는 것이 중요하다.

또한 팀 프로젝트에서는 각 Notebook이 직접 복잡한 경로를 계산하기보다
공통 경로 처리 함수를 하나 만들어 함께 사용하는 것이 좋다.

---

## 데이터 분석 복습 체크리스트

* [ ] Python 인터프리터와 Jupyter Kernel의 차이를 설명할 수 있다.
* [ ] `Path.cwd()`가 항상 프로젝트 루트를 의미하지 않는다는 것을 이해했다.
* [ ] `.parent`로 부모 폴더를 이동할 수 있다.
* [ ] `/` 연산자로 경로를 연결할 수 있다.
* [ ] `.is_dir()`의 역할을 설명할 수 있다.
* [ ] 프로젝트 루트를 위쪽으로 탐색하는 로직을 이해했다.
* [ ] 반복되는 기능을 함수로 만들 수 있다.
* [ ] 공통 함수를 별도의 Python 모듈로 분리하는 이유를 이해했다.
* [ ] `__init__.py`의 역할을 이해했다.
* [ ] `sys.path` 수정 방식의 단점을 이해했다.
* [ ] `pip install -e .`의 목적을 설명할 수 있다.

---

# 오늘의 핵심 한 줄

데이터베이스에서는 요구사항에서 데이터 구조와 관계를 먼저 찾아내는 것이 SQL 작성보다 우선이고,
데이터 분석에서는 프로젝트 구조와 실행 경로를 안정적으로 관리하는 것이 중요하다.

---

## 다음 할 일

* 카페 주문 ERD를 손으로 다시 그려 보기
* customers → orders → order_details 관계 직접 설명해 보기
* CREATE TABLE → INSERT → SELECT 순서로 카페 예제를 다시 실행해 보기
* JOIN과 GROUP BY를 이용해 메뉴별 판매량 집계해 보기
* Python 프로젝트에서 `pyproject.toml`과 editable install 구조 다시 확인하기
* Jupyter Notebook에서 공통 utility 모듈 import 테스트하기
* `Path.cwd()`, `.parent`, `.is_dir()` 직접 실행해 보기

```
```
