2026-09-15 TIL 요구사항 기반 DB 설계와 데이터 분석·머신러닝 전체 흐름

## 오늘의 주제

- 요구사항을 바탕으로 데이터베이스 구조를 설계하는 과정 정리
- EDA에서 질문을 지표와 코드로 바꾸는 방법 이해
- 머신러닝을 위한 전처리, 학습, 평가, 예측 흐름 이해
- 학습한 모델을 실제 애플리케이션에서 사용하는 과정 확인

---

## 데이터베이스

### 1. 요구사항에서 데이터베이스 구조 도출하기

온라인 강의 수강 신청 시스템을 예제로 지금까지 배운 데이터베이스 설계 과정을 종합적으로 적용했다.

중요한 것은 처음부터 테이블을 만드는 것이 아니라 요구사항을 먼저 정확히 이해하는 것이다.

요구사항을 읽으면서 다음을 구분해야 한다.

- 엔터티 → 테이블이 될 대상
- 속성 → 테이블의 컬럼이 될 값
- 관계 → 테이블 사이의 연결 구조

예를 들어 학생, 강사, 강의, 수강 신청처럼 독립적으로 관리해야 하는 대상은 엔터티가 될 수 있고, 신청 상태나 가격 같은 값은 해당 엔터티의 속성이 될 수 있다.

### 2. 필요한 범위까지만 설계하기

요구사항에 없는 기능까지 처음부터 모두 넣으려고 하면 데이터베이스 구조가 불필요하게 복잡해질 수 있다.

예를 들어 온라인 강의 시스템이라고 해서 처음부터 다음 기능까지 모두 넣을 필요는 없다.

- 수강평
- 할인
- 강의 정원
- 추가 결제 정책

먼저 현재 요구사항을 만족하는 기본 구조를 만든 뒤 필요할 때 확장하는 것이 중요하다.

### 3. 테이블 관계와 카디널리티

엔터티를 찾은 뒤에는 각 테이블 사이의 관계를 확인해야 한다.

특히 한 행이 무엇을 의미하는지 명확하게 정하고,

- 1:1
- 1:N
- N:M

중 어떤 관계인지 판단해야 한다.

데이터베이스 설계는 특정 예제의 정답을 외우는 것이 아니라 요구사항이 달라져도 같은 순서로 분석할 수 있어야 한다.

### 4. 설계 이후 검증

구조를 만든 뒤에는 실제 데이터를 넣고 다음 내용을 확인한다.

- CRUD가 정상적으로 동작하는지
- JOIN 결과가 의도한 대로 나오는지
- 날짜와 숫자 등의 경계값이 정상적으로 처리되는지
- 데이터의 의미와 값이 서로 맞는지

이번 실습은 앞으로 다른 비즈니스 요구사항이 주어졌을 때도 동일한 순서로 DB를 설계하기 위한 기본 틀을 연습하는 과정이었다.

---

## 데이터 분석

### 1. EDA는 질문을 구체화하는 과정

EDA는 단순히 데이터를 출력하거나 그래프를 그리는 작업이 아니다.

기본 흐름은 다음과 같다.

```text
데이터 관찰
→ 질문
→ 지표 정의
→ 계산
→ 결과 확인
→ 가설
→ 검증
→ 다음 질문
````

좋은 분석 질문은 실제 계산 가능한 지표로 바꿀 수 있어야 한다.

예를 들어,

```text
어떤 상품이 인기 있는가?
```

라는 질문은 너무 모호하다.

이를 다음과 같이 바꿀 수 있다.

```text
카테고리별 완료 주문 수량은 얼마인가?
카테고리별 완료 주문 금액은 얼마인가?
고객별 완료 주문 횟수는 얼마인가?
```

이렇게 질문이 구체화되면 필요한 데이터와 코드도 자연스럽게 결정된다.

### 2. 분석 질문을 코드로 연결하기

분석하려는 값이 하나의 데이터프레임에 모두 존재하지 않을 수 있다.

예를 들어 카테고리별 완료 주문 금액을 구하려면 다음 정보가 필요하다.

* `products` → category
* `orders` → order_status
* `order_items` → quantity, unit_price

따라서 여러 데이터프레임을 `merge()`한 뒤 필요한 데이터만 필터링하고 `groupby()`로 집계해야 한다.

오늘 다시 느낀 중요한 흐름은 다음과 같다.

```text
질문
→ 필요한 컬럼 확인
→ 컬럼이 있는 데이터프레임 확인
→ merge
→ filtering
→ groupby
→ aggregation
```

### 3. 단변량과 이변량 분석

하나의 컬럼을 확인하는 것은 단변량 분석이다.

범주형 데이터는 주로 `value_counts()`로 분포를 확인한다.

```text
city
gender
category
```

연속형 데이터는 `describe()`를 사용하여 분포를 확인한다.

```text
age
price
quantity
```

평균과 중앙값을 같이 보면 값이 어느 방향으로 몰려 있는지도 생각해 볼 수 있다.

여러 변수를 함께 보기 시작하면 `groupby()`와 집계 함수가 중요해진다.

### 4. 관찰과 인과관계를 구분하기

데이터에서 어떤 패턴이 보였다고 해서 바로 원인을 단정하면 안 된다.

예를 들어,

```text
3월 매출이 2월보다 증가했다.
```

는 데이터에서 확인한 사실이다.

하지만

```text
프로모션 때문에 3월 매출이 증가했다.
```

라고 바로 결론 내릴 수는 없다.

이는 별도의 가설과 통계적 검증이 필요하다.

즉,

```text
관찰 ≠ 인과관계
```

라는 점을 항상 주의해야 한다.

---

## 머신러닝

### 1. 머신러닝용 데이터 준비

타이타닉 데이터를 이용해 머신러닝의 전체 흐름을 다시 확인했다.

원본 데이터에서 필요한 컬럼을 선택하고 다음과 같은 파생 피처도 추가했다.

* `FamilySize`
* `IsAlone`

이후 데이터를 크게 다음으로 나누었다.

```text
X = 문제에 사용할 피처
y = 정답
```

그리고 다시 학습용과 테스트용 데이터로 분리했다.

```text
Train 80%
Test 20%
```

학습 데이터는 모델이 공부하는 데이터이고 테스트 데이터는 학습한 모델의 성능을 확인하는 데이터이다.

### 2. 숫자형과 범주형을 따로 처리하는 이유

머신러닝에 데이터를 넣기 전에는 데이터 타입에 따라 처리 방법이 달라진다.

숫자형 데이터에서는 결측값을 중앙값 등으로 처리할 수 있고,

범주형 데이터는 문자 그대로 모델에 사용할 수 없기 때문에 One-Hot Encoding 등을 통해 숫자로 변환해야 한다.

즉,

```text
숫자형 → 결측 처리 + 스케일링
범주형 → 결측 처리 + 인코딩
```

처럼 각각 다른 전처리가 필요하다.

### 3. 스케일링이 필요한 이유

각 피처의 숫자 범위가 크게 다르면 숫자가 큰 컬럼이 모델에 더 큰 영향을 주는 것처럼 보일 수 있다.

예를 들어,

```text
나이: 20 ~ 80
운임: 0 ~ 500
```

처럼 단위와 범위가 서로 다르다.

이를 비슷한 기준으로 맞춰 주는 작업이 스케일링이다.

대표적인 방식은 다음과 같다.

* MinMaxScaler → 값을 0~1 사이로 변환
* StandardScaler → 평균 0, 표준편차 1을 기준으로 변환

중요한 것은 값의 절대 크기가 아니라 각 피처가 모델에 공정하게 반영될 수 있도록 기준을 맞추는 것이다.

### 4. 모델 학습

이번 실습에서는 분류 모델을 이용해 생존 여부를 예측했다.

대표적으로 사용한 모델은 다음과 같다.

* Logistic Regression
* Random Forest

모델의 `fit()`에는 학습 데이터의 문제와 정답을 전달한다.

```text
X_train = 문제
y_train = 정답

fit(X_train, y_train)
```

즉 기존 알고리즘에 우리의 데이터를 주어 실제 예측에 사용할 모델로 학습시키는 과정이다.

### 5. 정확도만 보면 안 되는 이유

모델의 성능은 단순히 Accuracy 하나만 보고 판단하면 안 된다.

이번에 확인한 주요 평가 지표는 다음과 같다.

* Accuracy
* Precision
* Recall
* F1 Score

그리고 Confusion Matrix를 통해 다음 네 가지 경우를 구분한다.

```text
TP
TN
FP
FN
```

어떤 오류가 더 중요한지는 모델을 사용하는 목적에 따라 달라진다.

예를 들어 암 진단 모델에서는 실제 암 환자를 정상으로 판단하는 경우가 매우 위험하다.

따라서 모델 평가는 단순히 점수가 높은지를 보는 것이 아니라 실제 서비스에서 어떤 오류를 줄여야 하는지도 함께 판단해야 한다.

### 6. 여러 모델을 비교해야 하는 이유

한 모델만 사용하고 결과를 확정하는 것이 아니라 여러 알고리즘을 비교할 수 있다.

이번에는 Logistic Regression과 Random Forest의 Accuracy와 F1 Score 등을 비교했다.

성능이 부족하면 다음과 같은 부분도 다시 확인할 수 있다.

```text
모델 변경
피처 추가/제거
전처리 수정
파라미터 조정
```

머신러닝은 한 번 실행해서 끝나는 작업이 아니라 결과를 보면서 반복적으로 개선하는 과정이라는 점을 이해했다.

### 7. 학습한 모델로 새로운 데이터 예측하기

학습한 모델이 만들어지면 새로운 승객 정보를 입력하여 생존 여부를 예측할 수 있다.

이때 단순히

```text
0 또는 1
```

만 확인하는 것보다 예측 확률도 함께 보는 것이 중요하다.

예를 들어,

```text
예측 결과: 생존
생존 확률: 76%
```

처럼 결과와 확률을 함께 확인할 수 있다.

### 8. 모델을 실제 앱에서 사용하기

학습이 끝난 모델은 파일로 저장한 뒤 다시 불러와 웹 애플리케이션이나 대시보드에서 사용할 수 있다.

이번 실습에서는 모델과 모델의 입력 구조를 파일로 저장하고 Streamlit 앱에서 불러와 새로운 데이터를 입력하면 예측 결과를 보여 주도록 연결했다.

전체 흐름은 다음과 같다.

```text
데이터
→ EDA
→ 전처리
→ 피처 엔지니어링
→ Train/Test 분리
→ 모델 학습
→ 모델 평가
→ 모델 선택
→ 모델 저장
→ 애플리케이션에서 로드
→ 새로운 데이터 예측
```

---

## 오늘 이해한 핵심

데이터베이스와 데이터 분석 모두 결국 바로 코드를 작성하는 것이 아니라 먼저 문제를 정확하게 정의하는 과정이 중요했다.

데이터베이스에서는

```text
요구사항 → 엔터티 → 속성 → 관계 → 테이블 → 검증
```

의 순서가 중요했고,

데이터 분석에서는

```text
질문 → 필요한 데이터 → 지표 → 코드 → 관찰 → 검증
```

의 순서가 중요했다.

머신러닝 역시 단순히 모델을 실행하는 것이 아니라

```text
데이터를 제대로 준비하고
→ 학습시키고
→ 여러 지표로 평가하고
→ 실제 사용할 수 있는 형태로 저장하는 것
```

까지가 하나의 전체 과정이라는 것을 이해했다.

특히 오늘은 개별 코드보다 데이터가 실제 예측 모델과 애플리케이션까지 어떻게 이어지는지 전체 흐름을 연결해서 볼 수 있었다.

```

이번 버전은 일부러 **강의 내용 전체를 받아쓰기하듯 넣지 않고**, 나중에 네가 GitHub TIL을 다시 봤을 때 **“아 이날 DB 설계 흐름이랑 EDA·머신러닝 전체 사이클을 배웠지”**가 바로 떠오르게 줄였어.
```
