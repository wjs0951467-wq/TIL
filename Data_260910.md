# 2026-09-10 TIL 요구사항 기반 데이터 모델링과 신뢰할 수 있는 데이터 전처리

## 오늘 배운 것

오늘은 데이터베이스와 데이터 분석에서 각각 데이터를 다루기 전에 무엇을 확인하고 준비해야 하는지 배웠다.

데이터베이스에서는 **요구사항을 읽고 엔터티, 속성, 사건을 찾아 테이블과 관계로 설계하는 과정**을 학습했다.

데이터 분석에서는 원본 데이터가 항상 깨끗한 것은 아니기 때문에 **데이터를 분석하기 전에 문자열, 자료형, 결측값, 중복값, 이상값, 관계 등을 점검하고 정리하는 전처리 과정**을 학습했다.

두 영역 모두 공통적으로 중요한 점은 데이터를 바로 사용하지 않고 먼저 **구조와 상태를 확인하고 검증한 뒤 사용하는 것**이다.

---

# 데이터베이스

## 1. 요구사항에서 데이터 모델로 변환하기

데이터베이스 설계는 단순히 테이블을 만드는 것부터 시작하는 것이 아니다.

먼저 업무의 요구사항을 읽고 다음 내용을 구분해야 한다.

* 관리해야 하는 대상
* 대상이 가지는 속성
* 반복해서 발생하는 사건
* 업무 규칙
* 아직 결정되지 않은 정책

이 내용을 토대로 테이블과 컬럼, 관계를 설계한다.

예를 들어 도서관 서비스의 요구사항에 다음 내용이 있다고 가정한다.

* 회원이 있다.
* 도서가 있다.
* 회원은 도서를 대출할 수 있다.
* 한 회원은 여러 번 대출할 수 있다.
* 하나의 도서도 시간에 따라 여러 번 대출될 수 있다.

여기에서 관리 대상은 다음과 같이 찾을 수 있다.

```text
회원
도서
대출
```

결과적으로 다음과 같은 테이블 후보가 나온다.

```text
members
books
loans
```

---

## 2. 엔터티와 속성 구분하기

요구사항에서 등장하는 명사가 모두 테이블이 되는 것은 아니다.

일부는 엔터티가 되고 일부는 엔터티가 가지는 속성이 된다.

예를 들어 다음과 같이 구분할 수 있다.

```text
회원 → 엔터티
도서 → 엔터티
대출 기록 → 엔터티

이메일 → 회원의 속성
ISBN → 도서의 속성
반납 예정일 → 대출의 속성
```

데이터베이스에서는 이를 다음과 같이 연결해서 이해할 수 있다.

```text
엔터티 → 테이블
속성 → 컬럼
사건 → 레코드가 반복해서 생성되는 원인
```

예를 들어 회원 한 명이 새로 가입하면 `members` 테이블에 새로운 행이 하나 추가된다.

도서 한 권이 대출되면 `loans` 테이블에 새로운 행이 하나 추가된다.

---

## 3. 한 행의 의미를 이해하기

테이블을 설계할 때 매우 중요한 질문은 다음과 같다.

```text
이 테이블의 한 행은 무엇을 의미하는가?
```

예를 들어 다음과 같다.

```text
members 한 행
→ 회원 한 명

books 한 행
→ 도서 한 권

loans 한 행
→ 한 회원이 한 도서를 대출한 한 번의 기록
```

한 행의 의미가 명확해야 테이블의 역할도 명확해진다.

특히 사건을 기록하는 테이블에서는 시간이 지날수록 새로운 레코드가 계속 추가된다.

---

## 4. 스냅샷이 아니라 Long-term View로 관계 보기

관계를 판단할 때 현재 순간만 보면 잘못 판단할 수 있다.

예를 들어 도서 한 권은 현재 한 사람에게만 대출될 수 있다.

현재 모습만 보면 다음처럼 생각할 수 있다.

```text
도서 1권 → 회원 1명
```

하지만 데이터베이스에는 과거 기록까지 계속 쌓인다.

```text
1월 → 회원 A 대출
2월 → 회원 B 대출
3월 → 회원 C 대출
```

따라서 장기적으로 보면 하나의 도서는 여러 번 대출될 수 있다.

마찬가지로 회원 한 명도 여러 권의 책을 대출할 수 있다.

결국 회원과 도서의 관계는 다음과 같다.

```text
회원 N : M 도서
```

관계를 분석할 때는 현재 상태만 보는 **Snapshot View**가 아니라 시간의 흐름을 포함하는 **Long-term View**로 생각해야 한다.

---

## 5. N:M 관계와 중간 테이블

관계형 데이터베이스에서는 N:M 관계를 그대로 표현하지 않고 중간 테이블을 사용한다.

회원과 도서를 예로 들면 다음과 같다.

```text
회원 N : M 도서
```

이 사이에서 실제로 발생하는 사건은 `대출`이다.

따라서 다음 구조로 분리한다.

```text
members 1 : N loans
books   1 : N loans
```

즉,

```text
members
    |
    | 1:N
    |
loans
    |
    | N:1
    |
books
```

`loans`가 회원과 도서 사이에서 발생한 대출 사건을 저장하는 중간 테이블이 된다.

---

## 6. PK와 FK

각 테이블은 하나의 행을 구분할 수 있는 기본키가 필요하다.

예를 들어 다음과 같다.

```text
members
- id (PK)

books
- id (PK)

loans
- id (PK)
- member_id (FK)
- book_id (FK)
```

`members.id`와 `books.id`는 각각 자신의 테이블에서 기본키다.

`loans`에서는 이 값들을 참조해야 하므로 다음과 같이 외래키가 된다.

```text
loans.member_id → members.id
loans.book_id   → books.id
```

정리하면 다음과 같다.

```text
PK = 테이블의 한 행을 식별하는 키

FK = 다른 테이블의 PK를 참조하는 키
```

---

## 7. 자동 증가 ID와 업무상 고유값

회원의 이메일이나 학번처럼 실제 업무에서도 고유한 값이 있을 수 있다.

하지만 실습에서는 별도의 자동 증가 `id`를 PK로 사용했다.

```text
id
1
2
3
...
```

PostgreSQL에서는 Identity 등을 이용해 자동 증가 값을 만들 수 있다.

이메일, 학번 등은 PK가 아니라 업무상 고유값으로 관리할 수 있다.

```text
id      → PK
email   → UNIQUE
```

---

## 8. 정책은 테이블을 만들기 전에 결정해야 한다

테이블을 만든 뒤에 데이터 입력 규칙을 결정하는 것이 아니라, 가능한 한 설계 단계에서 결정해야 한다.

예를 들어 ISBN을 반드시 입력해야 하는지 확인해야 한다.

```text
ISBN 필수
→ NOT NULL

ISBN 없어도 됨
→ NULL 허용
```

이 결정 없이 먼저 테이블을 만들면 나중에 예상하지 못한 결측 데이터가 쌓일 수 있다.

따라서 다음과 같은 정책을 먼저 검토한다.

```text
필수값인가?
중복이 가능한가?
NULL을 허용하는가?
삭제가 가능한가?
어떤 상태값을 사용하는가?
```

데이터 품질은 데이터를 입력한 뒤에만 관리하는 것이 아니라 **설계 단계에서부터 시작된다.**

---

## 9. DBeaver와 데이터베이스 연결

SQL 파일을 DBeaver에서 열었다고 해서 자동으로 데이터베이스에 연결되는 것은 아니다.

SQL 파일을 실행하려면 해당 SQL Editor에 **Active Connection**이 필요하다.

예를 들어 다음 쿼리를 실행한다.

```sql
SELECT current_database();
SELECT current_user;
SELECT current_schema();
```

여기서 확인할 수 있는 것은 다음과 같다.

```text
current_database()
→ 현재 연결한 데이터베이스

current_user
→ 현재 접속 사용자

current_schema()
→ 현재 작업 중인 스키마
```

SQL 파일 상단이 `N/A` 또는 연결되지 않은 상태라면 데이터 소스를 선택하여 연결해야 한다.

---

## 10. 테이블 생성 순서와 외래키

도서관 예제에서는 다음과 같은 테이블을 생성했다.

```text
members
books
loans
```

`loans`는 `members`, `books`를 참조한다.

따라서 부모 데이터가 없는 상태에서 대출 데이터부터 넣으면 외래키 제약조건 오류가 발생한다.

잘못된 순서:

```text
loans INSERT
↓
members INSERT
books INSERT
```

올바른 순서:

```text
members INSERT
books INSERT
↓
loans INSERT
```

회원과 도서가 존재해야 대출이라는 사건이 발생할 수 있기 때문이다.

이것이 FK 제약조건을 사용하는 이유 중 하나다.

---

## 11. Seed 데이터

Seed 데이터는 실습이나 프로그램의 초기 상태를 구성하기 위해 미리 넣어두는 기본 데이터다.

예를 들어 다음 데이터를 먼저 생성할 수 있다.

```text
members 3건
books 3건
loans 4건
```

이 데이터를 이용해 조회와 관계를 검증한다.

---

## 12. SELECT를 이용한 데이터 확인

전체 회원을 확인하려면 다음과 같이 조회할 수 있다.

```sql
SELECT *
FROM members;
```

데이터가 적을 때는 직접 확인할 수 있지만 데이터가 많으면 `COUNT()`를 사용해야 한다.

```sql
SELECT COUNT(*)
FROM members;
```

특정 회원의 대출 기록을 조회한다면 다음과 같이 조건을 사용할 수 있다.

```sql
SELECT *
FROM loans
WHERE member_id = 101;
```

여기서 중요한 것은 `loans` 테이블에는 회원 이름이 아니라 `member_id`가 저장되어 있다는 점이다.

---

## 13. JOIN이 필요한 이유

대출 테이블에는 다음과 같은 정보가 있다.

```text
member_id
book_id
borrowed_at
due_date
returned_at
```

하지만 다음 정보는 없다.

```text
회원 이름
책 제목
```

회원 이름은 `members`에 있고 책 제목은 `books`에 있다.

따라서 세 테이블의 정보를 함께 확인하려면 JOIN이 필요하다.

개념적으로는 다음과 같다.

```text
loans.member_id = members.id
loans.book_id = books.id
```

테이블 별칭을 사용하면 다음과 같이 표현할 수 있다.

```text
loans   → l
members → m
books   → b
```

그리고 다음처럼 각 테이블의 컬럼을 구분할 수 있다.

```text
l.id
m.name
b.title
```

이번 수업에서는 JOIN을 본격적으로 학습하기보다는 **왜 필요한지와 어떤 키를 이용해 연결하는지**를 먼저 확인했다.

---

# 데이터 분석

## 1. 데이터 분석의 전체 흐름

데이터 분석은 단순히 CSV 파일을 열어보는 것이 아니다.

오늘 수업에서 다룬 전체적인 흐름은 다음과 같다.

```text
과거 데이터
    ↓
데이터 로드
    ↓
데이터 전처리
    ↓
탐색적 데이터 분석(EDA)
    ↓
시각화
    ↓
통계 분석
    ↓
머신러닝
    ↓
미래 예측
```

데이터 분석에서는 과거 데이터를 이용해서 현재 상태를 이해하고 미래의 결과를 예측할 수 있다.

예를 들어 다음과 같다.

```text
과거 공부 패턴
→ 다음 시험 점수 예측

타이타닉 승객 데이터
→ 생존 여부 예측

고객 구매 패턴
→ VIP 고객이 될 가능성 예측
```

---

## 2. 회귀와 분류

예측 문제는 크게 숫자를 예측하는 문제와 범주를 예측하는 문제로 생각할 수 있다.

### 회귀

연속적인 숫자 값을 예측한다.

```text
공부 시간과 과거 성적
→ 다음 시험 점수 예측
```

### 분류

어떤 범주에 속하는지를 예측한다.

```text
타이타닉 승객 정보
→ 생존 / 사망

고객 구매 패턴
→ VIP / 일반 고객
```

---

## 3. 전처리가 중요한 이유

현실의 데이터는 처음부터 깨끗하게 들어오지 않는다.

다음과 같은 문제가 있을 수 있다.

```text
문자열 앞뒤 공백
잘못된 데이터 타입
결측값
중복값
서로 다른 표기 방식
이상값
깨진 PK-FK 관계
```

이 상태에서 바로 분석하면 분석 결과도 신뢰하기 어렵다.

따라서 머신러닝 모델의 성능 이전에 **입력 데이터의 품질**이 매우 중요하다.

오늘 수업에서 특히 강조된 내용은 다음과 같다.

```text
전처리가 잘되어야
→ EDA가 의미가 있고
→ 시각화가 정확해지고
→ 머신러닝 예측 결과도 신뢰할 수 있다.
```

---

## 4. 전처리 기본 순서

수업에서 반복해서 강조한 전처리 흐름은 다음과 같다.

```text
1. 데이터 불러오기
2. 원본 확인
3. 복사본 생성
4. 결측값 확인
5. 중복값 확인
6. 문자열 정리
7. 데이터 타입 변환
8. 범주 표준화
9. 이상값 후보 확인
10. 파생 컬럼 생성
11. PK-FK 관계 검증
12. 전처리 결과 검증
13. Cleaned CSV 저장
```

모든 데이터에서 무조건 똑같이 처리한다는 의미가 아니라, **이 항목들을 순서대로 점검하는 습관을 가지는 것**이 중요하다.

---

## 5. 원본 데이터는 보존한다

전처리하면서 원본 데이터를 직접 수정하면 잘못된 작업이 발생했을 때 되돌리기 어렵다.

따라서 원본 데이터는 그대로 보존하고 복사본으로 작업한다.

```python
df_clean = df.copy()
```

폴더도 다음과 같이 구분할 수 있다.

```text
data/
├── raw/
└── processed/
```

역할은 다음과 같다.

```text
raw
→ 원본 데이터

processed
→ 전처리가 끝난 데이터
```

---

## 6. CSV를 DataFrame으로 불러오기

CSV 파일은 pandas의 `read_csv()`를 사용해 DataFrame으로 읽는다.

```python
pd.read_csv(...)
```

수업에서는 여러 CSV를 불러와 다음과 같은 DataFrame을 생성했다.

```text
customers
products
orders
order_items
```

DataFrame은 행과 열로 이루어진 테이블 형태의 데이터 구조다.

---

## 7. shape로 데이터 크기 확인

DataFrame의 크기는 `shape`를 이용해 확인할 수 있다.

예를 들어 다음과 같은 결과가 있다.

```text
(150, 6)
```

의미는 다음과 같다.

```text
150 → 행의 개수
6   → 컬럼의 개수
```

즉,

```text
shape = (rows, columns)
```

전처리 전후의 행과 컬럼 수가 예상대로 유지되었는지 확인하는 데도 사용할 수 있다.

---

## 8. 여러 DataFrame을 딕셔너리로 관리하기

여러 DataFrame을 각각 처리하는 대신 딕셔너리에 넣어 관리할 수 있다.

개념적으로 다음 구조다.

```text
{
    "customers": customers_df,
    "products": products_df,
    "orders": orders_df,
    "order_items": order_items_df
}
```

딕셔너리의

```text
Key   → 데이터셋 이름
Value → DataFrame
```

이 된다.

이렇게 하면 반복문으로 여러 DataFrame에 동일한 검사를 수행할 수 있다.

```text
결측값 확인
중복값 확인
shape 확인
타입 확인
```

데이터셋이 하나 추가되더라도 딕셔너리에 추가하면 동일한 검사를 반복할 수 있어 자동화하기 쉽다.

---

## 9. 데이터 타입 확인

전처리에서는 값이 어떻게 보이는지만 확인하면 안 된다.

실제 데이터 타입을 확인해야 한다.

전체적인 구조를 확인할 때는 다음과 같은 방법을 사용한다.

```python
df.info()
```

타입을 직접 확인할 때는 다음을 사용할 수 있다.

```python
df.dtypes
```

예를 들어 가격 데이터가 화면에는 다음처럼 보일 수 있다.

```text
10000
20000
30000
```

하지만 실제 타입은 문자열일 수도 있다.

```text
object / string
```

숫자 계산을 하려면 숫자형으로 변환해야 한다.

---

## 10. 문자열 앞뒤 공백 제거

문자열 데이터에는 눈에 보이지 않는 공백이 들어갈 수 있다.

예를 들어 다음 두 값은 사람이 보기에는 비슷하지만 데이터에서는 다른 값이다.

```text
"홍길동"
"홍길동 "
```

따라서 문자열의 양쪽 끝 공백을 제거해야 한다.

이때 사용하는 대표적인 문자열 메서드는 다음과 같다.

```python
.str.strip()
```

가운데 공백까지 없애면 안 된다.

```text
"Bill Gates"
```

처럼 실제 데이터의 일부일 수 있기 때문이다.

따라서 제거 대상은 **문자열의 앞과 뒤에 불필요하게 붙은 공백**이다.

---

## 11. NULL과 빈 문자열은 다르다

다음 값들은 서로 다른 의미를 가진다.

```text
NaN
""
" "
```

### NaN

결측값이다.

### ""

빈 문자열이다.

문자열 타입이지만 문자가 하나도 없다.

### " "

공백 문자가 들어 있는 문자열이다.

따라서 문자열을 정리할 때는 단순히 NULL만 검사하는 것이 아니라 빈 문자열과 공백도 함께 확인해야 한다.

---

## 12. 결측값 확인

결측값을 찾을 때 대표적으로 다음 개념을 사용한다.

```python
isna()
```

컬럼별 결측값 개수를 확인하면 어떤 컬럼을 처리해야 하는지 판단할 수 있다.

전처리가 끝난 데이터에서는 가능한 한 **의도하지 않은 결측값이 남아 있지 않도록 검증**해야 한다.

---

## 13. 결측값 처리 방법

결측값이 있다고 무조건 삭제하면 안 된다.

크게 다음 두 방향을 생각할 수 있다.

```text
1. 해당 행 삭제
2. 다른 값으로 대체
```

### 삭제

데이터를 제거한다.

하지만 데이터는 비용을 들여 수집한 중요한 자원이기 때문에 무조건 삭제해서는 안 된다.

특히 데이터 수가 적다면 한 행을 삭제하는 영향이 매우 커질 수 있다.

따라서 삭제는 충분히 검토한 뒤 사용해야 한다.

### 대체

결측값을 다른 값으로 채운다.

숫자형 데이터에서는 다음과 같은 값을 고려할 수 있다.

```text
평균값
중앙값
```

범주형 데이터에서는 다음을 고려할 수 있다.

```text
최빈값
Unknown
기타
```

어떤 값을 사용해야 하는지는 데이터의 의미와 업무 요구사항에 따라 결정해야 한다.

---

## 14. 중앙값으로 결측값 채우기

예를 들어 나이 데이터의 결측값을 중앙값으로 채울 수 있다.

수업에서 중요한 개념으로 등장한 것은 다음이다.

```text
median
→ 중앙값

fillna
→ 결
```
