#2026-09-21 TIL 작은 데이터 분석 프로젝트와 시각화

## 오늘 배운 것

오늘은 여러 CSV 파일을 불러온 뒤 전처리, 검증, 병합, 집계, 시각화까지 이어지는 작은 데이터 분석 프로젝트의 전체 흐름을 복습했다.

단순히 차트를 그리는 것이 목적이 아니라, 먼저 **분석 질문을 정하고 데이터가 믿을 만한지 검증한 뒤 결과를 해석하는 과정**이 중요하다는 것을 배웠다.

## 핵심 개념

### 1. 데이터 분석의 전체 흐름

1. CSV 파일 불러오기
2. 데이터 구조와 자료형 확인하기
3. 결측치·중복값·PK·FK 검증하기
4. 필요한 데이터끼리 병합하기
5. 분석 질문에 맞게 필터링하고 집계하기
6. 적절한 차트로 시각화하기
7. 결과를 해석하고 인사이트 정리하기

분석은 데이터를 무작정 살펴보는 것이 아니라, **무엇을 알고 싶은지 질문을 정하는 것**에서 시작한다.

### 2. 여러 데이터프레임 관리하기

`customers`, `orders`, `order_items`, `products`처럼 여러 데이터프레임을 함께 다룰 때는 딕셔너리로 묶어 관리할 수 있다.

리스트는 데이터의 위치를 기억해야 하지만, 딕셔너리는 데이터 이름을 Key로 사용할 수 있어 더 직관적이다.

`items()`와 반복문을 사용하면 여러 데이터프레임을 같은 기준으로 한 번에 점검할 수도 있다.

### 3. 병합 전후 검증하기

데이터를 병합하기 전에는 다음 내용을 확인해야 한다.

- PK에 결측치가 있는가?
- PK에 중복값이 있는가?
- FK가 존재하지 않는 PK를 가리키고 있는가?
- 병합 전후의 행 수가 예상과 일치하는가?
- 분석에 필요한 컬럼이 포함되어 있는가?

검증하지 않고 병합하면 데이터가 사라지거나 행이 예상보다 늘어날 수 있다.

`order_items`는 주문과 상품을 연결하는 중심 데이터이므로, 이를 기준으로 `orders`, `products`, `customers`를 차례로 병합할 수 있다.

병합 후에는 `_merge`와 행 수를 확인해 데이터가 올바르게 연결되었는지 검증한다.

### 4. 파생 컬럼 만들기

기존 컬럼을 계산해 새로운 의미를 가진 파생 컬럼을 만들 수 있다.

```python
order_items["line_total"] = (
    order_items["quantity"] * order_items["unit_price"]
)
```

`line_total`은 상품의 수량과 단가를 곱한 주문 상세 금액이다.

파생 컬럼을 만든 뒤에는 다음 내용을 확인해야 한다.

- 계산 결과가 맞는가?
- 자료형이 숫자형인가?
- 결측치가 발생하지 않았는가?

코드가 오류 없이 실행됐다고 해서 계산 결과까지 반드시 정확한 것은 아니므로 검산이 필요하다.

### 5. 필요한 데이터만 필터링하기

주문 상태가 `completed`인 데이터만 분석하려면 불리언 마스크나 `eq()`를 사용할 수 있다.

```python
completed_orders = orders[
    orders["order_status"].eq("completed")
].copy()
```

필터링한 뒤에는 `value_counts()`나 행 수 비교를 통해 완료된 주문만 남았는지 다시 확인한다.

### 6. 집계 기준 구분하기

주문 수와 주문 상세 행 수는 서로 다르다.

한 번의 주문에 여러 상품이 포함될 수 있기 때문이다.

따라서 집계할 때는 구하려는 값의 기준을 명확하게 구분해야 한다.

- 판매 수량: `quantity`의 합계
- 판매 금액: `line_total`의 합계
- 주문 수: `order_id`의 고유값 개수

월별, 카테고리별, 상품별처럼 분석 기준을 정한 뒤 `groupby()`와 `agg()`를 사용해 요약 데이터를 만든다.

### 7. 질문에 맞는 차트 선택하기

| 확인할 내용 | 적합한 차트 |
| --- | --- |
| 카테고리·상품 간 크기 비교 | 막대그래프 |
| 월별 변화와 추세 | 선그래프 |
| 금액의 분포 | 히스토그램 |
| 두 변수의 관계 | 산점도 |
| 전체에서 차지하는 비율 | 원그래프 |
| 분포와 이상치 후보 | 박스플롯 |

원그래프는 비율이 비슷할 때 차이를 비교하기 어렵다.

정확한 크기 비교가 필요하다면 막대그래프가 더 직관적일 수 있다.

상품이 많다면 모든 상품을 표시하지 않고 매출 상위 10개처럼 범위를 줄이는 것이 가독성에 도움이 된다.

### 8. 박스플롯과 이상치 후보

박스플롯은 데이터의 중앙값, 사분위수, 전체적인 분포와 이상치 후보를 보여 준다.

- Q1: 하위 25% 지점
- 중앙값: 50% 지점
- Q3: 하위 75% 지점
- IQR: `Q3 - Q1`
- 아래 경계: `Q1 - 1.5 × IQR`
- 위 경계: `Q3 + 1.5 × IQR`

경계 밖에 있는 값은 이상치일 가능성이 있다.

하지만 차트만 보고 바로 삭제하면 안 된다. 실제 데이터와 데이터가 만들어진 과정을 확인한 뒤 오류인지 의미 있는 값인지 판단해야 한다.

## 가장 중요하게 느낀 것

차트를 예쁘게 그리는 것보다 **왜 이 차트를 선택했고, 차트에서 무엇을 읽을 수 있는지 설명하는 능력**이 더 중요하다.

또한 계산과 병합은 코드가 실행되었다고 끝난 것이 아니다.

행 수, 자료형, 결측치, 중복값과 계산 결과를 다시 확인해야 분석 결과를 신뢰할 수 있다.

## 실습·복습 체크리스트

- [ ] 네 개의 CSV 파일을 상대경로로 불러오기
- [ ] 각 데이터프레임의 `shape`, `head()`, `info()` 확인하기
- [ ] PK의 결측치와 중복값 확인하기
- [ ] FK에 연결되지 않는 값이 있는지 확인하기
- [ ] 병합 전후 행 수와 `_merge` 결과 비교하기
- [ ] `line_total` 파생 컬럼을 만들고 계산 결과 검증하기
- [ ] 완료된 주문만 필터링한 뒤 건수 검산하기
- [ ] 월별·카테고리별·상품별 집계 결과 만들기
- [ ] 분석 질문에 맞는 차트를 선택하기
- [ ] 차트 결과를 한 문장으로 해석하기
- [ ] 박스플롯에서 이상치 후보를 찾고 실제 데이터 확인하기

## 오늘의 핵심 한 줄

데이터 분석은 차트를 그리는 작업이 아니라, 질문을 세우고 데이터를 검증한 뒤 결과를 근거 있게 해석하는 과정이다.
