# 2026-09-14 TIL 타이타닉 데이터를 활용한 데이터 분석 전체 흐름과 탐색적 데이터 분석(EDA)

## 오늘 배운 것

오늘은 타이타닉 데이터를 이용해 데이터 분석의 전체 흐름을 살펴보고, 실제 데이터를 불러온 뒤 관찰·전처리·문제 정의·시각화까지 진행했다.

데이터 분석은 단순히 코드를 실행하는 것이 아니라 데이터를 이해하고, 결과를 해석하고, 다음 분석 방향을 직접 판단하는 과정이 중요하다.

전체 흐름은 다음과 같다.

```text
과거 데이터
→ 데이터 로딩
→ 데이터 이해 및 전처리
→ 탐색적 데이터 분석(EDA)
→ 문제 정의
→ Feature / Target 결정
→ 머신러닝
→ 미래 데이터 예측
→ 대시보드 및 결과 활용
```

---

## 핵심 개념

### 1. 데이터 분석의 전체 흐름

데이터를 받았다고 바로 머신러닝을 시작하는 것이 아니다.

먼저 데이터를 불러온 뒤 어떤 데이터인지 확인하고, 필요한 전처리를 수행한 다음 시각화와 EDA를 통해 데이터의 특징과 관계를 파악한다.

이후 어떤 문제를 해결할 것인지 정의하고, 머신러닝에 사용할 Feature와 Target을 정한다.

이번 수업에서는 이 전체 흐름 중 머신러닝 직전 단계까지 한 번에 살펴보았다.

또한 Jupyter Notebook에서는 코드만 작성하지 않고 다음과 같은 흐름으로 기록하는 것이 중요하다.

```text
Markdown
→ 무엇을 확인할 것인지 작성

Code
→ 실제 코드 실행

Markdown
→ 실행 결과와 해석 작성
→ 다음에 확인할 내용 기록
```

즉, 분석 과정과 판단 근거를 함께 남기는 것이 중요하다.

---

### 2. 데이터 확인과 전처리

데이터를 불러온 뒤에는 먼저 전체 구조를 확인한다.

```python
df.shape
df.columns
df.head()
df.info()
df.describe()
```

* `shape`: 행과 열의 개수 확인
* `columns`: 컬럼 이름 확인
* `head()`: 실제 데이터 앞부분 확인
* `info()`: 자료형과 결측치 확인
* `describe()`: 평균, 최소값, 최대값, 사분위수 등의 기초 통계 확인

이 과정에서 단순히 자료형만 보는 것이 아니라 컬럼이 실제로 어떤 의미를 가지는지 함께 확인해야 한다.

예를 들어 `Survived`는 `0`, `1`이라는 숫자로 저장되어 있지만 실제 의미는 사망과 생존이라는 범주이다.

`Pclass` 역시 `1`, `2`, `3`이라는 숫자이지만 실제로는 1등석, 2등석, 3등석이라는 범주형 데이터이다.

따라서 다음을 구분해야 한다.

```text
저장된 자료형
≠
실제 데이터의 의미
```

결측치도 중요한 확인 대상이다.

타이타닉 데이터에서는 대표적으로 `Age`, `Cabin`, `Embarked`에 결측치가 존재했다.

결측치를 발견했다고 무조건 삭제하거나 값을 채우면 안 된다.

```text
결측치의 개수는 얼마나 되는가?
컬럼은 어떤 의미를 가지는가?
분석에 필요한 컬럼인가?
어떤 방법으로 처리하는 것이 적절한가?
```

를 먼저 판단해야 한다.

예를 들어 탐색 과정에서는 `Age`를 중앙값 등으로 대체하고, `Embarked`는 최빈값을 활용하는 방법을 살펴보았다. `Cabin`은 결측치가 매우 많아 활용 여부를 추가로 판단해야 했다.

또한 원본 데이터를 직접 계속 수정하지 않고 작업용 데이터프레임을 따로 만드는 것이 좋다.

```python
df_work = df.copy()
```

```text
df
→ 원본 데이터

df_work
→ 전처리와 EDA를 진행할 작업용 데이터
```

중복 데이터 역시 확인해야 한다.

```python
df.duplicated().sum()
```

다만 이 코드는 행 전체가 동일한 중복을 확인하는 것이므로 특정 ID의 중복과는 의미가 다르다.

```text
전체 행 중복
≠
특정 PK 중복
```

**중요:** 전처리는 기계적으로 처리하는 작업이 아니라 데이터의 의미와 상황을 보고 판단하는 과정이다.

---

### 3. 문제 정의와 Feature / Target

데이터를 어느 정도 이해한 뒤에는 어떤 문제를 해결할 것인지 정해야 한다.

이번 타이타닉 데이터에서는 다음과 같이 문제를 정의했다.

```text
과거 승객의 정보를 이용하여
새로운 승객의 생존 여부를 예측할 수 있을까?
```

이때 머신러닝이 맞혀야 하는 정답을 **Target**이라고 한다.

타이타닉 데이터에서는 다음 값이 Target이다.

```text
Survived

0 = 사망
1 = 생존
```

정답이 두 가지 범주로 나뉘기 때문에 이번 문제는 **이진 분류(Binary Classification)** 문제이다.

```text
생존 / 사망
합격 / 불합격
구매 / 미구매

→ 분류
```

반대로 다음과 같이 숫자 값을 예측하는 문제는 회귀 문제이다.

```text
시험 점수
가격
매출액
체중

→ 회귀
```

Target을 예측하기 위해 사용하는 입력 정보를 **Feature**라고 한다.

타이타닉에서는 다음과 같은 컬럼이 Feature 후보가 될 수 있다.

```text
성별
나이
객실 등급
티켓 가격
탑승지
```

즉 다음과 같은 관계를 확인하는 것이다.

```text
성별 ─────┐
나이 ─────┤
객실등급 ─┤
티켓가격 ─┤ → 생존 여부
탑승지 ───┘

 Feature       Target
```

모든 컬럼을 머신러닝에 넣는 것이 좋은 것은 아니다.

Target과 관계가 없거나 동일한 의미가 반복되는 컬럼이 많아지면 모델이 불필요하게 복잡해질 수 있다.

따라서 컬럼을 다음과 같이 판단할 수 있다.

```text
Keep
→ 그대로 사용할 컬럼

Exclude
→ 모델 입력에서 제외할 컬럼

Derive
→ 새로운 파생 Feature를 만드는 재료

Defer
→ 지금은 판단을 보류할 컬럼
```

이처럼 Feature를 선택하거나 새롭게 만드는 과정을 **Feature Engineering**이라고 한다.

또 하나 중요한 개념은 **데이터 누수(Data Leakage)**이다.

머신러닝에서는 실제 예측 시점에 알 수 없는 정보를 Feature로 사용하면 안 된다.

예를 들어 생존 여부 자체를 이용해 생존 여부를 예측하면 정답을 미리 알려주고 시험을 보는 것과 같다.

Feature를 정할 때는 다음 질문이 중요하다.

```text
이 정보는 실제 예측 시점에도 알 수 있는 정보인가?
```

---

### 4. 범주형 데이터와 Encoding

머신러닝에서는 입력 데이터를 숫자로 처리해야 한다.

따라서 문자열 형태의 범주형 데이터는 숫자로 변환해야 한다.

이 과정을 **Encoding**이라고 한다.

예를 들어 성별은 다음과 같이 매핑할 수 있다.

```text
male   → 0
female → 1
```

또 다른 방법으로 **One-Hot Encoding**을 사용할 수 있다.

예를 들어 `Embarked` 값이 `S`, `C`, `Q`라면 다음처럼 각각의 컬럼으로 나눌 수 있다.

```text
        S  C  Q

S       1  0  0
C       0  1  0
Q       0  0  1
```

Pandas에서는 다음 기능을 사용할 수 있다.

```python
pd.get_dummies()
```

One-Hot Encoding에서는 범주의 개수만큼 컬럼이 늘어나므로 경우에 따라 기준이 되는 컬럼 하나를 제외할 수도 있다.

또한 `Pclass`처럼 숫자로 저장되어 있어도 실제 의미가 범주라면 그대로 연속형 숫자로 생각하지 않고 범주형 데이터로 판단해야 한다.

---

### 5. EDA와 시각화

EDA는 **Exploratory Data Analysis**, 즉 탐색적 데이터 분석이다.

데이터를 여러 방향으로 살펴보면서 패턴과 관계를 발견하는 과정이다.

이번 수업에서는 다음과 같은 내용을 확인했다.

```text
전체 생존자와 사망자 수

성별에 따른 생존율

객실 등급에 따른 생존율

나이와 생존 여부

티켓 가격과 생존 여부
```

시각화에는 Matplotlib과 Seaborn을 사용할 수 있다.

성별이나 객실 등급별 생존율을 확인할 때는 `groupby()`를 이용해 그룹별로 집계할 수 있다.

`Survived`가 `0`, `1`로 구성되어 있기 때문에 평균을 계산하면 해당 그룹의 생존 비율로 해석할 수 있다.

나이와 같은 연속형 데이터의 분포를 확인할 때는 Histogram을 사용할 수 있다.

티켓 가격과 생존 여부처럼 데이터의 분포와 중앙값, 이상치 후보를 비교할 때는 Box Plot을 사용할 수 있다.

EDA를 통해 다음과 같은 관계가 있어 보인다는 것을 확인할 수 있었다.

```text
성별에 따라 생존율의 차이가 있어 보인다.

객실 등급에 따라 생존율의 차이가 있어 보인다.

티켓 가격과 생존 여부도 어느 정도 관계가 있어 보인다.
```

하지만 그래프에서 관계가 보인다고 바로 원인이라고 판단하면 안 된다.

예를 들어 여름에는 아이스크림 판매량과 익사 사고가 모두 증가할 수 있다.

```text
여름
├─ 아이스크림 판매량 증가
└─ 익사 사고 증가
```

두 값이 함께 증가한다고 해서 아이스크림 판매가 익사 사고의 원인은 아니다.

두 현상 모두 `여름`이라는 다른 요인의 영향을 받은 것이다.

따라서 다음을 구분해야 한다.

```text
상관관계
→ 두 값이 함께 변하는 관계

인과관계
→ 하나의 변화가 다른 변화의 원인이 되는 관계
```

**중요:** 상관관계가 있다고 해서 반드시 인과관계가 있는 것은 아니다.

---

## 중요 포인트

1. 데이터 분석에서는 개별 코드보다 전체 분석 흐름을 이해하는 것이 중요하다.
2. 데이터를 불러온 뒤에는 구조, 자료형, 결측치, 중복, 기초 통계를 먼저 확인한다.
3. 숫자로 저장되어 있어도 실제 의미는 범주형일 수 있다.
4. 결측치는 무조건 삭제하거나 대체하지 않고 데이터의 의미를 보고 처리한다.
5. 원본 데이터는 보존하고 작업용 데이터프레임을 따로 만드는 것이 좋다.
6. Target은 예측해야 하는 정답이고 Feature는 예측에 사용하는 입력 정보이다.
7. 모든 컬럼을 머신러닝에 사용하는 것이 좋은 것은 아니다.
8. 실제 예측 시점에 알 수 없는 정보를 사용하면 데이터 누수가 발생할 수 있다.
9. 범주형 데이터는 머신러닝을 위해 Encoding이 필요하다.
10. EDA는 데이터를 탐색하면서 의미 있는 패턴과 분석 방향을 찾는 과정이다.
11. 상관관계와 인과관계를 구분해야 한다.
12. AI가 코드를 작성할 수 있어도 문제 정의, 결과 해석, 다음 행동의 판단은 사람이 해야 한다.

---

## 체크리스트

* [ ] 데이터 분석의 전체 흐름을 설명할 수 있다.
* [ ] `shape`, `head()`, `info()`, `describe()`의 역할을 구분할 수 있다.
* [ ] 숫자형으로 저장된 범주형 데이터를 구분할 수 있다.
* [ ] 결측치를 무조건 삭제하면 안 되는 이유를 설명할 수 있다.
* [ ] Feature와 Target의 차이를 설명할 수 있다.
* [ ] 분류와 회귀의 차이를 설명할 수 있다.
* [ ] 데이터 누수가 무엇인지 설명할 수 있다.
* [ ] Encoding과 One-Hot Encoding의 기본 원리를 설명할 수 있다.
* [ ] EDA의 목적을 설명할 수 있다.
* [ ] 상관관계와 인과관계를 구분할 수 있다.

---

## 예시 코드

```python
# 데이터 기본 확인
df.shape
df.columns
df.head()
df.info()
df.describe()

# 결측치 확인
df.isna().sum()

# 중복 확인
df.duplicated().sum()

# 범주별 개수 확인
df["Survived"].value_counts()

# 범주별 비율 확인
df["Survived"].value_counts(normalize=True)

# 원본 데이터 보존
df_work = df.copy()

# One-Hot Encoding
pd.get_dummies(df_work["Embarked"])
```

---

## 한 줄 정리

데이터 분석은 데이터를 불러와 바로 머신러닝을 하는 것이 아니라 데이터를 이해하고 전처리한 뒤 문제와 Feature·Target을 정의하고 EDA를 통해 분석 방향을 판단하는 과정이다.

## 다음 할 일
타이타닉 EDA 과정 다시 복습하기
Feature와 Target 구분 확실히 이해하기
분류와 회귀 차이 복습하기
Encoding과 One-Hot Encoding 다시 확인하기
데이터 누수 사례 생각해보기
시각화 결과를 보고 직접 해석하는 연습하기
이후 머신러닝 단계가 EDA 결과와 어떻게 연결되는지 확인하기
