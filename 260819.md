# 2026-08-19 TIL

## 주제: AI를 활용한 코딩 학습 방법 & Python 기초

### 오늘 배운 것

#### 1. AI에게 정답보다 **이해를 요청하기**

* AI에게 단순히 `"코드 고쳐줘"`라고 질문하지 않는다.
* **내 수준 + 목표 + 현재 상황 + 코드/오류 + 원하는 도움**을 함께 전달한다.
* 학습할 때는 **정답 코드를 바로 보여주지 말고 힌트와 설명을 요청**한다.
* `"단계별로 가이드해 줘"`, `"가장 먼저 해야 할 작업 한 가지만 알려줘"`처럼 질문하면 스스로 생각할 수 있다.
* 어려운 개념은 **쉬운 비유나 작은 예제**로 다시 설명해 달라고 요청한다.

#### 2. AI가 만든 코드는 반드시 직접 실행하기

* AI가 작성하거나 수정한 코드를 **직접 실행하고 결과를 확인**해야 한다.
* 실행 → 결과 확인 → 값 변경 → 다시 실행하는 과정을 반복한다.
* 오류가 발생하면 실패가 아니라 **문제를 발견한 것**이다.
* 오류 메시지에서 **오류 종류와 발생한 줄**을 확인하는 습관이 중요하다.

#### 3. 좋은 오류 질문 만들기

AI에게 오류를 물어볼 때 다음 정보를 함께 준다.

* 내가 무엇을 하려고 했는지
* 현재 코드
* 실제 실행 결과 / 오류 메시지
* 내가 기대한 결과
* **정답 대신 오류 원인과 다음 행동을 알려달라는 요청**

#### 4. Python 코드의 기본 흐름

프로그램은 기본적으로 **입력 → 처리 → 출력**의 흐름으로 생각할 수 있다.

예:

```
first_number = 10
second_number = 20

result = first_number + second_number

print(result)
```

* `first_number`, `second_number` → 데이터를 저장
* `+` → 두 숫자를 계산
* `result` → 계산 결과 저장
* `print()` → 화면에 결과 출력

#### 5. `print()`와 문자열

```
print("Hello Python")
```

* `print()` → 화면에 값을 보여주는 기능
* `()` 안 → 출력할 내용
* `" "` 또는 `' '` → 글자(문자열)를 나타냄

```
print(100)       # 숫자
print("100")     # 문자
```

겉으로는 둘 다 `100`으로 보이지만 **숫자와 문자는 서로 다른 데이터**이다.

#### 6. 쉼표를 이용한 여러 값 출력

```
name = "홍길동"
study_time = 2

print(name, "님의 학습 시간은", study_time, "시간입니다.")
```

`print()` 안에서 쉼표(`,`)로 여러 값을 나누면 **여러 값을 함께 출력**할 수 있다.

또한 값 사이에 기본적으로 **공백이 들어간다.**

#### 7. 코드 실행 순서

Python 코드는 기본적으로 **위에서 아래로 순서대로 실행**된다.

```
print("첫 번째")
print("두 번째")
print("세 번째")
```

→ 첫 번째 → 두 번째 → 세 번째 순서로 실행된다.

#### 8. 주석

```
# 이 부분은 설명입니다.
print("Hello Python")
```

* `#` 뒤의 내용은 **주석**
* 프로그램 실행에는 영향을 주지 않는다.
* 코드를 설명하기 위해 사용한다.
* 너무 많은 주석보다는 **필요한 부분만 간결하게 작성**한다.

#### 9. 들여쓰기

Python에서는 **들여쓰기 자체가 문법**이다.

특히 `if`, 반복문, 함수 등 코드의 묶음을 표현할 때 중요하다.

들여쓰기를 잘못하면:

```
IndentationError
```

가 발생할 수 있다.

#### 10. 오류와 리팩토링

* **오류 = 실패가 아님**
* 오류를 발견하고 원인을 찾아 수정하는 과정이 개발의 일부이다.
* 오류 메시지를 보고 무엇이 잘못됐는지 확인하는 습관이 중요하다.

**리팩토링(Refactoring)**
→ 프로그램의 결과는 그대로 유지하면서 **코드의 구조와 가독성을 개선하는 것**

예를 들어 `a`, `b`, `c`보다 `math_score`, `english_score`처럼 **의미가 드러나는 변수 이름**을 사용하는 것이 좋다.

---

### 가장 중요하게 느낀 것

> **AI에게 정답을 받는 것이 목적이 아니라, AI를 이용해서 내가 스스로 생각하고 이해하는 것이 중요하다.**

특히 코딩할 때는
**예상 → 직접 실행 → 결과 확인 → 값 변경 → 다시 실행 → 오류 해결**
이 과정을 반복해야 한다.

---

### 오늘의 핵심 한 줄

**"AI에게 코드를 대신 만들어 달라고 하기보다, 내가 이해하고 직접 실행·검증할 수 있도록 가이드받는다."**
