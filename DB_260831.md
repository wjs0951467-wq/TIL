# 2026-08-31 TIL 데이터베이스 개요

## 오늘 배운 것

### 1. AI 시대에도 데이터베이스를 배워야 하는 이유

AI를 사용하면 SQL이나 테이블 설계를 쉽게 만들 수 있지만, **AI가 만든 결과가 항상 올바른 것은 아니다.**

SQL이 오류 없이 실행되더라도 요구사항과 다른 결과가 나올 수 있기 때문에 사람이 직접 결과를 검증해야 한다.

> 요구사항 확인 → 데이터 모델링 → SQL 작성 → 실행 → 결과 검증 → 운영·분석

---

### 2. 데이터베이스 기본 개념

- **DB (Database)**: 데이터를 저장하고 관리하는 공간
- **DBMS (Database Management System)**: 데이터베이스를 관리하는 시스템
- **RDBMS (Relational Database Management System)**: 데이터를 테이블과 관계로 관리하는 방식
- **PostgreSQL**: 이번 과정에서 사용하는 데이터베이스
- **DBeaver**: 데이터베이스를 편리하게 관리하는 도구
- **SQL**: 데이터베이스의 데이터를 조회·생성·수정·삭제하는 언어

---

### 3. 테이블과 레코드

데이터베이스의 **테이블은 엑셀의 한 시트**라고 생각하면 이해하기 쉽다.

- 테이블 → 데이터 전체
- 행(Row) / 레코드(Record) → 실제 데이터 하나
- 열(Column) → 데이터의 항목

특히 **레코드 하나가 무엇을 의미하는지 정확하게 파악하는 것**이 중요하다.

예를 들어 학생 학습 현황 테이블에서 레코드 하나는 **특정 학생 한 명의 학습 현황**을 의미한다.

---

### 4. 데이터 모델링과 ERD

**데이터 모델링**은 비즈니스 요구사항을 데이터베이스 구조로 설계하는 과정이다.

**ERD (Entity Relationship Diagram)**는 테이블과 테이블 사이의 관계를 그림으로 표현한 것이다.

데이터 모델링에서는 다음 질문이 중요하다.

1. 어떤 테이블이 필요한가?
2. 테이블의 한 행은 무엇을 의미하는가?
3. 기본 키(PK)는 무엇인가?
4. 외래 키(FK)는 무엇인가?
5. 테이블은 어떻게 연결되는가?
6. 아직 결정되지 않은 정책은 무엇인가?

---

### 5. PK와 FK

- **PK (Primary Key)**: 각 레코드를 고유하게 구분하는 값
- **FK (Foreign Key)**: 다른 테이블의 PK를 참조하는 값

FK를 통해 테이블 사이의 관계가 만들어지며, 잘못된 데이터를 참조하지 않도록 **참조 무결성**을 유지해야 한다.

---

### 6. 정규화와 데이터 무결성

**정규화(Normalization)**는 데이터 중복을 줄이고 데이터의 일관성을 유지하기 위해 데이터를 적절한 테이블로 나누는 과정이다.

데이터 무결성을 유지하기 위해 다음과 같은 제약 조건을 사용할 수 있다.

- `PRIMARY KEY`
- `FOREIGN KEY`
- `NOT NULL`
- `UNIQUE`
- `CHECK`

---

### 7. 트랜잭션

**트랜잭션(Transaction)**은 데이터베이스에서 처리되는 작업의 단위이다.

데이터를 변경하는 작업을 하나의 단위로 처리해야 할 때 `COMMIT`, `ROLLBACK` 등을 사용하여 데이터의 일관성을 관리한다.

---

### 8. 인덱스

**인덱스(Index)**는 원하는 데이터를 빠르게 찾기 위한 구조이다.

책의 뒤쪽에 있는 **색인**과 비슷한 역할을 한다.

인덱스를 적절하게 사용하면 조회 성능을 향상시킬 수 있지만, 무조건 많이 만드는 것이 좋은 것은 아니다.

---

### 9. CRUD와 SQL

CRUD는 데이터 처리의 기본 작업이다.

| 구분 | 의미 | SQL |
|---|---|---|
| C | Create | `INSERT` |
| R | Read | `SELECT` |
| U | Update | `UPDATE` |
| D | Delete | `DELETE` |

또한 SQL은 데이터 구조를 정의하는 **DDL**과 데이터를 조작하는 **DML** 등으로 구분할 수 있다.

- **DDL (Data Definition Language)**: 테이블 생성 등 데이터 구조 정의
- **DML (Data Manipulation Language)**: 데이터 추가·조회·수정·삭제

---

## 가장 중요하게 배운 것

### SQL 실행 성공 ≠ 올바른 결과

SQL에 오류가 없다고 해서 결과까지 맞는 것은 아니다.

예를 들어

- JOIN 조건이 잘못되거나
- 집계 기준이 잘못되거나
- 요구사항과 다른 데이터를 조회하면

SQL은 정상적으로 실행되지만 결과는 틀릴 수 있다.

따라서 AI가 작성한 SQL도 반드시 직접 검증해야 한다.

> **업무 질문 확인 → 요구사항 확인 → SQL 작성 → 실행 → 결과 확인 → 예상 결과와 비교**

---

## 앞으로의 학습 방향

이번 과정에서는 데이터베이스 기초부터 시작해서

**DBMS → PostgreSQL → SQL → 데이터 모델링 → ERD → 정규화 → JOIN → 트랜잭션 → 인덱스 → 성능 → 보안·백업 → AI 검증 → Python·Pandas 데이터 분석**

순서로 학습한다.

## 오늘의 한 줄 정리

> **AI 시대의 데이터베이스 학습은 SQL 문법을 외우는 것보다 데이터의 의미와 SQL 결과가 요구사항에 맞는지 검증하는 능력이 중요하다.**
