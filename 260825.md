# TIL — 딕셔너리와 중첩 자료 구조

## 오늘 배운 것

오늘은 파이썬의 복합 자료 구조 중 **딕셔너리(Dictionary)**를 학습했다.

리스트, 튜플, 셋이 여러 데이터를 묶어서 관리하는 자료 구조라면, 딕셔너리는 **데이터를 Key(키)와 Value(값)의 쌍으로 관리**하는 자료 구조이다.

특히 여러 사람이나 제품처럼 각각 다른 속성을 가진 데이터를 표현할 때 유용하다.

---

## 핵심 개념

### 1. 딕셔너리 기본 구조

딕셔너리는 `{}`를 사용하고 `Key : Value` 형태로 데이터를 저장한다.

```python
student = {
    "name": "민수",
    "age": 20,
    "score": 85
}
```

구조를 보면 다음과 같다.

```text
name  → 민수
age   → 20
score → 85
```

파이썬에서 딕셔너리의 타입은 `dict`이다.

```python
print(type(student))
```

```text
<class 'dict'>
```

---

### 2. 딕셔너리는 Key로 데이터에 접근한다

리스트와 튜플은 인덱스를 사용하지만, 딕셔너리는 **Key를 사용해서 값에 접근**한다.

```python
print(student["name"])
print(student["score"])
```

```text
민수
85
```

따라서 리스트처럼 `student[0]` 방식으로 접근하는 것이 아니라 `student["name"]`처럼 Key를 이용한다.

---

### 3. 데이터 추가와 수정

없는 Key에 값을 넣으면 **새로운 항목이 추가**된다.

```python
student["city"] = "서울"
```

기존에 존재하는 Key에 값을 넣으면 **기존 값이 수정**된다.

```python
student["score"] = 95
```

정리하면:

```text
Key가 없음 → 새로운 Key-Value 추가
Key가 있음 → 기존 Value 수정
```

---

### 4. `get()`으로 안전하게 조회하기

존재하지 않는 Key를 `[]`로 조회하면 `KeyError`가 발생한다.

```python
student["phone"]
```

반면 `get()`을 사용하면 Key가 없어도 오류가 발생하지 않는다.

```python
print(student.get("phone"))
```

기본값도 지정할 수 있다.

```python
print(student.get("phone", "정보 없음"))
```

```text
정보 없음
```

따라서 `get()`은 **Key가 존재하지 않을 수도 있을 때 안전하게 데이터를 조회하는 방법**이다.

---

### 5. 딕셔너리 데이터 삭제

`del`을 사용하면 특정 Key-Value를 삭제할 수 있다.

```python
del student["city"]
```

`pop()`은 데이터를 **가져오면서 동시에 삭제**한다.

```python
score = student.pop("score")
```

정리하면:

```text
del  → 삭제
pop  → 가져오기 + 삭제
```

---

### 6. `keys()`, `values()`, `items()`

딕셔너리의 데이터를 종류별로 가져올 수 있다.

```python
student.keys()
```

→ Key만 가져온다.

```python
student.values()
```

→ Value만 가져온다.

```python
student.items()
```

→ Key와 Value를 함께 가져온다.

| 메서드        | 역할               |
| ---------- | ---------------- |
| `keys()`   | Key 가져오기         |
| `values()` | Value 가져오기       |
| `items()`  | Key + Value 가져오기 |

특히 **`items()`는 반복문에서 중요하다.**

---

### 7. `items()`와 `for`문

```python
student = {
    "name": "민수",
    "age": 20,
    "score": 85
}

for key, value in student.items():
    print(key, value)
```

결과:

```text
name 민수
age 20
score 85
```

`items()`를 사용하면 Key와 Value가 각각 `key`, `value` 변수에 나누어 들어간다.

이것은 앞에서 배운 **튜플 언패킹과 비슷한 방식**이다.

```text
items()
   ↓
Key + Value
   ↓
key, value로 나누어 받음
```

---

## 리스트와 딕셔너리의 차이

리스트로 학생 정보를 저장하면:

```python
student = ["민수", 20, 85]
```

여기서 `20`이 나이인지, `85`가 점수인지 데이터만 보고 바로 알기 어렵다.

반면 딕셔너리는:

```python
student = {
    "name": "민수",
    "age": 20,
    "score": 85
}
```

각 데이터의 의미가 Key에 나타나기 때문에 훨씬 이해하기 쉽다.

```python
student["age"]
student["score"]
```

즉,

> **비슷한 값들을 단순히 나열하는 경우에는 리스트가 편하고, 각각의 값에 의미를 부여해서 관리해야 한다면 딕셔너리가 적합하다.**

---

## 리스트 + 딕셔너리

실제 데이터에서는 **리스트 안에 여러 개의 딕셔너리를 넣는 구조**를 많이 사용한다.

```python
students = [
    {
        "name": "민수",
        "score": 85
    },
    {
        "name": "영희",
        "score": 90
    },
    {
        "name": "철수",
        "score": 75
    }
]
```

구조는 다음과 같다.

```text
students → 리스트
    ↓
딕셔너리
    ↓
학생 1명 정보

딕셔너리
    ↓
학생 1명 정보

딕셔너리
    ↓
학생 1명 정보
```

즉,

> **리스트로 여러 대상을 묶고, 딕셔너리로 각각의 대상이 가진 정보를 관리하는 구조**이다.

이런 구조는 실제 데이터나 테이블 형태의 데이터를 표현할 때 많이 사용된다.

---

## 리스트 안의 딕셔너리 반복하기

```python
for student in students:
    print(student["name"], student["score"])
```

학생을 한 명씩 가져온 후 딕셔너리의 Key를 이용해서 필요한 값을 가져온다.

조건문과 함께 사용하면 특정 조건을 만족하는 데이터만 찾을 수도 있다.

```python
for student in students:
    if student["score"] >= 80:
        print(student["name"])
```

이런 형태는 앞으로 데이터를 처리할 때 자주 사용하게 될 구조이다.

---

## 중첩 자료 구조

자료 구조 안에 또 다른 자료 구조를 넣을 수도 있다.

예를 들어 딕셔너리 안에 딕셔너리를 넣을 수 있다.

```python
product = {
    "name": "노트북",
    "spec": {
        "ram": 16,
        "storage": 512,
        "size": 15
    }
}
```

`spec`이라는 Key의 Value가 또 다른 딕셔너리이다.

따라서 `ram` 값을 가져오려면:

```python
print(product["spec"]["ram"])
```

처럼 단계적으로 접근한다.

```text
product
  ↓
spec
  ↓
ram
  ↓
16
```

이처럼 자료 구조가 안쪽으로 계속 들어가는 형태를 **중첩 구조(Nested Structure)**라고 한다.

---

## 문자열 `split()`

문자열을 특정 기준으로 나누고 싶을 때 `split()`을 사용할 수 있다.

```python
sentence = "Python is easy to learn"

words = sentence.split()
```

결과:

```python
["Python", "is", "easy", "to", "learn"]
```

`split()`을 사용하면 하나의 문자열이 여러 개의 데이터로 나뉘고, 결과는 **리스트**가 된다.

```text
문자열
 ↓
split()
 ↓
단어 분리
 ↓
리스트
```

특정 문자를 기준으로 나눌 수도 있다.

```python
text = "apple,banana,orange"

words = text.split(",")
```

---

## 딕셔너리를 이용한 단어 빈도수 세기

오늘 배운 내용을 연결하면 문장에서 단어가 몇 번 등장했는지도 확인할 수 있다.

```python
sentence = "apple banana apple orange banana apple"

words = sentence.split()

count = {}

for word in words:
    if word in count:
        count[word] += 1
    else:
        count[word] = 1

print(count)
```

결과:

```text
{'apple': 3, 'banana': 2, 'orange': 1}
```

여기서 딕셔너리는 다음과 같은 역할을 한다.

```text
단어 → 등장 횟수

apple  → 3
banana → 2
orange → 1
```

이런 방식은 이후 **텍스트 마이닝이나 자연어 처리(NLP)**에서 데이터를 다룰 때 연결되는 중요한 기본 로직이다.

---

## 가장 중요하게 느낀 것

이번 강의에서 가장 중요한 것은 **자료 구조를 단순히 외우는 것이 아니라 데이터의 형태에 맞는 자료 구조를 선택하는 것**이라고 느꼈다.

특히 다음 구조가 중요했다.

```text
리스트
└── 딕셔너리
    ├── Key : Value
    ├── Key : Value
    └── Key : Value
```

여러 학생의 데이터를 관리한다면:

```text
리스트
 ├── 학생 딕셔너리
 ├── 학생 딕셔너리
 └── 학생 딕셔너리
```

처럼 표현할 수 있다.

또한 딕셔너리 안에 또 다른 딕셔너리를 넣을 수도 있기 때문에 실제 데이터가 복잡해져도 여러 자료 구조를 조합해서 표현할 수 있다는 것을 이해했다.

---

## 실습·복습 체크리스트

* [ ] 딕셔너리의 `Key : Value` 구조 이해하기
* [ ] `dict` 타입 이해하기
* [ ] Key를 이용해서 값 조회하기
* [ ] 없는 Key에 값을 넣어 데이터 추가하기
* [ ] 기존 Key의 값 수정하기
* [ ] `get()`으로 안전하게 조회하기
* [ ] `del`로 데이터 삭제하기
* [ ] `pop()`으로 가져오면서 삭제하기
* [ ] `keys()`, `values()`, `items()` 구분하기
* [ ] `items()`와 `for`문 연결하기
* [ ] 리스트 안에 딕셔너리를 넣는 구조 이해하기
* [ ] 리스트 + 딕셔너리를 반복문으로 처리하기
* [ ] 딕셔너리 안에 딕셔너리를 넣는 중첩 구조 이해하기
* [ ] `split()`으로 문자열을 리스트로 나누기
* [ ] 딕셔너리를 이용해 단어 빈도수 세기

---

## 오늘의 핵심 한 줄

> **딕셔너리는 데이터를 Key와 Value로 의미 있게 관리하며, 리스트와 결합하면 여러 개의 데이터를 실제 테이블처럼 구조화해서 처리할 수 있다.**
