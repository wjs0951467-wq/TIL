# 2026-08-27 TIL 프로젝트에서 안전하게 파일 경로 다루기와 예외 처리

## 오늘 배운 것

오늘은 프로젝트를 진행할 때 자주 사용하는 **파일 경로 처리 방법**과 프로그램에서 오류가 발생했을 때 대응하는 **예외 처리(Exception Handling)**를 배웠다.

특히 프로젝트에서는 내 컴퓨터에서만 작동하는 코드를 작성하면 안 되기 때문에 **절대 경로 대신 프로젝트의 기준 위치를 이용한 상대 경로**를 사용하는 것이 중요하다.

또한 프로그램이 실행되는 도중 예상하지 못한 값이 들어오더라도 프로그램 전체가 종료되지 않도록 `try-except`를 이용해 예외를 처리하는 방법을 배웠다.

---

## 1. 파일 경로는 절대 경로보다 상대 경로를 사용한다

컴퓨터마다 프로젝트가 저장되는 위치가 다르다.

예를 들어 내 컴퓨터에서는

```text
C:\dev\python_basics
```

에 프로젝트가 있을 수 있지만 다른 사람은

```text
D:\project\python_basics
```

에 있을 수도 있다.

Mac을 사용하는 사람이라면 경로 자체가 완전히 다를 수도 있다.

따라서 다음과 같이 특정 컴퓨터의 경로를 코드에 직접 작성하는 것은 좋지 않다.

```text
C:\dev\python_basics\data\chapter16\memo.txt
```

이런 방식은 **절대 경로**라고 한다.

### 왜 문제가 될까?

프로젝트를 다른 컴퓨터로 옮기면 경로가 달라지기 때문에 코드가 작동하지 않을 수 있다.

그래서 프로젝트에서는 **프로젝트의 루트 디렉터리(Base Directory)를 기준으로 하위 경로를 만드는 방식**을 사용한다.

예를 들어 구조가 다음과 같다고 가정한다.

```text
프로젝트 루트
├── data
│   └── chapter16
│       └── memo.txt
└── main.py
```

프로젝트 루트를 기준으로 한다면

```text
Base Directory / data / chapter16 / memo.txt
```

와 같이 경로를 만들 수 있다.

이렇게 하면 프로젝트가 다른 위치로 이동하더라도 **프로젝트 루트만 제대로 찾으면 나머지 경로는 동일하게 사용할 수 있다.**

---

## 2. `__file__`과 실행 환경

파이썬 파일에서는 `__file__`이라는 특별한 값이 존재한다.

`__file__`은 현재 실행하고 있는 `.py` 파일의 위치를 나타내는 데 사용할 수 있다.

따라서 일반적인 `.py` 파일에서는 `__file__`을 기준으로 프로젝트의 위치를 찾는 방법을 사용할 수 있다.

그런데 여기서 문제가 하나 있다.

우리가 사용하는 **Jupyter Notebook에서는 일반적인 `.py` 파일과 실행 환경이 다르다.**

그래서 `.py` 파일에서는 사용할 수 있는 `__file__`이 Jupyter Notebook에서는 정의되어 있지 않을 수 있다.

이때 다음과 같은 오류가 발생할 수 있다.

```text
NameError: name '__file__' is not defined
```

### 핵심

```text
.py 파일
→ __file__ 사용 가능

Jupyter Notebook
→ __file__이 없을 수 있음
```

따라서 프로젝트에서 파일 경로를 공통적으로 처리하려면 **현재 실행 환경이 `.py`인지 Jupyter Notebook인지 구분하는 방법**이 필요하다.

---

## 3. `__file__`이 특별한 이유

`__file__`에서 앞뒤에 붙은 `__` 때문에 일반적인 변수처럼 보이지 않을 수 있다.

이런 이름은 파이썬에서 특별한 의미를 가진 값으로 사용되는 경우가 많다.

특히 `__file__`은 현재 실행 중인 파이썬 파일의 위치와 관련된 정보를 제공한다.

다만 중요한 것은 **모든 실행 환경에서 항상 존재하는 것은 아니라는 것**이다.

따라서 오류가 발생했을 때 단순히 "`__file__`이 틀렸다"라고 생각하기보다는

> 지금 코드를 어떤 환경에서 실행하고 있는가?

를 먼저 확인해야 한다.

---

## 4. `Path`와 `resolve()`

파일 경로를 다룰 때 파이썬에서는 `pathlib`을 사용할 수 있다.

`Path`는 파일이나 폴더의 경로를 객체로 다룰 수 있도록 도와준다.

그리고 `resolve()`는 경로를 실제 경로로 해석하는 데 사용할 수 있다.

여기서 `resolve`는 단순히 "결과"라는 의미가 아니라 **경로를 실제 위치로 해석한다**는 의미로 이해하면 된다.

예를 들어 개념적으로는

```text
현재 파일 위치
→ 경로를 해석
→ 실제 프로젝트 위치 확인
```

과 같은 흐름이다.

---

## 5. 실행 환경에 따라 분기하기

프로젝트에서 `.py` 파일과 Jupyter Notebook 모두에서 사용할 수 있는 코드를 만들고 싶다면 실행 환경에 따라 로직을 나눌 수 있다.

이때 사용하는 것이 `if`문이다.

개념적으로는 다음과 같다.

```text
현재 실행 환경 확인
        ↓
   .py 파일인가?
    ↙       ↘
  YES        NO
   ↓          ↓
__file__     Notebook 방식
사용          사용
```

이렇게 하나의 코드에서 상황에 따라 다른 방법을 선택하는 것을 **분기**라고 한다.

이렇게 만들어 두면 나중에 여러 프로젝트에서 같은 로직을 재사용하기 편해진다.

---

# 6. CSV 파일은 어떻게 읽어야 할까?

CSV 파일을 직접 텍스트로 읽는 방법도 있지만, 데이터 분석에서는 일반적으로 **pandas를 사용하는 것이 훨씬 편리하다.**

예를 들어 CSV를 직접 읽어서

```text
파일 열기
→ 한 줄씩 읽기
→ 데이터 나누기
→ 자료형 처리
→ 계산
```

하는 방식은 가능하지만 상당히 번거롭다.

그래서 데이터 분석에서는 보통 pandas를 이용한다.

```text
CSV
 ↓
pandas
 ↓
DataFrame
 ↓
데이터 분석
```

특히 다음과 같은 기능을 활용할 수 있다.

* `info()` : 데이터의 기본 정보 확인
* `describe()` : 통계 정보 확인
* `shape` : 행과 열의 개수 확인

따라서 CSV를 다룰 때는 **"CSV = pandas + DataFrame"**이라는 흐름을 기억해 두는 것이 중요하다.

다만 일반적인 텍스트 파일을 처리해야 하는 경우에는 `open()` 등을 이용해 직접 파일을 읽을 수도 있다.

---

# 7. 오류의 종류

프로그램에서 발생하는 오류는 크게 세 가지로 생각할 수 있다.

### ① 문법 오류 (Syntax Error)

코드 자체의 문법이 잘못된 경우다.

예를 들어 `if`문 뒤에 콜론이 빠지는 경우가 있다.

```python
if score >= 60
    print("통과")
```

문법 자체가 잘못되었기 때문에 실행 전에 수정해야 한다.

---

### ② 런타임 오류 (Runtime Error)

프로그램이 실행되는 과정에서 문제가 발생하는 경우다.

예를 들어 숫자로 변환할 수 없는 문자열을 `int()`로 변환하려고 하면 오류가 발생한다.

또는 0으로 나누려고 해도 오류가 발생한다.

```text
프로그램 실행
    ↓
코드 실행
    ↓
예상하지 못한 상황 발생
    ↓
Runtime Error
```

런타임 오류가 처리되지 않으면 프로그램이 중간에 종료될 수 있다.

---

### ③ 논리 오류 (Logical Error)

가장 까다로운 오류 중 하나다.

프로그램 자체는 정상적으로 실행되지만 **결과가 내가 원하는 결과와 다른 경우**다.

예를 들어 총 가격을 계산해야 하는데

```python
total_price = price + quantity
```

처럼 작성했다고 해보자.

문법 오류도 없고 프로그램도 실행된다.

하지만 총 가격은 가격과 수량을 곱해야 하므로 논리적으로 잘못된 코드다.

```python
total_price = price * quantity
```

처럼 되어야 한다.

### 세 가지를 비교하면

| 오류     | 프로그램 실행 | 특징             |
| ------ | ------- | -------------- |
| 문법 오류  | 실행 전 문제 | 문법 자체가 잘못됨     |
| 런타임 오류 | 실행 중 문제 | 예상하지 못한 값이나 상황 |
| 논리 오류  | 실행됨     | 결과가 의도와 다름     |

특히 **논리 오류는 에러 메시지가 나오지 않을 수 있기 때문에 더 찾기 어렵다.**

---

# 8. 예외(Exception)와 예외 처리

프로그램을 만들다 보면 사용자가 항상 정상적인 값을 입력한다고 보장할 수 없다.

예를 들어

```text
나이를 입력하세요.
→ 20
```

은 정상적으로 처리할 수 있다.

하지만

```text
나이를 입력하세요.
→ hello
```

가 들어오면 문자열을 정수로 바꾸는 과정에서 문제가 발생한다.

이런 상황을 프로그램에서 적절하게 처리하기 위해 사용하는 것이 **예외 처리**다.

가장 중요한 문법이 바로 다음이다.

```python
try:
    # 오류가 발생할 가능성이 있는 코드
except:
    # 오류가 발생했을 때 처리할 코드
```

---

# 9. `try-except`의 실행 흐름

`try`에는 **오류가 발생할 가능성이 있는 코드**를 넣는다.

```text
try 실행
   ↓
오류 없음 ─────→ try 아래 코드 계속 실행
   ↓
오류 발생
   ↓
except로 이동
   ↓
오류에 맞는 처리 실행
```

예를 들어 사용자가 나이를 입력하는 상황이라면

```text
사용자 입력
    ↓
정수로 변환
    ↓
성공 → 다음 코드 실행
    ↓
실패 → ValueError 발생
    ↓
except에서 처리
    ↓
"정수를 입력해주세요"
```

이런 방식으로 프로그램이 갑자기 종료되는 것을 막을 수 있다.

---

# 10. 자주 볼 수 있는 예외

이번 강의에서 중요하게 본 예외들은 다음과 같다.

### `ValueError`

값의 형식이 잘못되어 원하는 형태로 변환할 수 없을 때 발생할 수 있다.

```python
int("hello")
```

문자열 `"hello"`를 정수로 바꿀 수 없기 때문에 `ValueError`가 발생한다.

---

### `ZeroDivisionError`

0으로 나눌 때 발생한다.

```python
100 / 0
```

---

### `FileNotFoundError`

존재하지 않는 파일을 열려고 할 때 발생할 수 있다.

```text
파일 열기
 ↓
파일 없음
 ↓
FileNotFoundError
```

---

### `IndexError`

존재하지 않는 리스트의 인덱스에 접근할 때 발생한다.

예를 들어 데이터가 2개밖에 없는데 5번째 데이터를 요청하는 경우다.

---

### `KeyError`

딕셔너리에 존재하지 않는 키를 요청할 때 발생할 수 있다.

---

# 11. 오류 메시지를 읽는 법

오류가 발생했을 때 무조건 GPT에게 에러 화면 전체를 던지는 것보다 **에러 메시지의 구조를 먼저 보는 습관**이 중요하다.

특히 다음 세 가지를 확인해야 한다.

### ① 예외 이름

```text
ValueError
KeyError
FileNotFoundError
ZeroDivisionError
```

어떤 종류의 오류인지 알려준다.

### ② 오류가 발생한 코드 위치

몇 번째 줄에서 문제가 발생했는지 확인한다.

### ③ 실제 오류 메시지

무엇 때문에 오류가 발생했는지 설명해 준다.

따라서 오류를 볼 때

```text
무슨 오류인가?
↓
몇 번째 줄인가?
↓
어떤 값 때문에 발생했는가?
```

순서로 확인하면 원인을 훨씬 쉽게 찾을 수 있다.

---

# 12. `try-except`는 모든 오류를 해결하는 만능 기능이 아니다

중요한 부분이다.

오류가 발생할 수 있다고 해서 **모든 코드를 무조건 `try-except`로 감싸면 안 된다.**

예를 들어 프로그램 전체를 하나의 `try`로 감싸버리면 실제로 문제가 발생했는데도 오류를 제대로 발견하지 못하고 넘어갈 수 있다.

즉,

```text
오류 발생
 ↓
무조건 잡아버림
 ↓
문제가 있었는지 알기 어려움
```

이라는 문제가 생길 수 있다.

예외 처리는 **오류가 예상되는 특정 로직에 적절하게 사용하는 것**이 중요하다.

특히 `try` 안에 너무 많은 코드를 넣는 것도 좋지 않다.

```text
try
 ├── 코드 1
 ├── 코드 2
 ├── 코드 3
 ├── 코드 4
 ├── 코드 5
 └── 코드 6
```

처럼 지나치게 큰 범위를 감싸면 어떤 코드에서 문제가 발생했는지 파악하기 어려워진다.

---

# 13. 구체적인 예외를 먼저 처리하기

`except`에는 특정 예외의 종류를 지정할 수 있다.

예를 들어

```text
ValueError → 잘못된 값 처리
ZeroDivisionError → 0으로 나눈 경우 처리
FileNotFoundError → 파일이 없는 경우 처리
```

처럼 상황에 맞게 나눠서 처리할 수 있다.

그리고 마지막에 정말 예상하지 못한 예외까지 처리하고 싶을 경우 일반적인 `except`를 사용할 수 있다.

중요한 것은 **구체적인 예외를 먼저 작성하고 일반적인 예외를 마지막에 작성하는 것**이다.

---

# 14. `else`와 `finally`

`try-except`에는 `else`와 `finally`도 사용할 수 있다.

### `else`

`try`에서 **오류가 발생하지 않았을 때** 실행된다.

```text
try
 ↓
오류 없음
 ↓
else 실행
```

따라서 성공했을 때만 실행해야 하는 코드를 넣을 수 있다.

---

### `finally`

오류가 발생했는지 여부와 상관없이 **무조건 마지막에 실행된다.**

```text
try
 ↓
성공 ─────→ finally
 ↓
실패
 ↓
except
 ↓
finally
```

대표적인 예로 데이터베이스 연결을 생각할 수 있다.

```text
DB 연결
 ↓
작업
 ↓
성공 또는 오류
 ↓
연결 종료
```

DB 작업이 성공하더라도 연결을 닫아야 하고, 오류가 발생하더라도 연결을 닫아야 한다.

이처럼 **성공과 실패에 관계없이 반드시 실행되어야 하는 정리 작업**에 `finally`가 적합하다.

---

# 15. 입력값 검증과 예외 처리는 다르다

사용자가 입력한 값이 잘못되었다고 해서 모든 것을 `try-except`에 맡기면 안 된다.

예를 들어 사용자가 점수를 입력한다고 해보자.

```text
문자 입력
→ ValueError
```

이것은 예외 처리가 필요할 수 있다.

하지만

```text
150 입력
```

은 숫자로 변환하는 데는 문제가 없다.

그렇지만 점수의 범위를 `0~100`으로 제한한다면 150은 잘못된 값이다.

따라서

```text
1. 사용자 입력
2. 자료형 변환
3. 범위 검증
4. 실제 로직 실행
```

처럼 단계별로 검증해야 한다.

즉,

> 형변환에 성공했다고 해서 입력값이 올바른 것은 아니다.

이 점이 중요하다.

---

# 16. `while`과 `break`, `continue`

사용자가 올바른 값을 입력할 때까지 계속 입력을 받는 경우에는 `while`을 활용할 수 있다.

특히

```python
while True:
```

는 조건이 항상 `True`이기 때문에 **무한 루프**가 된다.

따라서 반드시 반복을 종료할 수 있는 조건이 필요하다.

```text
while True
   ↓
입력
   ↓
잘못된 값
   ↓
다시 입력
   ↓
정상적인 값
   ↓
break
   ↓
반복 종료
```

### `continue`

현재 반복의 남은 부분을 실행하지 않고 다음 반복으로 넘어간다.

### `break`

반복문 자체를 종료한다.

다만 `break`, `continue`를 너무 많이 사용하면 프로그램의 흐름이 복잡해질 수 있으므로 **읽을 수는 있어야 하지만 무조건 많이 사용하는 것이 좋은 것은 아니다.**

---

# 17. 함수 안에서 발생한 예외도 호출한 곳에서 처리할 수 있다

예외 처리는 함수 안에서만 해야 하는 것은 아니다.

예를 들어

```text
함수 실행
 ↓
함수 내부에서 오류 발생
 ↓
예외가 밖으로 전달됨
 ↓
함수를 호출한 곳의 try-except에서 처리
```

하는 것도 가능하다.

반대로 함수 내부에 직접 `try-except`를 넣어 처리할 수도 있다.

즉, 상황에 따라

```text
함수 내부에서 처리
```

하거나

```text
함수를 호출하는 곳에서 처리
```

할 수 있다.

---

# 18. 개발할 때 중요한 사고방식

이번 강의에서 가장 중요하다고 느낀 부분은 **오류를 없애는 것과 오류를 적절하게 처리하는 것은 다르다**는 것이다.

개발 중에는 오류가 발생하는 것이 오히려 자연스럽다.

오류가 발생해야

```text
어디가 잘못되었는지
↓
어떤 상황에서 문제가 발생하는지
↓
어떻게 수정해야 하는지
```

를 알 수 있다.

따라서 개발 단계에서는 오류를 무조건 숨기기보다 **정확한 오류를 확인하고 원인을 해결하는 것**이 중요하다.

반면 실제 사용자에게 제공하는 프로그램에서는 예상 가능한 오류에 대해 적절한 안내를 제공해야 한다.

예를 들어 사용자에게

```text
ValueError
Traceback...
```

를 그대로 보여주는 것보다

```text
정수를 입력해주세요.
```

처럼 사용자가 이해할 수 있는 메시지를 제공하는 것이 좋다.

---

## 가장 중요하게 느낀 것

### 1. 파일 경로는 하드코딩하지 않기

프로젝트에서

```text
C:\...
D:\...
```

처럼 내 컴퓨터에만 존재하는 절대 경로를 직접 작성하면 다른 환경에서 문제가 발생한다.

**프로젝트의 루트 경로를 기준으로 상대적인 경로를 구성하는 습관**이 중요하다.

---

### 2. 오류 메시지를 읽는 능력이 중요하다

에러가 발생했다고 바로 코드를 다시 작성하기보다

```text
예외 이름
→ 발생한 줄
→ 오류 메시지
→ 실제 입력값
```

을 먼저 확인해야 한다.

이것만 잘해도 GPT 없이도 상당수의 오류 원인을 직접 파악할 수 있다.

---

### 3. `try-except`는 적절하게 사용해야 한다

`try-except`는 오류를 숨기는 도구가 아니라 **예상되는 예외 상황에 대응하기 위한 도구**다.

따라서 모든 코드를 무작정 감싸는 것이 아니라 문제가 발생할 가능성이 있는 **작은 단위의 로직**에 사용하는 것이 좋다.

---

### 4. 입력값 검증이 먼저다

사용자 입력에서는

```text
형식이 맞는가?
↓
자료형 변환이 가능한가?
↓
범위가 맞는가?
↓
실제 계산
```

순서로 생각해야 한다.

예외 처리만 믿지 말고 `if`문 등을 이용해 애초에 잘못된 값이 다음 단계로 넘어가지 않도록 하는 것도 중요하다.

---

## 실습·복습 체크리스트

* [ ] 절대 경로와 상대 경로의 차이를 설명할 수 있다.
* [ ] 프로젝트 루트 디렉터리가 무엇인지 설명할 수 있다.
* [ ] `pathlib.Path`의 역할을 이해했다.
* [ ] `resolve()`가 경로를 해석하는 역할을 한다는 것을 이해했다.
* [ ] `__file__`이 무엇인지 이해했다.
* [ ] `.py` 파일과 Jupyter Notebook의 실행 환경 차이를 이해했다.
* [ ] CSV 데이터는 일반적으로 pandas로 처리한다는 것을 기억했다.
* [ ] `Syntax Error`, `Runtime Error`, `Logical Error`의 차이를 설명할 수 있다.
* [ ] `ValueError`가 언제 발생하는지 이해했다.
* [ ] `ZeroDivisionError`가 언제 발생하는지 이해했다.
* [ ] `FileNotFoundError`가 언제 발생하는지 이해했다.
* [ ] `try-except`의 기본 흐름을 설명할 수 있다.
* [ ] `else`가 언제 실행되는지 설명할 수 있다.
* [ ] `finally`가 언제 실행되는지 설명할 수 있다.
* [ ] 여러 `except`를 사용할 수 있다는 것을 이해했다.
* [ ] 구체적인 예외를 먼저 처리해야 한다는 것을 이해했다.
* [ ] 모든 코드를 무작정 `try-except`로 감싸면 안 되는 이유를 이해했다.
* [ ] 입력값의 자료형과 범위를 각각 검증해야 한다는 것을 이해했다.
* [ ] `while True`, `break`, `continue`의 역할을 이해했다.

---

## 오늘의 핵심 한 줄

> **프로젝트에서는 파일 경로를 환경에 맞게 안전하게 관리하고, 오류는 무조건 숨기는 것이 아니라 예상되는 예외를 정확히 파악하고 적절하게 처리해야 한다.**
