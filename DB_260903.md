# 2026-09-03 TIL 데이터베이스 기본 개념과 DBMS 이해하기

## 오늘 배운 것

데이터베이스는 단순히 데이터를 저장하는 공간이 아니라 **데이터를 체계적으로 저장하고 관리하기 위한 시스템**이다.

이번 학습에서는 데이터베이스와 DBMS의 차이, PostgreSQL과 DBeaver의 역할, 테이블의 구조, PK와 FK, 테이블 간 관계, 정규화와 제약 조건 등 데이터베이스를 이해하기 위한 기본 개념을 정리했다.

---

## 핵심 개념

### 1. 데이터베이스와 DBMS

* **Data:** 의미가 있는 개별 데이터
* **Database:** 데이터를 체계적으로 저장하고 관리하는 공간
* **DBMS:** 데이터베이스를 생성하고 데이터를 관리하는 프로그램

예를 들어 PostgreSQL은 DBMS이고, DBeaver는 PostgreSQL에 접속하여 SQL을 작성하고 실행하는 도구이다.

```text
DBeaver
   ↓ SQL 전달
PostgreSQL
   ↓ SQL 실행
Database
   ↓
결과 반환
   ↓
DBeaver
```

---

### 2. 데이터베이스의 기본 구조

데이터베이스는 여러 단계의 구조로 데이터를 관리한다.

```text
DBMS
 └── Database
      └── Schema
           └── Table
                ├── Column
                └── Row
```

* **Database:** 데이터를 관리하는 논리적인 공간
* **Schema:** 데이터베이스 내부를 구분하는 영역
* **Table:** 실제 데이터를 저장하는 공간
* **Column:** 데이터의 속성이나 항목
* **Row:** 하나의 데이터 기록

PostgreSQL에서는 기본적으로 `public` 스키마를 사용할 수 있다.

---

### 3. Table의 구조

테이블은 행(Row)과 열(Column)로 구성된다.

* **Column:** 어떤 종류의 데이터를 저장할 것인지 정의
* **Row:** 하나의 실제 데이터
* **Cell:** Row와 Column이 만나는 하나의 값
* **Tuple:** Row를 의미하는 데이터베이스 용어

예를 들어 학생 테이블이라면

```text
student_id | name | age
-----------|------|----
1          | 철수 | 20
2          | 영희 | 21
```

`student_id`, `name`, `age`는 Column이고, 학생 한 명의 정보가 하나의 Row가 된다.

---

### 4. Primary Key와 Foreign Key

**Primary Key(PK)**는 테이블의 각 Row를 고유하게 식별하기 위한 값이다.

* 중복될 수 없음
* NULL을 가질 수 없음
* 각 데이터를 구분하는 식별자 역할

**Foreign Key(FK)**는 다른 테이블의 PK 등을 참조하여 테이블 사이의 관계를 연결한다.

```text
학생 테이블
student_id (PK)

      ↓ 참조

수강신청 테이블
student_id (FK)
```

즉, **PK는 자신의 테이블에서 데이터를 식별하고 FK는 다른 테이블과 연결하는 역할**을 한다.

---

### 5. PK와 UNIQUE의 차이

둘 다 중복을 방지할 수 있지만 역할이 다르다.

* **PK:** 테이블의 대표적인 식별자
* **UNIQUE:** 특정 컬럼의 값이 중복되지 않도록 제한

예를 들어 학생의 내부 번호는 PK로 사용하고, 이메일 주소는 UNIQUE로 설정할 수 있다.

---

### 6. 테이블 간의 관계

테이블은 서로 관계를 가질 수 있다.

* **1:1:** 하나의 데이터가 하나의 데이터와 연결
* **1:N:** 하나의 데이터가 여러 데이터와 연결
* **N:M:** 여러 데이터가 여러 데이터와 연결

특히 N:M 관계는 중간 테이블을 이용해 관리한다.

```text
Student 1 ─── N Enrollment N ─── 1 Course
```

학생과 강의를 직접 연결하는 것이 아니라 `Enrollment`라는 수강신청 테이블을 통해 관계를 관리할 수 있다.

---

### 7. 정규화

**정규화(Normalization)**는 데이터의 중복을 줄이고 일관성을 유지하기 위해 데이터를 적절한 테이블로 분리하는 방법이다.

예를 들어 학생, 강사, 강의, 수강신청 정보를 하나의 테이블에 모두 저장하면 같은 정보가 반복될 수 있다.

따라서

```text
Student
Instructor
Course
Enrollment
```

처럼 의미에 따라 테이블을 나누고 서로 관계를 연결한다.

정규화를 통해 데이터 중복과 추가·수정·삭제 과정에서 발생하는 문제를 줄일 수 있다.

---

### 8. 제약 조건과 데이터 타입

데이터베이스에는 데이터가 지켜야 할 규칙을 설정할 수 있다.

* `PRIMARY KEY` → 고유한 식별자
* `UNIQUE` → 중복 방지
* `FOREIGN KEY` → 테이블 간 관계 유지
* `NOT NULL` → 반드시 값이 존재하도록 설정

또한 저장하려는 데이터에 맞는 자료형을 지정해야 한다.

* `INTEGER`, `BIGINT` → 숫자
* `VARCHAR` → 문자열
* `DATE`, `TIMESTAMP` → 날짜와 시간
* `BOOLEAN` → 참/거짓

---

### 9. CRUD

데이터베이스에서 가장 기본적으로 수행하는 작업이 **CRUD**이다.

* **Create** → 생성 → `INSERT`
* **Read** → 조회 → `SELECT`
* **Update** → 수정 → `UPDATE`
* **Delete** → 삭제 → `DELETE`

결국 데이터베이스는 데이터를 **생성하고, 조회하고, 수정하고, 삭제하기 위해 사용한다.**

---

### 10. DDL과 DML

**DDL(Data Definition Language)**은 데이터베이스의 구조를 정의하고 변경한다.

```sql
CREATE
ALTER
DROP
```

**DML(Data Manipulation Language)**은 데이터를 다룬다.

```sql
INSERT
SELECT
UPDATE
DELETE
```

즉, **DDL은 데이터베이스의 구조를 관리하고 DML은 데이터를 관리한다.**

---

### 11. 데이터 조회와 정렬

`WHERE`를 사용하면 특정 조건에 맞는 데이터만 조회할 수 있다.

```sql
SELECT *
FROM students
WHERE age >= 20;
```

`ORDER BY`를 사용하면 원하는 기준으로 데이터를 정렬할 수 있다.

```sql
SELECT *
FROM students
ORDER BY age;
```

특별히 `ORDER BY`를 사용하지 않았다면 조회 결과의 순서가 항상 일정하다고 보장할 수 없다.

---

### 12. AI 시대의 데이터베이스 설계

AI를 이용하면 SQL이나 테이블 구조를 쉽게 만들 수 있지만, **AI가 만든 결과를 그대로 사용하는 것은 위험하다.**

다음 내용을 직접 확인해야 한다.

* 하나의 Row가 무엇을 의미하는가?
* Primary Key가 적절한가?
* Foreign Key가 필요한 곳에 설정되어 있는가?
* 테이블 간 관계가 올바른가?
* 데이터가 불필요하게 중복되어 있지 않은가?
* 자료형과 NULL 설정이 적절한가?
* UNIQUE 등의 제약 조건이 필요한가?

결국 AI 시대에는 SQL 문법만 작성하는 능력보다 **데이터의 의미와 구조를 이해하고 AI가 만든 결과를 검증하는 능력**이 중요하다.

---

## 오늘의 핵심 한 줄

> **데이터베이스는 데이터를 단순히 저장하는 것이 아니라, 테이블과 관계를 적절하게 설계하고 제약 조건을 적용하여 데이터를 일관되게 관리하는 시스템이다.**
