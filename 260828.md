# 2026-08-28 TIL 클래스와 객체, 자료구조 선택 그리고 프로그램 설계

##  오늘 배운 것

오늘은 파이썬에서 중요한 개념인 **클래스(Class)와 객체(Object)**를 배웠다.

단순히 클래스 문법만 배우는 것이 아니라, 실제 프로그램을 만들 때

* 데이터를 어떻게 구조화할지
* 어떤 자료구조를 사용할지
* 기능을 어떻게 나눌지
* 프로그램을 어떻게 설계할지

까지 함께 배웠다.

오늘 배운 전체 흐름은 다음과 같다.

```text
클래스
↓
객체 생성
↓
속성과 메소드 사용
↓
여러 객체 생성
↓
객체를 리스트에 저장
↓
반복문으로 객체 처리
↓
프로그램 요구사항 정의
↓
Breakdown으로 문제 세분화
↓
IPO(Input → Process → Output) 설계
↓
자료구조 결정
↓
함수로 기능 분리
```

---

# 1. 클래스(Class)란?

클래스는 한마디로 **설계도**이다.

대표적으로 다음과 같이 비유할 수 있다.

```text
클래스 = 집의 설계도
객체 = 설계도를 이용해서 만든 실제 집
```

또는

```text
클래스 = 붕어빵 틀
객체 = 붕어빵 틀로 만든 붕어빵
```

하나의 클래스만 만들어 놓으면 그 클래스를 이용해서 여러 개의 객체를 만들 수 있다.

```text
Student 클래스
 ├── student1
 ├── student2
 ├── student3
 └── student4
```

즉, **클래스 하나를 이용해서 같은 구조를 가진 객체를 여러 개 만들 수 있다.**

---

# 2. 객체(Object)란?

객체는 클래스라는 설계도를 이용해서 실제로 만들어진 것이다.

예를 들어:

```python
class Student:
    ...
```

라는 클래스가 있다면,

```python
student1 = Student("민수", 85)
student2 = Student("지형", 92)
```

이렇게 만든 `student1`, `student2`가 객체이다.

두 객체는 같은 `Student` 클래스에서 만들어졌지만 서로 다른 객체이다.

각 객체는 서로 다른 메모리 영역을 사용한다.

따라서:

```python
student1 == student2
```

처럼 비교하면 서로 다른 객체이기 때문에 일반적으로 `False`가 나온다.

---

# 3. 객체를 만드는 이유

객체를 사용하는 이유는 크게 두 가지를 관리하기 위해서이다.

## ① 속성(Attribute / Property)

객체가 가지고 있는 데이터이다.

예를 들어 학생이라면:

```text
이름
점수
나이
```

등이 속성이 될 수 있다.

## ② 기능(Method)

객체가 할 수 있는 행동이다.

예:

```text
학생 정보 출력
합격 여부 판단
학생 정보 수정
```

클래스 안에 정의한 함수는 **메소드(Method)**라고 한다.

정리하면:

```text
객체
 ├── 속성(Attribute / Property)
 └── 기능(Method)
```

---

# 4. 클래스의 기본 문법

클래스를 정의할 때는 `class` 키워드를 사용한다.

```python
class Student:
    def __init__(self, name, score):
        self.name = name
        self.score = score

    def show_result(self):
        print(f"{self.name}: {self.score}점")
```

클래스를 만들 때 기본적인 형태를 익혀두는 것이 중요하다.

```python
class 클래스이름:
    def __init__(self, ...):
        self.속성 = 값
```

특히 처음에는 이 구조를 직접 여러 번 작성해보면서 익숙해지는 것이 좋다.

---

# 5. `__init__()`과 생성자

```python
def __init__(self, name, score):
```

여기서 `__init__()`은 **생성자(Constructor)**라고 한다.

객체를 만들 때 전달받은 값을 객체의 속성으로 설정하는 역할을 한다.

예:

```python
class Student:
    def __init__(self, name, score):
        self.name = name
        self.score = score
```

그리고 객체를 생성한다.

```python
student1 = Student("민수", 85)
```

그러면 전달한 값이 생성자를 통해 객체에 들어간다.

```text
name → "민수"
score → 85
```

그리고

```python
self.name = name
self.score = score
```

를 통해 객체의 속성이 된다.

---

# 6. `self`란?

클래스에서 가장 많이 헷갈리는 부분 중 하나이다.

```python
def __init__(self, name, score):
```

여기서 `self`는 **현재 객체 자신**을 의미한다.

예를 들어:

```python
student1 = Student("민수", 85)
```

라고 하면 `self`는 `student1` 객체를 가리킨다고 이해하면 된다.

우리가 객체를 만들 때 직접 `self`를 전달하지는 않는다.

```python
Student("민수", 85)
```

우리가 직접 전달하는 값은:

```text
"민수"
85
```

이다.

`self`는 객체 자신을 다루기 위해 사용하는 특별한 매개변수이다.

---

# 7. 속성(Attribute) 사용하기

생성자에서 만든 속성은 객체를 통해 접근할 수 있다.

```python
student1.name
student1.score
```

여기서 가장 중요한 것은 **점(`.`)**이다.

```text
객체.속성
```

형태로 접근한다.

예:

```python
print(student1.name)
print(student1.score)
```

출력:

```text
민수
85
```

---

# 8. 속성값 변경하기

속성은 값을 읽는 것뿐만 아니라 변경할 수도 있다.

```python
student1.score = 90
```

그러면 기존의 85점이 90점으로 변경된다.

즉:

```python
student1.score
```

→ 속성값 읽기

```python
student1.score = 90
```

→ 속성값 변경하기

일반적인 객체의 속성은 **읽거나 수정할 수 있다.**

---

# 9. 메소드(Method)란?

클래스 안에 정의된 함수가 메소드이다.

```python
class Student:
    def show_result(self):
        print(f"{self.name}: {self.score}점")
```

`show_result()`는 `Student` 클래스의 메소드이다.

메소드를 호출할 때도 점(`.`)을 사용한다.

```python
student1.show_result()
```

즉:

```text
객체.메소드()
```

형태이다.

---

# 10. 점(`.`)을 사용하는 것이 중요한 이유

객체 변수를 만들었다면 가장 먼저 생각할 것이 **점(`.`)**이다.

```python
student1.
```

객체 뒤에 점을 찍으면 해당 객체에서 사용할 수 있는 속성과 메소드를 확인할 수 있다.

객체를 사용하는 목적은 크게 두 가지다.

```text
1. 속성값을 읽거나 변경한다.
2. 메소드를 호출한다.
```

그리고 둘 다 기본적으로:

```text
객체.속성
객체.메소드()
```

형태로 접근한다.

따라서 개발할 때 객체를 만들었다면

> **"일단 점을 찍어보자."**

라는 습관을 들이는 것이 좋다.

---

# 11. 하나의 클래스로 여러 객체 만들기

클래스 하나로 여러 객체를 만들 수 있다.

```python
student1 = Student("민수", 85)
student2 = Student("지형", 92)
```

각 객체는 서로 다른 데이터를 가지고 있다.

```python
student1.show_result()
student2.show_result()
```

출력:

```text
민수: 85점
지형: 92점
```

이것이 클래스와 객체를 사용하는 가장 기본적인 이유이다.

**하나의 설계도로 같은 구조의 객체를 여러 개 만들 수 있다.**

---

# 12. 딕셔너리와 클래스

학생 정보를 딕셔너리로 관리할 수도 있다.

```python
student1 = {
    "name": "민수",
    "score": 85
}
```

이 방법도 잘못된 방법은 아니다.

하지만 같은 형태의 데이터가 계속 반복된다면 불편해질 수 있다.

학생이 100명이라면 각각의 데이터를 계속 딕셔너리 형태로 관리해야 한다.

이처럼:

> **같은 구조의 데이터와 기능이 반복적으로 필요하다면 클래스를 사용하는 것을 검토할 수 있다.**

---

# 13. 클래스에 속성 추가하기

필요하다면 속성을 추가할 수 있다.

예를 들어 학생의 나이를 추가한다면:

```python
class Student:
    def __init__(self, name, score, age):
        self.name = name
        self.score = score
        self.age = age
```

객체를 생성할 때도 세 가지 값을 전달한다.

```python
student1 = Student("민수", 85, 20)
```

그리고:

```python
print(student1.name)
print(student1.score)
print(student1.age)
```

처럼 사용할 수 있다.

---

# 14. 메소드로 합격 여부 판단하기

객체의 속성을 이용해서 기능을 만들 수도 있다.

예를 들어 60점 이상이면 합격이라고 해보자.

```python
class Student:
    def __init__(self, name, score):
        self.name = name
        self.score = score

    def is_passed(self):
        return self.score >= 60
```

`is_passed()`는 점수를 확인해서 `True` 또는 `False`를 반환한다.

예:

```python
student1 = Student("홍길동", 45)

print(student1.is_passed())
```

결과:

```text
False
```

75점이라면:

```text
True
```

---

# 15. Boolean과 `if` 연결하기

`is_passed()`는 `True` 또는 `False`를 반환하므로 `if`와 바로 연결할 수 있다.

```python
if student1.is_passed():
    result = "당당히 합격"
else:
    result = "아쉽지만 탈락"
```

여기서 중요한 것은 굳이:

```python
if student1.is_passed() == True:
```

라고 작성하지 않아도 된다는 것이다.

`is_passed()` 자체가 이미 `True` 또는 `False`를 반환하기 때문에:

```python
if student1.is_passed():
```

라고 작성하면 된다.

---

# 16. `is_`로 시작하는 메소드

`is_passed()`처럼 `is_`로 시작하는 이름은 보통 어떤 상태를 확인해서 **Boolean 값(True/False)을 반환하는 메소드**로 만들 때 많이 사용한다.

예:

```python
is_passed()
is_valid()
is_empty()
is_available()
```

`is`는 "~인가?"라는 질문처럼 생각할 수 있다.

```text
is_passed()
→ 합격인가?

True
→ 맞다

False
→ 아니다
```

따라서 이름만 보고도 메소드가 어떤 역할을 하는지 어느 정도 예상할 수 있다.

---

# 17. 객체를 리스트에 저장하기

학생이 두 명일 때:

```python
student1
student2
```

처럼 각각 관리할 수도 있다.

하지만 학생이 10명, 100명이라면 하나씩 따로 관리하는 것은 번거롭다.

이때 객체를 리스트에 넣을 수 있다.

```python
students = [
    Student("민수", 75),
    Student("강감찬", 59),
    Student("홍길동", 61)
]
```

이제 `students` 리스트 안에 `Student` 객체가 여러 개 들어 있다.

```text
students
 ├── Student 객체
 ├── Student 객체
 └── Student 객체
```

---

# 18. 객체 리스트를 반복문으로 처리하기

리스트 안에 객체가 여러 개 있으므로 `for`문을 이용해서 하나씩 꺼낼 수 있다.

```python
for student in students:
    student.show_result()
```

여기서 `student`는 리스트에서 하나씩 꺼낸 객체이다.

즉:

```text
students
   ↓
객체 하나 꺼내기
   ↓
student에 저장
   ↓
student.show_result()
   ↓
다음 객체
```

이 과정을 리스트의 모든 객체에 대해 반복한다.

객체가 10명이든 100명이든 같은 코드를 사용할 수 있다.

---

# 19. 객체 추가하기

객체 하나를 리스트에 추가할 때는 `append()`를 사용할 수 있다.

```python
students.append(Student("강감찬", 61))
```

여러 개를 추가할 때는 `extend()`를 사용할 수 있다.

```python
students.extend([
    Student("철수", 70),
    Student("영희", 80)
])
```

정리하면:

```text
append()
→ 하나 추가

extend()
→ 여러 개 추가
```

---

# 20. 클래스에서 발생할 수 있는 에러

클래스를 사용한다고 에러가 발생하지 않는 것은 아니다.

## AttributeError

존재하지 않는 속성이나 메소드를 사용했을 때 발생할 수 있다.

```python
student1.abc
```

`abc`라는 속성이 없다면 `AttributeError`가 발생할 수 있다.

여기서:

```text
Attribute
→ 속성
```

이다.

---

## TypeError

자료형이 잘못되었거나 함수나 메소드를 잘못된 방식으로 사용했을 때 발생할 수 있다.

클래스와 객체를 사용할 때도 잘못된 타입을 전달하거나 호출 방법을 잘못 사용하면 발생할 수 있다.

---

# 21. 코드는 위에서부터 실행된다

클래스와 객체를 사용하는 프로그램에서는 **필요한 코드가 먼저 실행되어 있어야 한다.**

예를 들어:

```python
class Student:
    ...
```

라는 클래스가 만들어져 있어야

```python
student1 = Student(...)
```

를 실행할 수 있다.

컴퓨터를 껐다가 다시 실행한다면 필요한 코드들을 다시 처음부터 실행해야 한다.

즉:

```text
클래스 정의
↓
객체 생성
↓
메소드 호출
```

순서를 생각해야 한다.

---

# 22. 프로그램을 한 번에 만들지 않기

오늘 클래스와 함께 가장 중요하게 배운 내용이다.

프로그램을 만들 때 처음부터 모든 코드를 작성하려고 하면 어렵다.

예를 들어:

> "학생 관리 프로그램 만들어줘."

라고만 하면 결정되지 않은 것이 너무 많다.

```text
학생 정보를 어떻게 저장할 것인가?
학생은 몇 명인가?
이름과 점수만 필요한가?
합격 기준은 몇 점인가?
결과는 어떻게 출력할 것인가?
파일을 사용할 것인가?
GUI가 필요한가?
```

이런 것들이 결정되지 않았기 때문이다.

---

# 23. AI에게 요청하기 전에 사람이 문제 범위를 정해야 한다

AI가 코드를 잘 만들어주더라도 **문제의 범위가 명확하지 않으면 원하는 결과가 나오기 어렵다.**

따라서 AI에게 코드를 요청하기 전에 먼저 사람이:

```text
무엇을 만들 것인지
무엇이 필요한지
무엇을 하지 않을 것인지
어떤 데이터가 필요한지
어떤 결과가 나와야 하는지
```

를 정해야 한다.

즉:

> **AI에게 코드를 작성시키기 전에 사람이 문제를 먼저 정의해야 한다.**

---

# 24. 요구사항(Requirements)은 문제 정의다

요구사항은 프로그램이 무엇을 해야 하는지를 정리한 것이다.

예를 들어 학생 관리 프로그램이라면:

```text
학생 이름과 점수를 저장한다.
여러 학생을 관리한다.
학생 정보를 출력한다.
점수를 기준으로 합격 여부를 판단한다.
```

이렇게 구체적으로 적으면 프로그램의 범위가 점점 명확해진다.

---

# 25. Breakdown으로 문제 세분화하기

큰 문제를 한 번에 해결하려고 하지 말고 작은 문제로 나눈다.

이를 **Breakdown(브레이크다운)**이라고 한다.

예:

```text
학생 관리 프로그램
        ↓
학생 정보 입력
        ↓
학생 정보 저장
        ↓
학생 점수 처리
        ↓
합격 여부 판단
        ↓
결과 출력
```

여기서 다시 더 작은 단위로 나눌 수도 있다.

핵심은:

> **세분화하고, 또 세분화한다.**

이렇게 하면 처음에는 막막했던 프로그램도 하나씩 처리할 수 있는 작업으로 바뀐다.

---

# 26. IPO(Input → Process → Output)

프로그램을 설계할 때 중요한 구조가 **IPO**이다.

```text
I = Input
P = Process
O = Output
```

즉:

```text
입력 → 처리 → 출력
```

이다.

예를 들어 학생 관리 프로그램이라면:

### Input

```text
학생 이름
학생 점수
```

### Process

```text
학생 정보 저장
합격 여부 판단
```

### Output

```text
학생 이름
학생 점수
합격 / 탈락
```

프로그램을 만들기 전에 이 구조를 먼저 생각하면 코드 작성이 쉬워진다.

---

# 27. 코드를 작성하기 전에 데이터의 모양을 생각하기

프로그램을 만들기 전에 중요한 것이 **자료구조**이다.

즉:

> "내가 다뤄야 하는 데이터는 어떤 모양인가?"

를 먼저 생각해야 한다.

기본 자료형:

```text
str
int
float
bool
```

여러 데이터를 묶는 자료구조:

```text
list
dict
tuple
set
```

---

# 28. 자료구조 선택 기준

데이터의 모양에 따라 자료구조를 선택한다.

```text
값 하나
→ 변수

여러 값을 순서대로 저장
→ 리스트

키 : 값 형태
→ 딕셔너리

변경하지 않을 데이터 묶음
→ 튜플

중복을 허용하지 않는 데이터 묶음
→ set
```

특히 파이썬에서 리스트는 매우 기본적인 자료구조이므로 잘 사용할 수 있어야 한다.

다른 언어의 **배열(Array)**과 비슷한 역할을 한다.

---

# 29. 리스트 + 딕셔너리 구조

예를 들어 지출 내역을 관리한다고 해보자.

지출 하나는:

```python
{
    "date": "2026-08-30",
    "category": "식비",
    "amount": 15000
}
```

처럼 표현할 수 있다.

키와 값으로 구성되어 있으므로 딕셔너리이다.

그런데 지출 내역이 여러 개라면?

```python
expenses = [
    {
        "date": "2026-08-30",
        "category": "식비",
        "amount": 15000
    },
    {
        "date": "2026-08-30",
        "category": "교통",
        "amount": 3000
    }
]
```

이렇게 리스트 안에 딕셔너리를 넣을 수 있다.

구조를 보면:

```text
리스트
 ├── 딕셔너리
 ├── 딕셔너리
 └── 딕셔너리
```

이다.

---

# 30. 데이터를 엑셀처럼 생각하기

위와 같은 구조는 엑셀의 표처럼 생각하면 이해하기 쉽다.

```text
date        category      amount
---------------------------------
8/30        식비           15,000
8/30        교통            3,000
```

각 행은 하나의 데이터이고,

각 열은 하나의 속성이다.

따라서:

```text
한 건의 데이터
→ 딕셔너리

여러 건의 데이터
→ 리스트
```

라고 생각할 수 있다.

---

# 31. 자료구조를 선택하는 능력이 중요하다

코딩을 잘한다는 것은 단순히 문법을 많이 외우는 것이 아니다.

주어진 데이터를 보고:

> **"이 데이터는 어떤 구조로 관리하는 것이 적절할까?"**

를 판단할 수 있어야 한다.

예:

```text
학생 한 명
→ 객체

학생 여러 명
→ 리스트 + 객체

지출 한 건
→ 딕셔너리

지출 여러 건
→ 리스트 + 딕셔너리
```

이런 패턴을 반복해서 연습해야 한다.

---

# 32. 함수(Function)는 기능이다

함수는 **특정 기능을 재사용하기 위해 만든다.**

```python
def add(a, b):
    return a + b
```

함수의 장점은 같은 기능을 여러 곳에서 사용할 수 있다는 것이다.

```text
공통으로 사용하는 기능
        ↓
함수로 만든다.
        ↓
필요할 때 호출한다.
```

---

# 33. 함수와 메소드

함수와 메소드의 관계도 중요하다.

```text
함수(Function)
        ↓
클래스 안에 정의되면
        ↓
메소드(Method)
```

예:

```python
def add(a, b):
    return a + b
```

일반적인 함수이다.

반면:

```python
class Student:
    def show_result(self):
        ...
```

클래스 안에 정의되어 있기 때문에 메소드라고 한다.

---

# 34. 함수에서는 `return`이 중요하다

함수는 단순히 화면에 출력하기 위한 것이 아니라 **처리한 결과를 반환하는 역할**을 많이 한다.

기본 구조는:

```text
Input
↓
Process
↓
Return
```

예:

```python
def add(a, b):
    result = a + b
    return result
```

그리고 함수 바깥에서:

```python
result = add(10, 20)
print(result)
```

처럼 사용할 수 있다.

즉:

```text
함수 내부
→ 계산 및 처리

return
→ 결과를 밖으로 전달

함수 외부
→ 반환된 결과 사용
```

---

# 35. 여러 값을 반환할 때

파이썬에서는 여러 값을 반환하는 것처럼 작성할 수 있다.

```python
def get_student():
    return "민수", 85
```

이때 반환되는 값은 튜플 형태로 묶인다.

```python
name, score = get_student()
```

즉:

```text
"민수", 85
↓
튜플 형태
```

로 반환된다고 이해하면 된다.

---

# 36. 코드 작성과 테스트 방법

코드를 한꺼번에 많이 작성하지 않는 것도 중요하다.

좋은 방법은:

```text
한 줄 작성
↓
실행
↓
확인

몇 줄 작성
↓
실행
↓
확인

다음 기능 작성
↓
실행
↓
확인
```

이다.

100줄을 작성하고 한 번에 실행하면 에러가 여러 개 발생했을 때 어디서 문제가 생겼는지 찾기 어렵다.

반대로 짧게 작성하고 계속 실행하면 문제가 발생한 지점을 쉽게 찾을 수 있다.

---

# 37. 오늘 배운 내용을 실제 개발에 연결하기

오늘 배운 내용을 실제 프로그램 개발 과정으로 연결하면 다음과 같다.

```text
① 요구사항 정의
↓
② 문제의 범위 결정
↓
③ Breakdown
↓
④ IPO 정리
↓
⑤ 데이터의 모양 파악
↓
⑥ 자료구조 선택
↓
⑦ 필요한 기능 정리
↓
⑧ 함수로 기능 분리
↓
⑨ 반복되는 구조가 있다면 클래스 설계
↓
⑩ 객체 생성
↓
⑪ 객체를 리스트 등에 저장
↓
⑫ 반복문으로 처리
↓
⑬ 조금씩 실행하면서 테스트
```

---

# 38. AI와 함께 코딩할 때

AI에게 코드를 요청할 때도 오늘 배운 설계 방법을 그대로 적용할 수 있다.

처음부터:

```text
학생 관리 프로그램 만들어줘.
```

라고 요청하는 것보다,

```text
목적:
학생 정보를 관리하는 콘솔 프로그램

요구사항:
1. 학생 이름과 점수를 저장한다.
2. 여러 학생을 관리한다.
3. 학생 정보를 출력한다.
4. 60점 이상이면 합격으로 판단한다.

Input:
학생 이름, 점수

Process:
학생 객체 생성
합격 여부 판단

Output:
학생 이름, 점수, 합격 여부
```

처럼 먼저 정리하는 것이 훨씬 좋다.

그다음 AI에게 세부 기능별로 코드를 요청한다.

---

# 39. 오늘 가장 중요하게 느낀 것

오늘 가장 중요한 것은 **클래스 문법 자체보다 프로그램을 바라보는 방법**이다.

특히 다음 네 가지를 기억해야 한다.

### ① 클래스는 설계도다.

```text
클래스 → 객체를 만들기 위한 설계도
```

### ② 객체는 데이터와 기능을 함께 가진다.

```text
객체
├── 속성
└── 메소드
```

### ③ 데이터를 보면 자료구조를 생각해야 한다.

```text
하나
→ 변수

여러 개
→ 리스트

키 : 값
→ 딕셔너리

같은 구조의 여러 객체
→ 리스트 + 객체
```

### ④ 프로그램은 바로 코딩하지 않는다.

```text
문제 정의
→ 요구사항
→ Breakdown
→ IPO
→ 자료구조
→ 기능
→ 코드
```

이 순서로 생각해야 한다.

---

# 40. 실습·복습 체크리스트

## 클래스와 객체

* [ ] 클래스가 설계도라는 것을 설명할 수 있다.
* [ ] 객체와 클래스의 관계를 설명할 수 있다.
* [ ] 하나의 클래스에서 여러 객체를 만들 수 있다.
* [ ] `class` 문법을 직접 작성할 수 있다.
* [ ] `__init__()`이 생성자라는 것을 이해했다.
* [ ] `self`가 객체 자신을 의미한다는 것을 이해했다.
* [ ] 생성자에서 객체의 속성을 설정할 수 있다.

## 속성과 메소드

* [ ] Attribute와 Property가 속성을 의미한다는 것을 이해했다.
* [ ] `객체.속성` 형태로 값을 읽을 수 있다.
* [ ] `객체.속성 = 값` 형태로 값을 변경할 수 있다.
* [ ] 클래스 안에 메소드를 만들 수 있다.
* [ ] `객체.메소드()` 형태로 메소드를 호출할 수 있다.
* [ ] 객체를 사용할 때 점(`.`)으로 접근한다는 것을 기억했다.

## 객체 관리

* [ ] 객체를 리스트에 저장할 수 있다.
* [ ] `append()`로 객체 하나를 추가할 수 있다.
* [ ] `extend()`로 여러 객체를 추가할 수 있다.
* [ ] `for`문으로 객체 리스트를 순회할 수 있다.
* [ ] 반복문 안에서 객체의 메소드를 호출할 수 있다.

## 조건문

* [ ] `True`와 `False`를 이해했다.
* [ ] Boolean을 `if` 조건식에 사용할 수 있다.
* [ ] `is_passed()` 같은 Boolean 반환 메소드를 만들 수 있다.
* [ ] `if student.is_passed():` 형태를 이해했다.

## 자료구조

* [ ] `str`, `int`, `float`, `bool`을 구분할 수 있다.
* [ ] 리스트의 기본적인 용도를 이해했다.
* [ ] 딕셔너리의 키-값 구조를 이해했다.
* [ ] 리스트 안에 딕셔너리를 넣을 수 있다.
* [ ] 리스트 안에 객체를 넣을 수 있다.
* [ ] 데이터의 모양에 따라 자료구조를 선택해야 한다는 것을 이해했다.

## 프로그램 설계

* [ ] 요구사항이 무엇인지 설명할 수 있다.
* [ ] 요구사항이 문제 정의와 연결된다는 것을 이해했다.
* [ ] Breakdown의 의미를 이해했다.
* [ ] 큰 문제를 작은 문제로 나눌 수 있다.
* [ ] IPO(Input → Process → Output)를 설명할 수 있다.
* [ ] 코드를 작성하기 전에 데이터 구조를 먼저 생각할 수 있다.

## 함수

* [ ] 함수가 재사용 가능한 기능이라는 것을 이해했다.
* [ ] 함수와 메소드의 차이를 이해했다.
* [ ] `return`의 역할을 이해했다.
* [ ] 여러 값을 반환할 때 튜플이 사용될 수 있다는 것을 이해했다.

---

# 41. 오늘의 핵심 한 줄

> **코딩은 바로 코드를 작성하는 것이 아니라, 먼저 문제를 정의하고 → 작게 나누고 → 입력·처리·출력을 정리하고 → 데이터 구조와 기능을 설계한 뒤 코드를 작성하는 것이다.**

그리고 클래스는 다음처럼 기억한다.

```text
클래스 = 설계도
객체 = 설계도로 만든 실제 객체
속성 = 객체가 가진 데이터
메소드 = 객체가 할 수 있는 기능
```

### 전체 흐름

```text
문제 정의
↓
요구사항 정리
↓
Breakdown
↓
IPO 설계
↓
자료구조 결정
↓
함수로 기능 분리
↓
필요하면 클래스 설계
↓
객체 생성
↓
객체를 리스트로 관리
↓
반복문으로 처리
↓
조금씩 실행하며 테스트
```
