# 2026-08-31 TIL — AI 코드 검증·디버깅·개선하기

## 오늘 배운 것

AI가 만든 코드는 완성품이 아니라 **초안**이므로, 실행 결과만 믿지 않고 사람이 직접 검증해야 한다.

핵심 과정은 다음과 같다.

> **코드 읽기 → 요구사항 비교 → 테스트 → 문제 재현 → 범위 축소 → 최소 수정 → 재검증 → 개선**

---

## 핵심 개념

### 1. 실행된다 ≠ 올바른 코드

코드에 문법 오류가 없어도 요구사항과 다르게 동작할 수 있다.

```python
if order_amount > 50000:
```

요구사항이 **50,000원 이상**이라면 `>`가 아니라 `>=`가 되어야 한다.

```python
if order_amount >= 50000:
```

따라서 **코드가 실행되는지보다 요구사항대로 동작하는지가 중요하다.**

---

### 2. AI 코드는 실행 전에 먼저 읽기

코드를 볼 때 다음을 확인한다.

* **입력:** 어떤 값을 받는가?
* **처리:** 조건, 계산, 반복이 어떻게 이루어지는가?
* **출력:** 무엇을 반환하거나 출력하는가?
* **낯선 문법:** 내가 아직 배우지 않은 내용이 있는가?

특히 파일 삭제, 네트워크 요청, 외부 패키지 설치 같은 코드는 역할을 이해한 후 실행한다.

---

### 3. 정상값·경계값·비정상값 테스트

테스트 입력은 크게 세 가지로 나눌 수 있다.

* **정상값:** 일반적으로 사용하는 값
* **경계값:** 조건이 바뀌는 기준 주변의 값
* **비정상값:** 예상하지 않은 입력

기준이 `50000`이라면 특히

```text
49999
50000
50001
```

을 테스트한다.

그리고 **실행 전에 예상 결과를 먼저 작성한 후 실제 결과와 비교한다.**

---

### 4. 문제가 생기면 재현하고 범위를 줄이기

문제가 발견되었다고 바로 수정하지 않는다.

```text
문제 발견
→ 문제 재현
→ 문제가 있는 함수로 범위 축소
→ 원인 확인
→ 최소 수정
→ 다시 테스트
```

전체 프로그램을 확인하는 것보다 문제가 발생한 함수만 확인하면 원인을 찾기 쉽다.

---

### 5. AI에게 디버깅 요청하기

AI에게 다음 4가지를 함께 제공한다.

1. 현재 코드
2. 문제 입력
3. 실제 결과
4. 예상 결과

그리고 전체 코드를 새로 작성하도록 하기보다 **원인을 설명하고 최소 수정 부분만 알려 달라고 요청**한다.

---

### 6. 수정 후에는 이전 정상값도 재검증

문제가 발생했던 입력만 테스트하면 안 된다.

수정 후에는

* 실패했던 값
* 기존 정상값
* 경계값

을 모두 다시 확인해야 한다.

수정 과정에서 기존에 정상적으로 작동하던 기능이 깨질 수 있기 때문이다.

---

### 7. 디버깅과 개선은 다르다

**디버깅**

→ 잘못된 동작을 올바르게 수정하는 것

**개선**

→ 동작은 유지하면서 가독성이나 구조를 좋게 만드는 것

따라서 **정확성을 먼저 확인한 후 개선**해야 한다.

AI가 제안한 개선 코드도 반드시 다시 테스트한다.

---

## 실습·복습 체크리스트

* [ ] AI 코드를 실행하기 전에 먼저 읽기
* [ ] 입력·처리·출력 구분하기
* [ ] 요구사항과 코드 비교하기
* [ ] 정상값·경계값·비정상값 테스트하기
* [ ] 예상 결과와 실제 결과 비교하기
* [ ] 문제 재현하기
* [ ] 문제 범위를 작은 함수로 좁히기
* [ ] AI에게 코드·입력·실제 결과·예상 결과 전달하기
* [ ] 최소 수정하기
* [ ] 수정 후 기존 정상값까지 다시 테스트하기
* [ ] 정확성 확인 후 코드 개선하기
* [ ] AI의 수정·개선안도 다시 검증하기

---

## 오늘의 핵심 한 줄

> **AI가 만든 코드는 그대로 믿지 말고, 요구사항과 테스트를 기준으로 사람이 직접 검증한 후 최소 수정하고 다시 확인해야 한다.**
