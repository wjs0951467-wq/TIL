# 2026-09-02 TIL 데이터 분석의 기본 관점과 AI를 활용한 데이터 분석 방법

## 오늘 배운 내용

오늘은 데이터를 단순히 가공하는 방법보다 **데이터 분석을 어떤 순서와 기준으로 해야 하는지**에 대해 배웠다.

특히 데이터 분석은 코드를 작성하는 것부터 시작하는 것이 아니라, **해결하려는 문제와 질문을 먼저 정확하게 정의하는 것**이 중요하다는 것을 배웠다.

---

# 데이터베이스

## 데이터 분석에 필요한 데이터 구조 이해

데이터 분석을 하기 위해서는 먼저 데이터가 **어떤 구조로 저장되어 있는지** 이해해야 한다.

쇼핑몰 데이터를 예로 들면 다음과 같이 여러 테이블로 나누어져 있을 수 있다.

```text
customers
products
orders
order_details
```

각 테이블은 서로 다른 정보를 가지고 있으며, `customer_id`, `order_id`, `product_id`와 같은 키를 이용해서 서로 연결할 수 있다.

---

## Table, Row, Column

### Table(테이블)

관련된 데이터를 모아놓은 표이다.

### Row(로우) — 행

테이블에 들어 있는 **하나의 데이터 단위**이다.

데이터를 볼 때 가장 먼저 확인해야 하는 것은

> **"한 행은 무엇을 의미하는가?"**

이다.

예를 들어:

* `customers` → 한 행 = 고객 한 명
* `orders` → 한 행 = 주문 한 건
* `order_details` → 한 행 = 주문에 포함된 상품 하나

처럼 테이블마다 한 행이 의미하는 대상이 다를 수 있다.

### Column(컬럼) — 열

데이터가 가지고 있는 정보의 종류이다.

예를 들어 고객 테이블이라면:

```text
customer_id
name
age
```

등이 컬럼이 될 수 있다.

---

## 테이블 간 관계

여러 테이블에 데이터가 나누어져 있다면 서로 어떤 관계를 가지고 있는지 이해해야 한다.

예를 들어:

```text
customers
    │
customer_id
    │
orders
    │
order_id
    │
order_details
```

처럼 키를 이용해 데이터를 연결할 수 있다.

데이터베이스에서 배운 **PK(Primary Key), FK(Foreign Key), JOIN** 등의 개념은 이후 데이터를 분석할 때도 여러 테이블의 관계를 이해하는 데 연결된다.

---

## DISTINCT

**DISTINCT(디스팅트)**는 중복을 제외하고 **고유한 값만 확인하는 것**이다.

예를 들어 고객의 구매 카테고리를 확인할 때 같은 카테고리가 여러 번 존재하더라도 고유한 카테고리 종류만 확인할 수 있다.

---

# 데이터 분석

## 데이터 분석은 질문에서 시작한다

데이터 분석은 단순히 데이터를 가져와서 계산하는 작업이 아니다.

먼저 **무엇을 알고 싶은지 질문을 정해야 한다.**

예를 들어

> "요즘 매출이 줄어든 것 같은데 왜 그런가요?"

라는 질문은 바로 분석하기 어렵다.

여기에는 여러 가지 기준이 빠져 있기 때문이다.

* '요즘'은 언제인가?
* 어떤 매출을 말하는가?
* 취소된 주문은 포함하는가?
* 환불된 주문은 어떻게 처리하는가?
* 무엇과 비교할 것인가?

따라서 질문을 분석할 수 있는 형태로 구체적으로 바꿔야 한다.

예를 들어:

> **2026년 8월의 실제 매출이 7월보다 감소했는가?**

처럼 분석 대상과 기간, 비교 기준을 명확하게 정할 수 있다.

---

## 분석 질문의 기준 정하기

질문을 만들 때는 다음과 같은 기준을 확인해야 한다.

| 기준    | 확인할 내용                |
| ----- | --------------------- |
| 분석 대상 | 무엇을 분석할 것인가?          |
| 기간    | 언제부터 언제까지 볼 것인가?      |
| 기준    | 무엇을 기준으로 판단할 것인가?     |
| 비교 대상 | 무엇과 비교할 것인가?          |
| 처리 기준 | 취소·환불 등을 어떻게 처리할 것인가? |

질문이 명확해지면 그때 필요한 데이터와 컬럼도 자연스럽게 정할 수 있다.

---

## "잘 팔리는 상품"의 기준

예를 들어

> "어떤 상품이 잘 팔리는가?"

라는 질문도 기준에 따라 결과가 달라질 수 있다.

'잘 팔린다'를 다음 중 무엇으로 정의하느냐에 따라 분석 결과가 달라진다.

* 판매 수량이 많은 상품
* 주문 횟수가 많은 상품
* 매출 금액이 높은 상품
* 구매한 고객 수가 많은 상품
* 재구매가 많은 상품

따라서 분석에서는 **용어와 기준을 먼저 정의하는 것**이 중요하다.

---

## 데이터 구조 확인

질문을 정한 다음에는 실제 데이터를 확인해야 한다.

Python에서는 **Pandas(판다스)**를 이용해 데이터를 불러오고 확인할 수 있다.

```python
import pandas as pd

df = pd.read_csv("data.csv")

df.head()
df.tail()
df.shape
```

### `head()`

데이터의 앞부분을 확인한다.

### `tail()`

데이터의 뒷부분을 확인한다.

### `shape`

데이터의 크기를 확인한다.

```text
(1000, 8)
```

이라면 **행 1,000개, 열 8개**라는 의미이다.

---

## 필요한 컬럼 선택하기

데이터에 컬럼이 많다고 해서 모든 컬럼을 사용할 필요는 없다.

분석 질문에 필요한 컬럼을 먼저 찾아야 한다.

예를 들어 매출을 분석한다면:

```text
order_date
amount
order_status
```

같은 데이터가 중요할 수 있다.

즉,

```text
분석 질문
   ↓
필요한 데이터
   ↓
필요한 컬럼
```

순서로 생각해야 한다.

---

## 데이터 전처리

**Preprocessing(프리프로세싱) — 전처리**는 데이터를 분석하기 전에 정리하는 과정이다.

주요 전처리에는 다음과 같은 작업이 있다.

* 결측값 확인 및 처리
* 중복 데이터 제거
* 잘못된 데이터 수정
* 날짜 형식 정리
* 데이터 타입 변환
* 취소 및 환불 데이터 처리

특히 데이터의 값이 존재한다고 해서 무조건 분석에 사용할 수 있는 것은 아니다.

---

## 주문 상태와 파생 데이터

데이터에 `order_status`라는 컬럼이 직접 존재하지 않을 수도 있다.

대신 다음과 같은 컬럼이 있다면:

```text
completed_at
cancelled_at
refunded_at
```

이 정보를 이용해서 주문 상태를 판단하거나 새로운 데이터를 만들 수 있다.

이처럼 **기존 데이터를 이용해 분석에 필요한 새로운 정보를 만드는 것**도 데이터 처리 과정에서 중요하다.

---

## EDA

**EDA(Exploratory Data Analysis, 익스플로러토리 데이터 애널리시스)**는

**탐색적 데이터 분석**을 의미한다.

데이터를 본격적으로 분석하기 전에 여러 방향으로 데이터를 살펴보면서 특징과 패턴을 찾는 과정이다.

예를 들어:

* 월별 매출 확인
* 상품별 판매량 확인
* 고객별 구매 금액 확인
* 구매 패턴 확인
* 이상한 데이터 확인

등을 할 수 있다.

EDA를 통해

> "이 데이터에는 어떤 특징이 있는가?"

를 먼저 파악한다.

---

## 시각화

**Visualization(비주얼라이제이션) — 시각화**는 데이터를 그래프로 표현하는 것이다.

대표적인 Python 시각화 도구로 **Matplotlib(맷플롯립)**이 있다.

그래프의 목적은 단순히 예쁘게 만드는 것이 아니다.

숫자로만 봤을 때 발견하기 어려운 **변화와 패턴을 쉽게 확인하고 다른 사람에게 전달하기 위해 사용한다.**

따라서 분석 목적에 맞는 그래프를 선택하는 것이 중요하다.

---

## 고객 구매 패턴 분석

고객 데이터를 분석할 때는 단순히 총 구매 금액만 확인하는 것이 아니라 여러 가지 특징을 볼 수 있다.

```text
총 구매 금액
구매 횟수
구매 간격
재구매 여부
구매 카테고리
반품·환불 여부
```

이러한 정보를 이용하면 고객의 구매 패턴이나 재구매 행동 등을 분석할 수 있다.

**Retention(리텐션)**은 고객이 계속 이용하거나 다시 구매하는 등의 유지·재방문 행동을 의미한다.

---

## Feature와 Feature Engineering

**Feature(피처)**는 데이터를 설명하는 특징 또는 변수이다.

예를 들어 고객의:

```text
나이
총 구매 금액
구매 횟수
마지막 구매일
구매 카테고리
```

등이 분석에 활용할 수 있는 Feature가 될 수 있다.

컬럼이 많다고 무조건 좋은 것은 아니며, **분석 목적에 맞는 의미 있는 Feature를 선택하는 것**이 중요하다.

**Feature Engineering(피처 엔지니어링)**은 기존 데이터를 이용해 분석에 더 유용한 새로운 Feature를 만드는 과정이다.

---

# AI 활용

## AI를 데이터 분석의 보조 도구로 사용하기

AI나 LLM은 데이터 분석 과정에서 여러 가지 작업을 도와줄 수 있다.

* 분석 질문 후보 만들기
* 필요한 컬럼 찾기
* Pandas 코드 작성
* 코드 오류 해결
* 그래프 작성
* 분석 결과 해석 초안 작성
* 보고서 작성 보조

하지만 AI가 제시한 결과를 그대로 사용하는 것은 위험하다.

---

## 코드 실행 성공과 분석 성공은 다르다

데이터 분석에서 매우 중요한 내용이다.

> **코드가 오류 없이 실행된다고 해서 분석 결과가 올바른 것은 아니다.**

예를 들어 매출을 계산하는 코드가 정상적으로 실행되더라도 취소 주문이나 환불 주문까지 포함했다면 실제 매출과 다른 결과가 나올 수 있다.

따라서 항상

```text
코드가 실행되는가?
        ↓
내가 원하는 데이터를 계산하고 있는가?
        ↓
분석 기준이 올바른가?
        ↓
결과를 제대로 해석했는가?
```

를 확인해야 한다.

---

## 상관관계와 인과관계

**Correlation(코릴레이션) — 상관관계**

두 데이터가 함께 변화하는 관계이다.

**Causation(코제이션) — 인과관계**

한 요인이 다른 요인의 원인이 되는 관계이다.

두 데이터가 함께 증가했다고 해서 한쪽이 다른 쪽의 원인이라고 바로 결론 내릴 수는 없다.

따라서:

> **상관관계가 있다고 해서 인과관계가 증명되는 것은 아니다.**

라는 점을 기억해야 한다.

---

## AI 사용 시 검증이 필요한 이유

AI에게 데이터를 분석해 달라고 할 때도 먼저 **분석 목적과 데이터 구조를 정확하게 알려주는 것**이 중요하다.

특히:

* 분석 목적
* 데이터의 구조
* 컬럼 목록
* 필요한 데이터의 일부

등을 명확하게 제공해야 한다.

AI가 만들어준 코드나 해석은 그대로 믿기보다 실제 데이터와 비교하여 검증해야 한다.

또한 AI를 사용한 경우 어떤 질문을 했고 어떤 코드를 사용했는지 기록해두면 나중에 결과를 다시 확인하거나 수정하기 쉽다.

---

# 데이터 분석 전체 흐름

데이터 분석은 다음과 같은 과정으로 진행된다.

```text
문제 정의
    ↓
데이터 수집
    ↓
데이터 구조 이해
    ↓
전처리
    ↓
EDA
    ↓
시각화
    ↓
결과 해석
    ↓
보고서 작성
    ↓
의사결정
```

중요한 것은 **코딩부터 시작하지 않는 것**이다.

먼저 질문을 정의하고 데이터를 이해한 뒤 필요한 작업을 진행해야 한다.

---

# 머신러닝과 데이터 분석

## EDA와 머신러닝의 차이

EDA는 사람이 데이터를 직접 탐색하면서 데이터의 특징과 패턴을 찾는 과정이다.

머신러닝은 데이터를 이용해 컴퓨터가 패턴을 학습하고 새로운 데이터를 예측하거나 분류하는 과정이다.

---

## Regression

**Regression(리그레션) — 회귀**

연속적인 숫자 값을 예측하는 방법이다.

예:

```text
다음 달 매출 예측
집값 예측
고객의 예상 구매 금액
```

---

## Classification

**Classification(클래시피케이션) — 분류**

데이터를 특정 범주로 나누거나 어떤 범주에 속하는지 예측하는 방법이다.

예:

```text
구매 / 미구매
정상 / 불량
이탈 고객 / 유지 고객
```

---

# 오늘의 핵심 정리

## 데이터베이스에서 기억할 것

데이터 분석을 하기 위해서는 데이터가 어떻게 저장되어 있는지 이해해야 한다.

```text
Table
→ Row / Column
→ 테이블 간 관계
→ Key를 이용한 연결
→ 필요한 데이터 조회
```

특히 **한 행이 무엇을 의미하는지** 이해하는 것이 중요하다.

---

## 데이터 분석에서 기억할 것

데이터 분석은 단순히 Pandas 코드를 작성하는 것이 아니다.

```text
문제 정의
→ 질문 구체화
→ 데이터 구조 확인
→ 전처리
→ EDA
→ 시각화
→ 해석
→ 의사결정
```

이라는 전체 과정을 이해해야 한다.

---

## 가장 중요하게 배운 것

> **좋은 데이터 분석은 코드를 잘 작성하는 것에서 시작하지 않는다.**
>
> **무엇을 알고 싶은지 정확하게 질문하고, 데이터의 의미를 제대로 이해하는 것에서 시작한다.**

그리고 분석 결과를 판단할 때는 항상

> **"이 코드가 실행됐는가?"가 아니라 "이 결과가 내가 정의한 질문에 대한 올바른 답인가?"**

를 확인해야 한다.
