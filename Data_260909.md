# 2026-09-09 TIL Pandas 데이터 전처리와 집계 분석의 기본 흐름

### 오늘의 주제

> 데이터 구조를 파악하고 필요한 데이터를 선택·병합한 뒤, 결측값과 날짜를 처리하여 집계 가능한 데이터로 만드는 방법

---

## 오늘 배운 것

## 데이터베이스

### 1. 테이블은 역할에 따라 분리되어 있다

실습 데이터는 하나의 파일에 모든 정보가 들어 있는 것이 아니라 여러 테이블로 나뉘어 있다.

```text
customers
orders
order_items
products
```

예를 들어 `order_items`에는 상품과 수량 정보가 있지만 주문 상태는 `orders`에 있다.

따라서 서로 다른 테이블의 정보를 함께 사용하려면 연결 작업이 필요하다.

```text
데이터베이스 → JOIN
Pandas → merge()
```

---

### 2. 주문과 주문 상세의 관계

하나의 주문에는 여러 상품이 포함될 수 있다.

```text
orders       1
              \
               N
order_items
```

* `orders` : 부모
* `order_items` : 자식
* 연결 기준 : `order_id`

따라서 `order_items`를 기준으로 `orders`의 정보를 가져오는 경우 `many_to_one` 관계로 볼 수 있다.

---

### 3. 주문 건수와 주문 상세 행 수는 다르다

하나의 주문에 상품이 여러 개 들어가면 `order_id`가 여러 행에서 반복될 수 있다.

따라서 단순한 행 개수와 실제 주문 건수는 다를 수 있다.

```python
count()
```

은 전체 행의 개수를 세는 개념이고,

```python
nunique()
```

는 중복을 제거한 고유 값의 개수를 센다.

예를 들어 실제 주문 건수를 구하려면 중복되는 `order_id`를 고려해야 한다.

---

### 4. JOIN과 Pandas merge의 연결

Pandas에서는 데이터베이스의 JOIN과 비슷한 역할을 `merge()`가 수행한다.

```python
order_sales = order_items.merge(
    orders[
        [
            "order_id",
            "customer_id",
            "order_date",
            "order_status",
        ]
    ],
    on="order_id",
    how="left",
    validate="many_to_one",
)
```

여기서 중요한 것은 다음과 같다.

```text
무엇을 합칠 것인가?
어떤 키로 연결할 것인가?
어느 쪽 데이터를 기준으로 남길 것인가?
관계가 1:N인지 N:1인지?
```

---

## 데이터 분석

### 1. 데이터 분석 전에 먼저 데이터를 확인한다

데이터프레임을 만들었다면 가장 먼저 데이터의 상태를 확인해야 한다.

```python
df.info()
```

`info()`를 사용하면 다음을 확인할 수 있다.

* 행의 개수
* 컬럼 이름
* 데이터 타입
* 각 컬럼의 Non-Null 개수
* 결측값 존재 여부

데이터를 받자마자 분석하지 말고 먼저 구조와 상태를 확인해야 한다.

---

### 2. Series와 DataFrame

Pandas에서 기본적으로 사용하는 두 가지 구조가 있다.

```text
Series     → 하나의 컬럼과 같은 1차원 데이터
DataFrame  → 여러 컬럼을 가진 표 형태 데이터
```

예를 들어:

```python
customers["age"]
```

처럼 하나의 컬럼을 가져오면 Series가 된다.

여러 컬럼을 선택하면 DataFrame이 된다.

```python
customers[["name", "age", "city"]]
```

---

### 3. 범주형 데이터와 value_counts()

주문 상태나 결제 수단처럼 정해진 몇 가지 값 중 하나를 가지는 데이터를 범주형 데이터라고 한다.

예:

```text
주문 상태
- completed
- cancelled
- refunded
```

범주형 데이터의 종류와 개수를 확인할 때는:

```python
value_counts()
```

를 사용한다.

```python
orders["order_status"].value_counts(dropna=False)
```

`dropna=False`를 사용하면 결측값도 포함하여 확인할 수 있다.

---

### 4. unique()와 value_counts()의 차이

```python
unique()
```

는 어떤 값들이 존재하는지 확인한다.

```python
customers["city"].unique()
```

반면:

```python
value_counts()
```

는 각 값이 몇 번 등장하는지까지 확인한다.

```python
customers["city"].value_counts()
```

정리하면:

```text
unique()       → 종류 확인
value_counts() → 종류 + 개수 확인
```

---

### 5. Boolean Mask를 이용한 필터링

원하는 조건을 만족하는 행만 선택할 때 Boolean Mask를 사용한다.

예를 들어 30세 이상 고객:

```python
customers_over_30 = customers[
    customers["age"] >= 30
]
```

조건식의 결과는 각 행마다:

```text
True
False
True
False
...
```

형태로 만들어진다.

이 중 `True`인 행만 데이터프레임에 남는다.

Boolean Mask는 체로 데이터를 걸러내는 것과 비슷하다.

---

### 6. 여러 조건을 함께 사용할 수 있다

Pandas에서는 여러 조건도 사용할 수 있다.

```text
AND → &
OR  → |
NOT → ~
```

여러 값 중 하나에 해당하는지 확인할 때는 `isin()`을 사용할 수 있다.

```python
city_customers = customers[
    customers["city"].isin(
        ["서울", "부산", "인천"]
    )
]
```

---

### 7. 원본 보존을 위한 copy()

데이터를 수정하기 전에 원본과 작업 데이터를 분리하는 것이 안전하다.

```python
orders_test = orders.copy()
```

이렇게 하면:

```text
orders       → 원본
orders_test  → 작업본
```

으로 사용할 수 있다.

날짜 변환, 결측값 처리, 파생 컬럼 생성처럼 데이터가 크게 달라질 수 있는 작업은 복사본에서 진행하는 것이 안전하다.

---

### 8. 파생 컬럼 만들기

기존 컬럼을 이용해 새롭게 만든 컬럼을 파생 컬럼이라고 한다.

이번 실습에서는:

```text
수량 × 단가 = 한 주문 상세 행의 금액
```

을 계산하였다.

```python
order_items["line_total"] = (
    order_items["quantity"]
    * order_items["unit_price"]
)
```

`line_total`은 원본 데이터에는 없었지만 기존 컬럼을 이용하여 만들어진 새로운 컬럼이다.

---

### 9. line_total은 전체 주문 금액이 아니다

`line_total`은 주문 전체 금액이 아니라 **주문 상세 한 줄의 금액**이다.

예:

```text
아메리카노 2개 × 4,000원 = 8,000원
라떼       1개 × 5,000원 = 5,000원
```

한 주문의 상품이 두 종류라면 주문 상세 행도 두 개가 될 수 있다.

따라서 계산하는 데이터의 단위를 정확하게 이해해야 한다.

---

### 10. 계산된 금액을 바로 매출이라고 부르면 안 된다

전체 데이터에는 다음과 같은 주문 상태가 함께 있을 수 있다.

```text
completed
cancelled
refunded
```

따라서 모든 `line_total`을 더한 값을 바로 매출이라고 하면 안 된다.

완료된 주문의 매출을 분석하려면 먼저 완료 주문만 선택해야 한다.

```python
completed_sales = order_sales[
    order_sales["order_status"] == "completed"
].copy()
```

분석에서는 항상 **어떤 범위의 데이터를 계산했는지**가 중요하다.

---

### 11. groupby()는 "~별" 분석에 사용한다

요구사항에서 다음과 같은 표현이 나오면 `groupby()`를 생각할 수 있다.

```text
카테고리별
상품별
월별
고객별
연령대별
```

그룹 집계를 할 때는 세 가지를 생각한다.

```text
1. 무엇으로 묶을 것인가?
2. 어떤 값을 계산할 것인가?
3. 어떤 계산을 할 것인가?
```

예:

```text
카테고리별 매출 합계
```

라면:

```text
그룹 기준 → category
계산 대상 → line_total
집계 방법 → sum
```

이 된다.

대표적인 집계 함수는:

```text
sum
count
mean
min
max
nunique
```

등이 있다.

---

### 12. 집계 전에 필터링 범위를 먼저 정한다

예를 들어:

```text
카테고리별 완료 주문 금액
```

을 구한다면 먼저 완료 주문만 필터링한다.

```text
전체 데이터
    ↓ Boolean Mask
completed 데이터
    ↓ groupby
카테고리별 집계
```

즉:

```text
필터링 → 그룹화 → 집계
```

의 흐름을 기억한다.

---

### 13. count와 nunique는 목적이 다르다

```python
count()
```

는 전체 개수를 센다.

```python
nunique()
```

는 중복되지 않는 값의 개수를 센다.

예를 들어 주문 상세에는 같은 `order_id`가 여러 번 등장할 수 있다.

따라서:

```text
주문 상세 행 수 → count
실제 주문 건수 → order_id의 nunique
```

처럼 목적에 맞게 선택해야 한다.

---

### 14. 날짜처럼 보인다고 날짜 타입은 아니다

예를 들어:

```text
2026-07-02
```

처럼 보이더라도 실제 데이터 타입이 문자열일 수 있다.

따라서 반드시 타입을 확인해야 한다.

```python
orders.info()
```

또는:

```python
orders["order_date"].dtype
```

---

### 15. 날짜 타입 변환

문자열 날짜를 Pandas의 날짜 타입으로 변환할 때:

```python
pd.to_datetime()
```

을 사용한다.

```python
orders_test["order_date"] = pd.to_datetime(
    orders_test["order_date"]
)
```

날짜 타입으로 변환해야 날짜 계산과 월별 분석 등을 제대로 할 수 있다.

---

### 16. 잘못된 날짜와 errors="coerce"

실제 데이터에는 다음과 같은 잘못된 값이 있을 수 있다.

```text
2026-02-30
```

2월 30일은 존재하지 않기 때문에 일반적인 날짜 변환을 하면 오류가 발생한다.

이때:

```python
orders_test["order_date"] = pd.to_datetime(
    orders_test["order_date"],
    errors="coerce",
)
```

처럼 처리하면 변환할 수 없는 값은 `NaT`가 된다.

```text
NaT = Not a Time
```

날짜 데이터에서 사용하는 결측값이라고 이해하면 된다.

---

### 17. 결측값 확인

특정 컬럼의 결측값 개수를 확인할 때:

```python
orders_test["order_date"].isna().sum()
```

을 사용할 수 있다.

```text
isna()
→ 각 값이 결측이면 True

sum()
→ True의 개수 계산
```

따라서 `isna().sum()`은 결측값 개수를 구하는 대표적인 방법이다.

---

### 18. 결측값을 무조건 삭제하면 안 된다

결측값이나 이상값을 발견했다고 바로 삭제하면 안 된다.

먼저 원인을 확인해야 한다.

```text
결측값 발견
    ↓
원인 확인
    ↓
대체할 수 있는가?
    ├─ 가능 → 적절한 값으로 대체
    └─ 불가능 → 삭제 여부 검토
```

대체 방법은 데이터의 성격에 따라 달라질 수 있다.

예:

```text
숫자 데이터 → 평균값 / 중앙값
범주형 데이터 → 최빈값
날짜 데이터 → 데이터 특성에 맞는 기준값
```

어떤 방법이 항상 정답인 것은 아니며 데이터의 의미를 먼저 확인해야 한다.

---

### 19. dropna()는 행을 삭제할 수 있으므로 주의한다

```python
df.dropna()
```

를 사용하면 결측값이 있는 행 자체가 제거될 수 있다.

따라서 날짜 하나가 비어 있다는 이유만으로 해당 행의 다른 중요한 데이터까지 버려질 수 있다.

결측값 제거는 신중하게 결정해야 한다.

---

### 20. loc와 iloc

특정 행이나 값을 선택할 때 다음 기능을 사용할 수 있다.

```text
loc  → 라벨이나 컬럼 이름을 기준으로 선택
iloc → 위치 번호를 기준으로 선택
```

예를 들어 특정 위치의 값을 확인하거나 수정할 때 사용한다.

이번 강의에서는 일부러 잘못된 날짜 값을 넣어 날짜 변환 오류를 확인하는 실습에 활용하였다.

---

### 21. 날짜 타입으로 바꾸는 이유

문자열과 날짜가 화면에서는 비슷하게 보일 수 있다.

하지만 실제 날짜 타입으로 변환하면 날짜 전용 기능을 사용할 수 있다.

```python
df["order_date"].dt
```

이를 이용하면 연도, 월 등 날짜 정보를 추출할 수 있다.

월별 분석을 하기 위해서는 주문 날짜를 이용해 파생 컬럼을 만들 수 있다.

```text
order_date
    ↓
연월 추출
    ↓
order_month
    ↓
월별 groupby
```

단순히 `5월`처럼 월만 저장하면 다른 연도의 5월과 섞일 수 있으므로 데이터 기간이 여러 해에 걸쳐 있다면 `연도-월` 형태를 사용하는 것이 안전하다.

---

### 22. 파생 컬럼을 만든 뒤 반드시 검증한다

파생 컬럼이 생성되었다고 끝난 것이 아니다.

예를 들어:

```python
order_items["line_total"]
```

을 만들었다면 대표 행을 확인해서:

```text
quantity × unit_price = line_total
```

이 실제로 맞는지 확인해야 한다.

파생 컬럼이 잘못되면 이후:

```text
카테고리별 집계
상품별 집계
월별 집계
고객별 집계
```

도 모두 잘못된다.

---

### 23. 분석 결과는 반드시 검산한다

집계 결과가 그럴듯하게 보인다고 해서 맞는 것은 아니다.

확인해야 할 것:

```text
필터링 범위가 같은가?
행 개수가 예상과 맞는가?
결측값은 없는가?
데이터 타입은 올바른가?
merge 후 행 개수가 이상하게 증가하지 않았는가?
그룹별 합계와 원본 합계가 일치하는가?
```

코드가 오류 없이 실행되는 것과 분석 결과가 정확한 것은 다른 문제다.

---

### 24. 정리된 데이터는 다시 저장할 수 있다

전처리가 끝난 데이터프레임은 컴퓨터 메모리에 존재한다.

프로그램을 종료하면 다시 처음부터 전처리를 해야 할 수 있기 때문에 정리된 데이터를 파일로 저장할 수 있다.

```python
df.to_csv(...)
```

이렇게 저장해 두면 다음 분석에서는 전처리 과정을 반복하지 않고 정리된 데이터를 바로 불러올 수 있다.

저장 후에도 다시 읽어:

```text
행 개수
컬럼 개수
합계
데이터 타입
```

등을 다시 확인하는 것이 안전하다.

---

## 핵심 개념 정리

| 개념                 | 의미                    |
| ------------------ | --------------------- |
| `info()`           | 데이터프레임 구조, 타입, 결측값 확인 |
| `head()`           | 앞쪽 데이터 확인             |
| `value_counts()`   | 값별 빈도 확인              |
| `unique()`         | 고유한 값의 종류 확인          |
| Boolean Mask       | 조건에 맞는 행 필터링          |
| `isin()`           | 여러 값 중 하나에 포함되는지 확인   |
| `copy()`           | 원본을 보존하고 작업본 생성       |
| 파생 컬럼              | 기존 컬럼을 계산하여 만든 새 컬럼   |
| `groupby()`        | "~별" 기준으로 데이터를 묶음     |
| `sum()`            | 합계                    |
| `count()`          | 전체 개수                 |
| `nunique()`        | 고유 값 개수               |
| `merge()`          | 여러 데이터프레임 연결          |
| `pd.to_datetime()` | 날짜 타입 변환              |
| `pd.to_numeric()`  | 숫자 타입 변환              |
| `errors="coerce"`  | 변환 실패 값을 결측값으로 처리     |
| `isna().sum()`     | 결측값 개수 확인             |
| `dropna()`         | 결측값이 있는 데이터 제거        |
| `loc`              | 라벨 기준 선택              |
| `iloc`             | 위치 기준 선택              |
| `.dt`              | 날짜 타입 전용 기능           |
| `to_csv()`         | 데이터프레임을 CSV로 저장       |

---

## 가장 중요하게 느낀 것

데이터 분석에서는 코드를 실행하는 것보다 **분석에 사용되는 데이터가 올바른지 확인하는 과정이 더 중요하다.**

특히:

```text
데이터 확인
→ 타입 확인
→ 결측값 확인
→ 필요한 데이터 선택
→ 데이터 병합
→ 파생 컬럼 생성
→ 필터링
→ 그룹화와 집계
→ 결과 검증
→ 저장
```

의 흐름을 이해해야 한다.

분석 결과가 깔끔하게 나왔다고 해서 반드시 올바른 결과라는 뜻은 아니다.

---

## 실습·복습 체크리스트

* [ ] DataFrame과 Series의 차이를 설명할 수 있다.
* [ ] `info()`로 데이터의 구조와 결측값을 확인할 수 있다.
* [ ] `value_counts()`가 필요한 상황을 설명할 수 있다.
* [ ] `unique()`와 `value_counts()`의 차이를 설명할 수 있다.
* [ ] Boolean Mask로 원하는 행을 필터링할 수 있다.
* [ ] `isin()`으로 여러 조건을 처리할 수 있다.
* [ ] 원본을 보호하기 위해 `copy()`를 사용할 수 있다.
* [ ] 기존 컬럼을 이용해 파생 컬럼을 만들 수 있다.
* [ ] `line_total = quantity × unit_price`의 의미를 설명할 수 있다.
* [ ] 전체 주문 금액과 완료 주문 금액이 다른 이유를 설명할 수 있다.
* [ ] "~별"이라는 요구사항에서 `groupby()`를 떠올릴 수 있다.
* [ ] 그룹 기준, 계산 대상, 집계 함수를 구분할 수 있다.
* [ ] `count()`와 `nunique()`의 차이를 설명할 수 있다.
* [ ] 데이터베이스 JOIN과 Pandas `merge()`의 관계를 설명할 수 있다.
* [ ] `many_to_one` 관계를 이해할 수 있다.
* [ ] 문자열 날짜를 `datetime`으로 변환할 수 있다.
* [ ] `errors="coerce"`의 역할을 설명할 수 있다.
* [ ] `NaT`가 무엇인지 설명할 수 있다.
* [ ] `isna().sum()`으로 결측값을 확인할 수 있다.
* [ ] 결측값과 이상값을 발견했을 때 바로 삭제하면 안 되는 이유를 설명할 수 있다.
* [ ] 날짜에서 연월 파생 컬럼을 만드는 이유를 설명할 수 있다.
* [ ] 집계 후 원본 합계와 결과 합계를 비교하여 검산할 수 있다.

---

## 실습 예시 코드

```python
import pandas as pd

# 데이터 확인
print(orders.info())
print(orders.head())

# 범주형 데이터 빈도 확인
print(
    orders["order_status"]
    .value_counts(dropna=False)
)

# 30세 이상 고객
customers_over_30 = customers[
    customers["age"] >= 30
]

# 여러 도시 필터링
city_customers = customers[
    customers["city"].isin(
        ["서울", "부산", "인천"]
    )
]

# 원본 보호
order_items_test = order_items.copy()

# 파생 컬럼
order_items_test["line_total"] = (
    order_items_test["quantity"]
    * order_items_test["unit_price"]
)

# 주문 정보 연결
order_sales = order_items_test.merge(
    orders[
        [
            "order_id",
            "customer_id",
            "order_date",
            "order_status",
        ]
    ],
    on="order_id",
    how="left",
    validate="many_to_one",
)

# 완료 주문만 선택
completed_sales = order_sales[
    order_sales["order_status"] == "completed"
].copy()

# 날짜 타입 변환
completed_sales["order_date"] = pd.to_datetime(
    completed_sales["order_date"],
    errors="coerce",
)

# 날짜 결측값 확인
print(
    completed_sales["order_date"]
    .isna()
    .sum()
)

# 연월 파생 컬럼
completed_sales["order_month"] = (
    completed_sales["order_date"]
    .dt.to_period("M")
    .astype(str)
)

# 월별 완료 주문 금액
monthly_sales = (
    completed_sales
    .groupby("order_month")["line_total"]
    .sum()
)

print(monthly_sales)
```

---

## 오늘의 핵심 한 줄

> 데이터 분석은 바로 계산하는 것이 아니라, 데이터를 확인하고 정리하고 연결하고 검증한 뒤 올바른 범위에서 집계하는 과정이다.

---

## 다음 할 일

1. `info()`, `value_counts()`, Boolean Mask를 직접 반복해서 사용해 보기
2. `groupby()` 문제에서 `무엇별 / 무엇을 / 어떤 계산`인지 먼저 적어 보기
3. `merge()`에서 부모·자식과 `1:N` 관계 다시 확인하기
4. `count()`와 `nunique()`를 직접 비교해 보기
5. 잘못된 날짜를 만들어 `errors="coerce"`와 `NaT` 확인하기
6. 날짜에서 연월 파생 컬럼을 만들고 월별 집계해 보기
7. 집계 결과와 원본 데이터의 합계를 비교하며 검산하는 습관 들이기
