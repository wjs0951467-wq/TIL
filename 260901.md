# 2026-09-01 TIL 데이터 분석의 시작, Pandas와 DataFrame

## 오늘 배운 것

파이썬 기초를 마치고 데이터 분석으로 넘어가면서 **Pandas(판다스)**를 처음 배웠다.
Pandas는 데이터를 표 형태로 다루고 분석할 수 있게 해주는 파이썬 라이브러리이다.

데이터 분석은 단순히 데이터를 읽는 것에서 끝나는 것이 아니라,

**데이터 불러오기 → 데이터 구조 확인 → 결측값 확인 → 전처리 → 분석**

순서로 진행하는 것이 중요하다.

---

## 1. Python과 데이터 분석

파이썬은 다양한 분야에서 사용할 수 있지만 특히 **데이터 분석, 머신러닝, 딥러닝, AI** 분야에서 많이 사용된다.

* 데이터 분석(Data Analysis): 데이터를 정리하고 의미 있는 정보를 찾아내는 과정
* 머신러닝(Machine Learning): 데이터를 이용해 컴퓨터가 패턴을 학습하는 방법
* 딥러닝(Deep Learning): 신경망을 이용한 머신러닝의 한 분야
* AI(Artificial Intelligence): 인공지능

파이썬에서는 이러한 작업을 쉽게 할 수 있도록 다양한 라이브러리를 제공한다.

---

## 2. Pandas란?

**Pandas(판다스)**는 파이썬에서 데이터를 분석하고 처리하기 위한 라이브러리이다.

파이썬 기본 기능만 사용하면 CSV 같은 데이터를 직접 읽고, 반복문으로 하나씩 처리하고, 데이터 타입도 직접 변환해야 하는 경우가 많다.

Pandas를 사용하면 데이터를 **DataFrame(데이터프레임)**이라는 표 형태로 가져와 쉽게 다룰 수 있다.

Pandas는 파이썬 기본 라이브러리에 포함되어 있지 않기 때문에 별도로 설치해야 한다.

```bash
python -m pip install pandas
```

사용할 때는 보통 `pd`라는 이름으로 줄여서 사용한다.

```python
import pandas as pd
```

여기서 `pd`는 Pandas를 대신해서 사용하는 별칭(alias)이다.

---

## 3. API란?

**API(Application Programming Interface)**는 프로그램과 프로그램이 서로 기능을 사용할 수 있도록 연결해주는 접점이다.

쉽게 말하면 다른 프로그램의 기능이나 데이터를 사용할 수 있도록 만들어 놓은 **창구 또는 연결 통로**라고 생각하면 된다.

Pandas 역시 다양한 기능을 API 형태로 제공하기 때문에 우리가 `pd.read_csv()` 같은 명령을 사용해서 기능을 이용할 수 있다.

---

## 4. Series와 DataFrame

Pandas에서 가장 기본이 되는 자료 구조는 **Series(시리즈)**와 **DataFrame(데이터프레임)**이다.

### Series

Series는 기본적으로 **하나의 열(Column)**을 표현하는 자료 구조이다.

```python
scores = pd.Series([85, 92, 88, 78])

print(scores)
```

실행하면 데이터와 함께 인덱스가 표시된다.

```text
0    85
1    92
2    88
3    78
```

Pandas에서는 데이터를 구분하고 접근하기 위해 **Index(인덱스)**를 사용한다.

기본적으로 0부터 시작하는 숫자 인덱스가 자동으로 만들어진다.

### DataFrame

DataFrame은 데이터를 **행(Row)과 열(Column)을 가진 표 형태**로 표현한다.

```python
data = {
    "name": ["철수", "영희", "민수"],
    "score": [85, 92, 78]
}

df = pd.DataFrame(data)

print(df)
```

결과는 다음과 같은 형태가 된다.

```text
  name  score
0   철수     85
1   영희     92
2   민수     78
```

정리하면:

* **Series** → 하나의 열을 다루는 형태
* **DataFrame** → 여러 열과 행으로 이루어진 표 형태
* **Index** → 데이터를 구분하고 접근하기 위한 기준

---

## 5. CSV 파일을 DataFrame으로 가져오기

데이터 분석에서는 CSV 파일을 많이 사용한다.

Pandas에서는 `read_csv()`를 이용하면 CSV 파일을 DataFrame으로 바로 읽어올 수 있다.

```python
import pandas as pd

df = pd.read_csv("data.csv")

print(df)
```

`pd.read_csv()`는 CSV 파일을 읽어서 **DataFrame을 반환**한다.

따라서 보통 변수 `df`에 저장해서 사용한다.

```python
df = pd.read_csv("data.csv")
```

파일 경로는 현재 실행하는 파일의 위치에 따라 달라질 수 있기 때문에 프로젝트 내부의 폴더 구조를 일정하게 관리하는 것도 중요하다.

웹에 공개된 CSV 파일이라면 URL을 직접 넣는 것도 가능하다.

```python
df = pd.read_csv("https://example.com/data.csv")
```

---

## 6. DataFrame의 기본적인 확인 방법

데이터를 불러왔다면 바로 분석하기보다 **데이터가 어떤 형태인지 먼저 확인**해야 한다.

### head()

처음 5개의 데이터를 확인한다.

```python
df.head()
```

원하는 개수만큼 확인할 수도 있다.

```python
df.head(10)
```

### tail()

마지막 5개의 데이터를 확인한다.

```python
df.tail()
```

---

## 7. info()

데이터의 전체적인 구조를 확인할 때 매우 유용하다.

```python
df.info()
```

주로 다음과 같은 정보를 확인할 수 있다.

* 전체 행 개수
* 컬럼 개수
* 컬럼 이름
* 각 컬럼의 데이터 개수
* 결측값 여부를 확인할 수 있는 Non-Null Count
* 데이터 타입(dtype)

데이터를 처음 분석할 때 `info()`를 먼저 확인하면 데이터의 전체적인 구조를 파악하기 쉽다.

---

## 8. describe()

숫자형 데이터의 기본적인 통계 정보를 확인할 수 있다.

```python
df.describe()
```

대표적으로 다음 정보를 제공한다.

* `count` → 데이터 개수
* `mean` → 평균
* `std` → 표준편차
* `min` → 최솟값
* `25%` → 1사분위수
* `50%` → 중앙값
* `75%` → 3사분위수
* `max` → 최댓값

특히 **중앙값(median)**은 데이터를 크기순으로 정렬했을 때 가운데에 위치한 값으로, 평균과 함께 데이터를 파악할 때 중요한 통계값이다.

기본 `describe()`는 주로 숫자형 컬럼의 통계 정보를 보여준다.

---

## 9. Attribute와 Method의 차이

Pandas를 사용하다 보면 `()`가 붙는 것과 붙지 않는 것이 있다.

### Method(메서드)

실제로 어떤 작업을 실행하는 기능이다.

```python
df.head()
df.tail()
df.info()
df.describe()
```

괄호 `()`가 붙는다.

### Attribute(속성)

데이터가 가지고 있는 정보를 확인하는 것이다.

```python
df.shape
df.columns
df.dtypes
```

괄호 `()`를 사용하지 않는다.

예를 들어:

```python
print(df.shape)
```

`shape`를 통해 DataFrame의 **행과 열의 크기**를 확인할 수 있다.

```python
print(df.columns)
```

`columns`를 통해 컬럼 이름을 확인할 수 있다.

```python
print(df.dtypes)
```

각 컬럼의 데이터 타입을 확인할 수 있다.

---

## 10. 특정 컬럼 선택하기

DataFrame에서 특정 컬럼을 선택할 수 있다.

```python
df["score"]
```

이렇게 하나의 컬럼을 선택하면 **Series**가 반환된다.

반면 대괄호를 한 번 더 사용하면:

```python
df[["score"]]
```

**DataFrame** 형태로 반환된다.

즉,

```python
df["score"]      # Series
df[["score"]]    # DataFrame
```

이 차이를 구분하는 것이 중요하다.

---

## 11. 결측값 확인하기

데이터에는 값이 비어 있는 경우가 있을 수 있다.

이러한 값을 **결측값(Missing Value)**이라고 한다.

Pandas에서는 일반적으로 `NaN` 형태로 표시되는 경우가 많다.

결측값이 있는지 확인하려면:

```python
df.isna()
```

각 데이터가 결측값이면 `True`, 아니면 `False`를 반환한다.

결측값의 개수를 확인하려면:

```python
df.isna().sum()
```

`True`는 계산할 때 1, `False`는 0으로 처리되기 때문에 컬럼별 결측값 개수를 계산할 수 있다.

결측값은 데이터 분석 결과에 영향을 줄 수 있기 때문에 **분석 전에 반드시 확인하고 필요한 전처리를 해야 한다.**

---

## 12. DataFrame의 장점

일반적인 텍스트 파일을 파이썬으로 직접 처리하면 데이터를 하나씩 읽고 반복문을 돌면서 원하는 형태로 변환해야 할 수 있다.

하지만 DataFrame을 사용하면 데이터가 이미 **행과 열을 가진 구조화된 형태**로 관리되기 때문에 데이터를 분석하기가 훨씬 편해진다.

즉, Pandas는 단순히 파일을 읽는 도구가 아니라 **데이터를 분석하기 위한 다양한 기능을 제공하는 도구**라고 볼 수 있다.

---

## 13. 데이터 분석을 시작할 때 확인할 것

데이터를 처음 가져왔다면 바로 계산하거나 분석하지 않고 먼저 데이터를 확인한다.

기본적인 흐름은 다음과 같다.

```text
1. 데이터 불러오기
        ↓
2. 데이터 구조 확인
        ↓
3. 데이터 일부 확인
        ↓
4. 통계 정보 확인
        ↓
5. 결측값 확인
        ↓
6. 데이터 전처리
        ↓
7. 데이터 분석
```

대표적으로 다음 코드를 사용할 수 있다.

```python
df.info()
df.head()
df.tail()
df.describe()
df.shape
df.columns
df.dtypes
df.isna().sum()
```

모든 코드를 항상 전부 사용할 필요는 없고, 데이터의 크기와 목적에 따라 필요한 정보를 확인하면 된다.

---

## 14. Pandas 버전 확인

현재 설치된 Pandas의 버전을 확인하려면 다음과 같이 작성한다.

```python
import pandas as pd

print(pd.__version__)
```

`__version__`은 라이브러리의 버전을 확인할 때 사용하는 대표적인 방법이다.

---

## 15. Chapter 23 과제

이번 강의에서는 다음 단계의 개인 지출 관리 프로그램 과제도 안내했다.

먼저 **콘솔 프로그램**을 완성하고 이후 **웹 프로그램**으로 확장하는 방식이다.

콘솔 프로그램은 `.py` 파일로 작성하는 것이 적절하며, 개인별로 자신의 저장소에 제출해야 한다.

웹 프로그램으로 확장할 때는 다음 조건을 지켜야 한다.

* 웹 페이지는 **5개 이하**
* 데이터베이스 테이블은 **5개 이하**
* AI를 활용한 Vibe Coding 가능
* 단, **Agent는 사용하지 않음**
* AI가 파일을 직접 수정하도록 맡기는 것이 아니라 코드를 받아서 직접 파일을 만들고 수정
* 콘솔 프로그램보다 실제로 사용하기 편한 웹 프로그램으로 발전시키는 것이 목표

이번 과제를 통해 단순히 파이썬 문법을 배우는 것에서 끝나는 것이 아니라, **파이썬으로 실제 데이터를 처리하고 하나의 프로그램을 만드는 과정**까지 연결하게 된다.

---

## 핵심 코드 정리

```python
import pandas as pd

# CSV 불러오기
df = pd.read_csv("data.csv")

# 데이터 확인
df.head()
df.tail()

# 데이터 구조 확인
df.info()

# 통계 정보 확인
df.describe()

# DataFrame 크기
df.shape

# 컬럼 이름
df.columns

# 데이터 타입
df.dtypes

# 결측값 확인
df.isna().sum()

# 컬럼 선택
df["score"]       # Series
df[["score"]]     # DataFrame

# Pandas 버전 확인
print(pd.__version__)
```

---

## 오늘의 핵심 정리

이번 강의에서는 파이썬을 이용한 **데이터 분석의 기본 도구인 Pandas**를 배웠다.

특히 **Series와 DataFrame의 차이**, CSV 데이터를 DataFrame으로 불러오는 방법, `head()`, `info()`, `describe()` 등을 이용한 데이터 확인 방법을 익혔다.

데이터 분석에서는 데이터를 가져온 뒤 바로 분석하는 것이 아니라 **데이터의 구조와 타입, 통계 정보, 결측값 등을 먼저 확인하는 과정이 중요하다.**

앞으로 Pandas를 이용해 데이터를 직접 선택하고, 수정하고, 정리하면서 본격적인 데이터 분석으로 이어갈 수 있다.
